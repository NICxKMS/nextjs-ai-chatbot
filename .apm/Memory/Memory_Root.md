# ai-assistant – APM Memory Root
**Memory Strategy:** Dynamic-MD + Repository Memory Baseline (`memory/`)
**Project Overview:** Runtime execution memory for manager-orchestrated migration tasks derived from the verified plan in `memory/final_plan_*.md`.

## Runtime Notes
- Use `memory/` as planning source-of-truth.
- Use `.apm/Memory/Phase_XX_*/Task_*.md` as execution evidence logs.
- Keep phase summaries concise and include links to both task logs and canonical phase/task docs.
