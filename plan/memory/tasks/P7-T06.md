---
task: P7-T06
title: "Create E2E test specs"
phase: P7
agent: hephaestus
depends_on: [P6-T14]
status: done
review: pass
attempt_count: 1
started: "2026-03-04 10:00"
finished: "2026-03-04 10:45"
has_deviations: true
has_issues: false
has_findings: true
---

## Summary

Created 4 comprehensive Playwright E2E test spec files covering auth, chat, artifacts, and sidebar flows. Added missing `data-testid` attributes to 4 component files and updated 2 existing testid names to match the P7-T06 spec.

## Files Changed

- `tests/e2e/auth.spec.ts` — **created** — Login, register, guest auto-bootstrap, logout E2E tests (10 tests)
- `tests/e2e/chat.spec.ts` — **created** — Send message, streaming, tool invocation, new chat, load existing chat E2E tests (9 tests)
- `tests/e2e/artifacts.spec.ts` — **created** — Text artifact, code artifact (CodeMirror), version navigation, close panel E2E tests (6 tests)
- `tests/e2e/sidebar.spec.ts` — **created** — Chat history, navigation, delete, new chat button, mobile sidebar toggle E2E tests (7 tests)
- `features/auth/components/auth-form.tsx` — **modified** — Added `data-testid="auth-form"` to form, `data-testid="login-button"/"register-button"` to submit button
- `features/voting/components/vote-buttons.tsx` — **modified** — Changed `data-testid="message-upvote"` → `"vote-up"`, `"message-downvote"` → `"vote-down"`
- `components/sidebar-toggle.tsx` — **modified** — Changed `data-testid="sidebar-toggle-button"` → `"sidebar-toggle"`
- `features/models/components/model-selector.tsx` — **modified** — Added `data-testid="model-selector"` to trigger button

## Validation

pnpm format    # ✅ pass (Checked 204 files, no fixes applied)
pnpm typecheck # ⚠️ 7 pre-existing errors in tests/mocks/ai.ts, tests/mocks/fetch.ts, tests/utils/stream.ts — none in P7-T06 files
pnpm lint      # ✅ pass (1 pre-existing warning in tests/mocks/ai.ts — none in P7-T06 files)

## Deviations

### data-testid naming changes

**Plan said:** P6-T02 established `data-testid="message-upvote"` and `data-testid="message-downvote"` "preserving oldapp convention for E2E tests". Sidebar toggle used `sidebar-toggle-button`.

**What was done:** Changed to `vote-up`, `vote-down`, and `sidebar-toggle` per the P7-T06 spec which explicitly lists these as the canonical testid names.

**Why:** P7-T06 is the E2E task that defines how test selectors are named. It supersedes P6's provisional naming. The new E2E tests reference these canonical names. Old app tests in `oldapp/` are reference only and unaffected.

**Trade-offs:** If any other new-codebase code references the old testid names, it would need updating. No such references were found.

## Findings

- **Pre-existing typecheck failures:** 7 errors exist in `tests/mocks/ai.ts` (4 errors — LanguageModelV1 references that should be V2), `tests/mocks/fetch.ts` (2 errors — possible undefined), `tests/utils/stream.ts` (1 error — ArtifactKind type mismatch). These are P7-T08 deliverables with type issues unrelated to P7-T06. Should be resolved in P7-T08 or a dedicated fix task.
- **data-testid coverage:** Many elements already had well-chosen testids from prior tasks (multimodal-input, send-button, stop-button, visibility-selector, artifact-panel, artifact-close-button, message-*, user-nav-*). Only 4 were missing: auth-form, login/register-button, model-selector. 2 needed renaming: vote-up/down, sidebar-toggle.
- **E2E test timeouts:** Artifact tests use 60s timeouts for AI generation (artifact creation requires model invocation + streaming). Chat tests use 30s. Auth tests use standard timeouts. These may need tuning based on the model provider's latency.

## Review

**Verdict:** ✅ PASS WITH NOTES
**Reviewer:** momus
**Date:** 2026-03-04

### Task Log

- **Completeness:** Complete — all required YAML fields present, Summary, Files Changed, Validation, Deviations, and Findings sections filled.
- **Flag: `has_deviations: true`** — Deviation is well-justified. P7-T06 defines canonical E2E test selectors; renaming `message-upvote`→`vote-up`, `message-downvote`→`vote-down`, `sidebar-toggle-button`→`sidebar-toggle` to match the spec is correct. No old-name references found in the new codebase. **Accepted.**
- **Flag: `has_findings: true`** — Pre-existing typecheck failures are real (verified: 1 error in `tests/integration/chat-flow.test.ts:170` from P7-T07, not P7-T06 files). Timeout tuning observation is actionable but non-blocking. **Accepted.**

### Code Quality

- **Intent match:** ✅ Yes — all 4 spec requirements covered:
  - Auth: login form, register form, guest auto-bootstrap, logout (guest check) ✅
  - Chat: send, receive, streaming/stop, tool invocation (weather), new chat, load existing ✅
  - Artifacts: text artifact panel opens, code artifact (CodeMirror `.cm-editor`), version navigation, close panel ✅
  - Sidebar: history empty/populated, navigation, delete, new chat button, mobile toggle ✅

- **Naming:** ✅ Compliant — all "artifact" naming (never "document"), `kebab-case` file names, Playwright conventions followed.

- **data-testid coverage:** ✅ All 13 spec-required testids verified present:

  | testid | Location | Status |
  |--------|----------|--------|
  | `multimodal-input` | `features/chat/components/multimodal-input.tsx:257` | ✅ pre-existing |
  | `send-button` | `features/chat/components/submit-button.tsx:51` | ✅ pre-existing |
  | `stop-button` | `features/chat/components/submit-button.tsx:29` | ✅ pre-existing |
  | `auth-form` | `features/auth/components/auth-form.tsx:100` | ✅ added P7-T06 |
  | `login-button` | `features/auth/components/auth-form.tsx` | ✅ added P7-T06 |
  | `register-button` | `features/auth/components/auth-form.tsx` | ✅ added P7-T06 |
  | `vote-up` | `features/voting/components/vote-buttons.tsx:60` | ✅ renamed P7-T06 |
  | `vote-down` | `features/voting/components/vote-buttons.tsx:69` | ✅ renamed P7-T06 |
  | `sidebar-toggle` | `components/sidebar-toggle.tsx:24` | ✅ renamed P7-T06 |
  | `model-selector` | `features/models/components/model-selector.tsx:152` | ✅ added P7-T06 |
  | `visibility-selector` | `features/visibility/components/visibility-selector.tsx:97` | ✅ pre-existing (P6-T07) |
  | `visibility-selector-item-private` | `features/visibility/components/visibility-selector.tsx:111` | ✅ pre-existing (P6-T07) |
  | `visibility-selector-item-public` | `features/visibility/components/visibility-selector.tsx:111` | ✅ pre-existing (P6-T07) |

- **Architecture:** ✅ Compliant — E2E specs in `tests/e2e/`, no source code imports, Playwright-only dependencies.

### Validation

- **Reported:** format ✅, typecheck ⚠️ pre-existing, lint ✅
- **Re-ran:**
  - `pnpm format` → `Checked 204 files in 114ms. No fixes applied.` ✅
  - `pnpm lint` → `Checked 204 files in 99ms. No fixes applied.` ✅
  - `pnpm typecheck` → Exit code 2. **1 error** in `tests/integration/chat-flow.test.ts:170` (P7-T07 file). Zero errors in P7-T06 files. ✅
  - IDE diagnostics: zero errors across all 8 P7-T06 files. ✅

### ⚠️ Notes (Non-Blocking)

1. **Test count inaccuracy in task log:** Summary claims "10 tests" for auth (actual: 12) and "9 tests" for chat (actual: 12). Artifacts 6 and sidebar 7 are correct. Total: 37 tests, not 32 as implied. This is a documentation discrepancy only — all tests exist and are well-structured.

2. **Pre-existing error description mismatch:** Task log describes "7 pre-existing errors in tests/mocks/ai.ts, tests/mocks/fetch.ts, tests/utils/stream.ts." Current typecheck shows 1 error in `tests/integration/chat-flow.test.ts:170`. Either the error landscape changed after the task was completed (other tasks may have fixed/introduced errors), or the original count was inaccurate. Either way, P7-T06 files are clean.

3. **Logout test coverage is minimal:** The logout section only verifies that guest users don't see a logout option. There's no test for an authenticated user's actual logout flow (session destroyed, redirect to login). This is likely acceptable given E2E tests can't easily set up authenticated sessions without fixtures, but worth noting for future enhancement.

### Recommendations

- **Accept.** All spec success criteria are met. All data-testid attributes verified. No P7-T06 files have type/lint/format errors.
- Minor: Consider correcting test counts in the task log if accuracy matters for traceability (12/12/6/7 = 37 total).
- No re-assignment needed.
