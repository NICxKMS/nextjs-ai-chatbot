---
task: P2-T03
title: "Create guest bootstrap"
phase: P2
agent: poseidon
depends_on: [P2-T01]
status: done
review: pass
attempt_count: 1
started: "2026-03-03 12:00"
finished: "2026-03-03 12:15"
has_deviations: false
has_issues: false
has_findings: true
---

## Summary

Implemented guest JWT lifecycle functions (`mintGuestToken`, `verifyGuestToken`, `rotateGuestToken`) in `features/auth/lib/guest.ts` using the `jose` library, and wired `verifyGuestToken` into `lib/auth/session.ts` to complete the guest session resolution pipeline.

## Files Changed

- `features/auth/lib/guest.ts` — **created** — Exports three async functions for guest JWT management: `mintGuestToken` (creates HS256 JWT with 1h expiry), `verifyGuestToken` (validates + decodes, never throws), `rotateGuestToken` (refreshes if <30 min remaining). Server-only via `import "server-only"`.
- `lib/auth/session.ts` — **modified** — Imported `verifyGuestToken` from `@/features/auth/lib/guest`; replaced the `TODO(P2-T03)` stub in `resolveGuestSession()` with actual JWT verification that returns a guest `AppSession` on success.

## Validation

pnpm format    # ✅ pass (2 files auto-fixed, 3 pre-existing warnings unrelated to this task)
pnpm typecheck # ✅ pass
pnpm lint      # ✅ pass (3 pre-existing warnings unrelated to this task)

## Findings

- `rotateGuestToken` is designed for use in `proxy.ts` (P2-T08) to rotate near-expiry guest cookies on each request. The rotation threshold (30 minutes) matches the oldapp behavior from `oldapp/proxy.ts` and `oldapp/lib/constants.ts`.
- `mintGuestToken` is the only function that throws (when `GUEST_JWT_SECRET` is missing) — this is intentional because callers (proxy guest bootstrap) should fail loudly if the secret is unconfigured. `verifyGuestToken` and `rotateGuestToken` return safe fallbacks (null / original token) instead.
- The JWT payload structure (`sub` for userId, `type: "guest"`) matches the oldapp convention, ensuring compatibility with the proxy token handling pattern in P2-T08.

## Review

**Verdict:** ⚠️ PASS WITH NOTES
**Reviewer:** momus
**Date:** 2026-03-03

### Task Log
- Completeness: Complete — all required YAML fields present, all markdown sections populated per Task_Log_Guide.md §2.
- Flags:
  - `has_deviations: false` — Nominally correct (implementation matches spec), but see Architecture note below.
  - `has_issues: false` — Confirmed, all validation passes.
  - `has_findings: true` — Three findings documented, all actionable context for P2-T08. Appropriate use of flag.

### Code Quality
- **Intent match:** ✅ Yes — all four success criteria from P2-T03 spec met:
  - `mintGuestToken` creates HS256 JWT with 1h expiry (`GUEST_TOKEN_TTL_SECONDS = 3600`)
  - `verifyGuestToken` returns `null` on any failure (try/catch → null, never throws)
  - `rotateGuestToken` preserves `payload.sub` userId, rotates only when `<30 min` remains
  - `pnpm typecheck` passes (verified)
- **Naming:** ✅ Compliant — `camelCase` functions, `SCREAMING_SNAKE_CASE` constants, `kebab-case` file name.
- **Size:** ✅ Within limits — `guest.ts` is ~107 lines, reasonable for 3 exported functions + JSDoc.
- **Architecture:** ⚠️ See import boundary note below.
- **Anti-patterns:** None detected — no `any`, no `@ts-ignore`, no `TODO`, no `console.log`, no `biome-ignore`. Empty catch blocks in `verifyGuestToken`/`rotateGuestToken` are justified by the documented never-throw contract.
- **Server-only guard:** ✅ `import "server-only"` present at top of `guest.ts`.
- **jose dependency:** ✅ `jose` ^6.1.2 confirmed in `package.json`.

### Session.ts Integration
- ✅ `verifyGuestToken` imported from `@/features/auth/lib/guest` at line 6
- ✅ `resolveGuestSession()` reads `guest_token` cookie via `GUEST_COOKIE_NAME` constant
- ✅ Returns `{ user: { id: result.userId, type: "guest" } }` — proper `AppSession` shape
- ✅ Never throws — try/catch → null fallback

### Validation
- Reported: All green (format ✅, typecheck ✅, lint ✅)
- Re-ran: ✅ Matches — `pnpm format` (0 fixes, 3 pre-existing warnings), `pnpm typecheck` (0 errors), `pnpm lint` (0 fixes, 3 pre-existing warnings)

### ⚠️ Import Boundary Concern (for orchestrator)
`lib/auth/session.ts` line 6 imports `verifyGuestToken` from `@/features/auth/lib/guest`. This violates conventions.md §3 Layer Rules (`lib/ → nothing above`) and the Forbidden Imports table (`lib/` cannot import `features/`). However, this import direction is **explicitly prescribed by the phase spec** (P2-T03 Outputs: "consumed by session resolution flow (`lib/auth/session.ts`)"; P2-T01 Action: "verify with `verifyGuestToken()`"). The implementation agent faithfully followed the spec; the conflict is between the architecture conventions and the phase spec design. **Orchestrator should evaluate** whether `verifyGuestToken` should be relocated to `lib/auth/` to respect import boundaries, or whether an allowlist exception should be added to conventions.md §3.

### Recommendations
- **Accept** — code quality is high, spec intent fully met, validation passes.
- **Orchestrator action:** Evaluate import boundary conflict between conventions.md §3 and phase spec P2-T01/P2-T03. Consider either (a) moving `guest.ts` to `lib/auth/guest.ts` or (b) documenting an allowlist exception. This is a spec-design issue, not an implementation defect.
