---
task: P6-T07
title: "Create VisibilitySelector component"
phase: P6
agent: apollo
depends_on: [P6-T06, P0-T11]
status: done
review: pass
attempt_count: 1
started: "2026-03-04 12:00"
finished: "2026-03-04 12:15"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created the `VisibilitySelector` client component at `features/visibility/components/visibility-selector.tsx`. Reads `visibility` and `setVisibility` from `ChatSessionContext` via `useChatSessionContext()` (CV-01 Option A). Provides a dropdown with Private (lock icon) and Public (globe icon) options, with descriptive subtitles, check icon on selected option, chevron indicator on trigger. On selection: instant context update via `setVisibility`, then Server Action persistence via `startTransition`. Reverts on failure with toast error. Hidden on mobile (`md:flex`), only shown for own chats (not readonly).

## Files Changed

- `features/visibility/components/visibility-selector.tsx` — created: "use client" dropdown component for toggling chat visibility

## Validation

pnpm format    # ✅ pass
pnpm typecheck # ✅ pass
pnpm lint      # ✅ pass

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-04

### Task Log
- Completeness: **complete** — all required YAML fields present, Summary/Files Changed/Validation sections filled, flags all false with no corresponding sections needed.
- Flags: `has_deviations: false`, `has_issues: false`, `has_findings: false` — all accurate per code inspection.

### Code Quality — Criterion-by-Criterion Verification

| # | Success Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Dropdown with Private and Public options | ✅ | `visibilities` array with `id: "private"` and `id: "public"`, rendered as `DropdownMenuItem` |
| 2 | Lock icon for private, globe icon for public | ✅ | `icon: <LockIcon />` (private), `icon: <GlobeIcon />` (public) — icons verified in `components/icons.tsx` L919/L900 |
| 3 | Descriptive subtitle per option | ✅ | `description: "Only you can access this chat"` / `"Anyone with the link can access this chat"` rendered in `text-muted-foreground text-xs` div |
| 4 | Check icon next to selected option | ✅ | `<CheckCircleFillIcon />` with `opacity-0 group-data-[active=true]/item:opacity-100` |
| 5 | Chevron indicator on trigger | ✅ | `<ChevronDownIcon />` rendered in trigger Button |
| 6 | Reads visibility from ChatSessionContext via `useChatSessionContext()` | ✅ | `const { chatId, visibility, setVisibility, isReadonly } = useChatSessionContext()` — import from `@/features/chat/hooks/use-chat-session-context` |
| 7 | Calls `setVisibility` for instant update + Server Action for persistence | ✅ | `handleSelect`: calls `setVisibility(newVisibility)` first, then `startTransition(async () => { await updateChatVisibility(...) })` |
| 8 | Reverts on failure + toast error | ✅ | `if (!result.success) { setVisibility(previousVisibility); toast.error(...) }` |
| 9 | Hidden on mobile (`md:flex`) | ✅ | Button className: `"hidden ... md:flex ..."` |
| 10 | Only shown for own chats | ✅ | `if (isReadonly) return null` — early return for non-owner |
| 11 | `data-testid="visibility-selector"` | ✅ | On `<Button>` element |
| 12 | `data-testid="visibility-selector-item-private"` / `"...-public"` | ✅ | Template literal `data-testid={\`visibility-selector-item-${item.id}\`}` with ids `"private"` and `"public"` |
| 13 | `variant="outline"` | ✅ | `<Button ... variant="outline">` |
| 14 | `align="start"` | ✅ | `<DropdownMenuContent align="start" ...>` |
| 15 | `min-w-[300px]` | ✅ | `className="min-w-[300px]"` on DropdownMenuContent |
| 16 | sr-only label text | ✅ | `<span className="md:sr-only">{selectedVisibility?.label}</span>` |

- **Naming:** Component `VisibilitySelector` (PascalCase ✅), file `visibility-selector.tsx` (kebab-case ✅), functions `handleSelect` (camelCase ✅)
- **Architecture:** Feature-collocated at `features/visibility/components/` ✅. Import direction: `features/visibility` → `features/chat` (context), `features/visibility` (action/types), `lib/utils`, `components/ui` — all valid ✅.
- **Shortcuts:** Zero `@ts-ignore`, `as any`, `TODO`, `console.log`, `biome-ignore` detected ✅.
- **"use client" directive:** Present at top of file ✅.

### Validation

| Check | Result | Evidence |
|---|---|---|
| `pnpm format` | ✅ | "Checked 194 files. No fixes applied." — 4 pre-existing warnings in unrelated files only |
| `pnpm typecheck` | ✅ | `tsc --noEmit` — zero errors, clean output |
| `pnpm lint` | ✅ | "Checked 194 files. No fixes applied. Found 4 warnings." — all 4 in carousel.tsx, sidebar.tsx, model-selector.tsx (pre-existing, unrelated) |
| Editor diagnostics | ✅ | `get_errors` on visibility-selector.tsx: "No errors found" |

### Recommendations
- **Accept** — implementation is complete, correct, and clean. All 16 success criteria verified. No deviations, no shortcuts, no issues.
