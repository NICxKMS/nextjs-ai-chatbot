---
task: P0-T10
title: "Create shared hooks"
phase: P0
agent: apollo
depends_on: [P0-T01]
status: done
review: null
attempt_count: 1
started: "2026-03-03 10:00"
finished: "2026-03-03 10:05"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created two shared client hooks — `useIsMobile` for responsive mobile breakpoint detection and `useDebounce` for debouncing rapidly changing values. Both placed in `lib/hooks/` with `"use client"` directives.

## Files Changed

- `lib/hooks/use-mobile.ts` — created; `useIsMobile()` hook copied from `oldapp/hooks/use-mobile.ts` with identical logic (matchMedia, hydration-safe SSR support via `initialIsMobile` option)
- `lib/hooks/use-debounce.ts` — created; generic `useDebounce<T>(value, delay)` hook using setTimeout/clearTimeout in useEffect

## Validation

```
pnpm format    # ✅ pass (2 files auto-fixed for semicolons by Biome)
pnpm typecheck # ✅ pass
pnpm lint      # ✅ pass
```

## Review

_Populated by review subagent after implementation._
