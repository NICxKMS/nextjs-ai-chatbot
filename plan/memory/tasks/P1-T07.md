---
task: P1-T07
title: Create message data access functions
phase: P1
agent: hephaestus
depends_on: [P0-T04, P0-T18]
status: done
review: pass
attempt_count: 1
started: "2026-03-03"
finished: "2026-03-03"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created `lib/data/message.ts` with 5 pure DB data access functions for the Message entity.

## Files Changed

- `lib/data/message.ts` — created with 5 exported functions for message data access

## Functions Implemented

| Function | Signature | Description |
|----------|-----------|-------------|
| `getMessagesByChatId` | `(chatId: string) → Promise<Message[]>` | Ordered by createdAt ASC |
| `getMessageById` | `(messageId: string) → Promise<Message \| null>` | Single lookup, returns null if not found |
| `saveMessages` | `(messages: NewMessage[]) → Promise<Message[]>` | Batch insert with `.returning()` |
| `deleteMessagesByIdAfter` | `(chatId: string, messageId: string) → Promise<void>` | Finds target's createdAt, deletes all >= |
| `deleteMessagesByChatId` | `(chatId: string) → Promise<void>` | Bulk delete for chat deletion |

## Design Decisions

- **Bare-ID signatures**: No userId parameter for reads — auth at caller level per patterns.md
- **Pure DB operations**: No `'use cache'`, no auth imports, no revalidation inline
- **Empty array guard**: `saveMessages` returns `[]` immediately for empty input
- **Safe undefined handling**: `target[0]` checked via named variable before use (TypeScript strict)

## Acceptance Criteria

- [x] getMessagesByChatId returns messages ordered by createdAt ASC
- [x] saveMessages does batch insert with `.returning()`
- [x] deleteMessagesByIdAfter deletes correct range (>= target createdAt)
- [x] All pure DB — no cache, no auth imports
- [x] `pnpm format` passes
- [x] `pnpm typecheck` passes (zero errors in this file)
- [x] `pnpm lint` passes

## Validation

```
pnpm typecheck  # ✅ Clean (0 errors)
pnpm lint       # ✅ Clean (0 warnings)
pnpm format     # ✅ Clean (no fixes applied)
```

## Flags

None.

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-03

### Task Log
- Completeness: **Complete** — all required YAML fields present, all sections filled
- Flags: All `false` — no deviations, issues, or findings reported. Accepted.

### Code Quality
- **Intent match:** ✅ Yes — all 5 functions from spec are implemented with correct signatures
- **createdAt ASC ordering:** ✅ Verified — `getMessagesByChatId` uses `orderBy(asc(messages.createdAt))`
- **Batch insert:** ✅ Verified — `saveMessages` uses `.insert().values().returning()` with empty-array guard
- **Range delete:** ✅ Verified — `deleteMessagesByIdAfter` first looks up target message's `createdAt`, then deletes all with `gte(createdAt, targetCreatedAt)` within the same chat. Correctly scoped by `chatId`.
- **Bare-ID signatures:** ✅ `getMessagesByChatId(chatId)`, `getMessageById(messageId)` — no userId parameters
- **Pure DB:** ✅ No `'use cache'`, no `cacheTag`, no `cacheLife`, no `getAppSession`, no auth imports (verified by grep)
- **Naming:** ✅ Compliant — camelCase functions, PascalCase types, kebab-case file
- **Types:** ✅ Strict — `Message`, `NewMessage` from models.types, proper return types, no `any`
- **No shortcuts:** ✅ No `@ts-ignore`, no `@ts-expect-error`, no `as any`, no `TODO`, no `console.log`
- **Architecture:** ✅ Correct layer (`lib/data/`), pure DB operations, errors bubble naturally

### Validation
- `pnpm typecheck`: ✅ Clean (0 errors)
- `pnpm biome lint lib/data/message.ts`: ✅ Clean (0 warnings)
- `pnpm biome format lib/data/message.ts`: ✅ Clean (no fixes applied)

### Recommendations
- ✅ **Accept** — code is correct, clean, and meets all spec requirements
