---
task: P1-T11
title: "Create AI provider registry"
phase: P1
agent: hephaestus
depends_on: [P0-T18]
status: done
review: pass
attempt_count: 1
started: "2026-03-03 14:00"
finished: "2026-03-03 14:15"
has_deviations: false
has_issues: true
has_findings: false
---

## Summary

Created `lib/ai/registry.ts` — the single AI provider registry using `createProviderRegistry()` from AI SDK 5.x. Registers Google (always), OpenAI (conditional on `OPENAI_API_KEY`), and OpenRouter (conditional on `OPENROUTER_API_KEY`, via `createOpenAI` with custom baseURL). Exports `registry` as the sole entry point for model resolution.

## Files Changed

- `lib/ai/registry.ts` — created. Single-file provider registry.

## Work Performed

1. Verified AI SDK 5.0.26 API: `createProviderRegistry<PROVIDERS extends Record<string, ProviderV2>>()` — stable, not experimental.
2. Verified `GoogleGenerativeAIProvider extends ProviderV2` and `OpenAIProvider extends ProviderV2` for type-safe providers record.
3. Implemented `buildRegistry()` function with conditional provider inclusion pattern from redesign spec.
4. Used `Record<string, ProviderV2>` explicit type for the providers map (no `any`).
5. Google uses `GOOGLE_GENERATIVE_AI_API_KEY` (the `@ai-sdk/google` default env var).
6. OpenRouter configured via `createOpenAI` with `baseURL: 'https://openrouter.ai/api/v1'` per spec.
7. No `getProvider()`, `getModel()`, or `MODEL_LIST` — only `registry` export.

## Validation

```
pnpm format    # ✅ pass (0 errors, 3 pre-existing warnings in carousel/sidebar)
pnpm typecheck # ⚠️ pass for registry.ts (0 errors). 2 pre-existing errors in lib/cache/client.ts (TS2322)
pnpm lint      # ✅ pass (0 errors, 3 pre-existing warnings)
```

## Issues

Pre-existing typecheck failure in `lib/cache/client.ts:75,77` — `Type 'unknown' is not assignable to type 'string | null'`. Not caused by this task. Reported as `has_issues: true` per guide (cannot fix pre-existing failure outside task scope).

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-03

### Task Log
- Completeness: **Complete.** All required YAML frontmatter fields present (`task`, `title`, `phase`, `agent`, `depends_on`, `status`, `review`, `attempt_count`, `started`, `finished`, `has_deviations`, `has_issues`, `has_findings`). All markdown sections populated (Summary, Files Changed, Work Performed, Validation, Issues).
- Flags:
  - `has_issues: true` — **Accepted.** Pre-existing `lib/cache/client.ts` TS2322 errors are confirmed outside task scope. Correctly reported and scoped.
  - `has_deviations: false` — **Confirmed.** Implementation matches spec exactly.
  - `has_findings: false` — **Confirmed.** No unexpected discoveries.

### Code Quality
- **Intent match:** ✅ Yes. `createProviderRegistry()` from `ai` package used. Google always registered, OpenAI conditional on `OPENAI_API_KEY`, OpenRouter conditional on `OPENROUTER_API_KEY` via `createOpenAI` with `baseURL: 'https://openrouter.ai/api/v1'`. Single `registry` export — no `getProvider()`, `getModel()`, or `MODEL_LIST`. All success criteria met.
- **Naming:** ✅ Compliant. File `registry.ts` (kebab-case). Function `buildRegistry()` (camelCase). Const `registry` (camelCase). Type usage `Record<string, ProviderV2>` — no violations.
- **Size:** ✅ 44 lines. Appropriate for a single-file registry.
- **Architecture:** ✅ Correct layer (`lib/ai/`). No forbidden patterns. No `any` types. No `@ts-ignore`, `TODO`, `console.log`, `biome-ignore`, or empty catch blocks. Clean JSDoc documentation.
- **Import boundaries:** ✅ Only imports from `@ai-sdk/*` and `ai` packages — no cross-layer violations.

### Validation
- Reported: format ✅, typecheck ✅ (registry.ts clean, pre-existing errors elsewhere), lint ✅
- Re-ran:
  - `pnpm format` — ✅ "Checked 80 files. No fixes applied. Found 3 warnings." (pre-existing carousel/sidebar warnings)
  - `pnpm typecheck` — ✅ `get_errors` shows 0 errors in `lib/ai/registry.ts`
  - `pnpm lint` — ✅ "Checked 81 files. No fixes applied. Found 3 warnings." (pre-existing)
- **Matches reported results.** No discrepancies.

### Anti-Pattern Scan
- No `any` types ✅
- No `@ts-ignore` / `@ts-expect-error` ✅
- No `TODO` / `FIXME` ✅
- No `console.log` ✅
- No `biome-ignore` / `eslint-disable` ✅
- No empty catch blocks ✅
- No hardcoded magic values ✅

### Recommendations
- **Accept.** Clean implementation, complete task log, no issues. Ready for downstream consumers (P1-T12, P3-T01).
