---
task: P3-T03
title: "Create tool enablement + title generation"
phase: P3
agent: hephaestus
depends_on: [P1-T12]
status: done
review: pass
attempt_count: 1
started: "2026-03-03 13:00"
finished: "2026-03-03 13:15"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created tool enablement logic (`getEnabledTools`) using metadata-driven `supportsToolCalling` boolean and title generation function (`generateTitle`) with lightweight model call and 5-second abort timeout.

## Files Changed

- `lib/ai/tools.ts` — created; exports `TOOL_IDS` constant array (`getWeather`, `createArtifact`, `updateArtifact`, `requestSuggestions`), `ToolId` type, and `getEnabledTools(model: ModelMetadata)` function that gates on `model.supportsToolCalling` boolean (no prefix-matching)
- `lib/ai/title.ts` — created; exports `generateTitle(message: string): Promise<string>` that uses `generateText` from AI SDK with `TITLE_MODEL` (lightweight `google:gemma-3-4b-it`), `AbortSignal.timeout(5000)` to prevent indefinite hangs, and graceful fallback to message excerpt on failure

## Validation

```
pnpm format    # ✅ pass (3 pre-existing warnings in carousel.tsx, sidebar.tsx)
pnpm typecheck # ✅ pass
pnpm lint      # ✅ pass (3 pre-existing warnings in carousel.tsx, sidebar.tsx)
```

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-03

### Task Log

- Completeness: **complete** — all YAML fields present, summary, files changed, and validation sections filled
- Flags: `has_deviations: false`, `has_issues: false`, `has_findings: false` — all verified accurate, no deviations or issues found in code

### Code Quality

#### `lib/ai/tools.ts`

- **Intent match:** ✅ `getEnabledTools(model: ModelMetadata)` gates on `model.supportsToolCalling` boolean — no hardcoded prefix-matching
- **Tool naming:** ✅ Uses `createArtifact`/`updateArtifact` — zero references to `createDocument`/`updateDocument`
- **Architecture:** ✅ Correct layer (`lib/ai/`), clean import from `@/lib/types/model.types`
- **Naming:** ✅ File kebab-case, function camelCase, constant SCREAMING_SNAKE_CASE (`TOOL_IDS`), type PascalCase (`ToolId`)
- **Shortcuts:** None — no `@ts-ignore`, `as any`, `TODO`, `console.log`, or lint suppressions

#### `lib/ai/title.ts`

- **Intent match:** ✅ `generateTitle(message: string): Promise<string>` generates a short title from first user message
- **Lightweight model:** ✅ Uses `TITLE_MODEL` constant (`google:gemma-3-4b-it`) — separate const from user's chat model
- **Abort timeout:** ✅ `AbortSignal.timeout(5_000)` applied to `generateText` call
- **Fallback:** ✅ Graceful try/catch with `fallbackTitle()` returning message excerpt or `"New Chat"`
- **Architecture:** ✅ Correct layer, imports resolve (`myProvider` from `@/lib/ai/provider`, `TITLE_MODEL` from `@/lib/types/model.types`)
- **Naming:** ✅ All conventions followed
- **Shortcuts:** None detected

### Validation

| Check            | Result | Evidence                                                        |
| ---------------- | ------ | --------------------------------------------------------------- |
| `pnpm format`    | ✅     | Exit 0 — "No fixes applied", 3 pre-existing warnings (carousel/sidebar) |
| `pnpm typecheck` | ✅     | Exit 0 — zero errors (confirmed via `tsc --noEmit` + VS Code language server) |
| `pnpm lint`      | ✅     | Exit 0 — zero issues in target files, 3 pre-existing warnings (carousel/sidebar) |
| IDE diagnostics  | ✅     | `get_errors` reports zero errors for both files and workspace-wide |

### Note

`TITLE_MODEL` and `DEFAULT_CHAT_MODEL` currently resolve to the same value (`google:gemma-3-4b-it`). This is acceptable — they are separate constants that can be changed independently, and the model is genuinely lightweight. The spec requirement ("not the full chat model") is satisfied by using a dedicated constant rather than the user's selected model.

### Recommendations

- Accept — clean, minimal, spec-compliant implementation with no issues
