---
task: P5-T03
title: "Create useSidebarHistory hook"
phase: P5
agent: apollo
depends_on: [P5-T01, P2-T06]
status: done
review: pass
attempt_count: 1
started: "2026-03-04 12:00"
finished: "2026-03-04 12:15"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created `features/sidebar/hooks/use-sidebar-history.ts` — a `"use client"` hook wrapping `useSWRInfinite` for paginated chat history fetching. Returns `chats`, `hasMore`, `loadMore`, and `isLoading`. Uses null key when no authenticated user to skip requests. Designed for loading additional pages only — initial 20 chats come from server via SidebarShell.

## Files Changed

- `features/sidebar/hooks/use-sidebar-history.ts` — created (110 lines)

## Validation

pnpm format    # ✅ pass
pnpm typecheck # ✅ pass (no errors in new file)
pnpm lint      # ✅ pass

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-04

### Task Log
- Completeness: complete — all YAML fields, summary, files changed, validation sections present
- Flags: `has_deviations: false`, `has_issues: false`, `has_findings: false` — all appropriate, no deviations or issues found

### Code Quality
- **Intent match:** Yes — hook wraps `useSWRInfinite` for paginated history, returns `chats`/`hasMore`/`loadMore`/`isLoading`, null key when unauthenticated, only for additional pages beyond initial server fetch
- **Key format:** Matches spec exactly: `GET /api/history?limit=20` (first page), `GET /api/history?limit=20&cursor={nextCursor}` (subsequent)
- **Naming:** All compliant — file `kebab-case`, hook `camelCase` with `use` prefix, return type `PascalCase`, constant `SCREAMING_SNAKE_CASE`
- **Size:** 106 lines — reasonable for M complexity task
- **Architecture:** Correct feature collocation in `features/sidebar/hooks/`. Import boundaries respected: `features/ → lib/` direction, auth provider peer import acceptable
- **Shortcuts:** Zero — no `@ts-ignore`, `as any`, `TODO`, `console.log`, `biome-ignore` found

### Validation
- Reported: all 3 commands ✅
- Re-ran: `biome check` ✅ (`Checked 1 file in 19ms. No fixes applied.`), `tsc --noEmit` ✅ (zero errors referencing file), `biome lint` ✅ (`Checked 1 file in 26ms. No fixes applied.`) — all confirmed

### Minor Note (Non-blocking)
The `loadMore` callback includes `size` in its dependency array, causing recreation on each page increment. A functional update (`setSize(prev => prev + 1)`) would keep the reference stable. This is a cosmetic optimization, not a correctness issue.

### Recommendations
- Accept as-is. Clean, well-documented, spec-compliant implementation.
