---
task: P1-T10
title: "Create suggestion data access functions"
phase: P1
agent: poseidon
depends_on: []
status: done
review: pass
attempt_count: 1
started: "2026-03-03 00:00"
finished: "2026-03-03 00:00"
has_deviations: false
has_issues: false
has_findings: false
---

## Summary

Created `lib/data/suggestion.ts` with three exported functions for suggestion data access:

- `getSuggestionsByArtifactId(artifactId)` — returns `Suggestion[]`, filters by `artifactId` (NOT documentId)
- `saveSuggestions(data)` — returns `Suggestion[]`, batch insert via `.values(data).returning()`
- `deleteSuggestionsByArtifactId(artifactId)` — returns `void`

## Design Decisions

- Uses `artifactId` throughout (NOT `documentId`) — per naming conventions and spec
- Bare-ID signatures — no `userId` parameter; auth happens at caller level
- `saveSuggestions` accepts `NewSuggestion[]` and returns the full `Suggestion[]` after insert — consistent with other batch insert patterns
- No try/catch wrapping — pure DB operations; errors bubble naturally to callers who handle them

## Acceptance Criteria

- [x] `getSuggestionsByArtifactId` filters by `artifactId` (NOT documentId)
- [x] `saveSuggestions` does batch insert
- [x] All functions properly typed with `Suggestion` / `NewSuggestion` models
- [x] Pure DB operations — no cache imports, no auth imports
- [x] `pnpm format && pnpm typecheck && pnpm lint` passes

## Flags

None.

## Review

**Verdict:** ✅ PASS WITH NOTES
**Reviewer:** momus
**Date:** 2026-03-03

### Task Log
- Completeness: Present — Summary, Design Decisions, Acceptance Criteria, Flags sections all filled
- Missing optional sections: Files Created, Validation output, `phase`/`complexity` YAML fields
- Flags: None declared, none found

### Code Quality
- **Intent match:** ✅ Yes — all 3 functions from spec implemented with correct signatures
- **artifactId (NOT documentId):** ✅ Verified — `getSuggestionsByArtifactId(artifactId)` uses `eq(suggestions.artifactId, artifactId)`. No `documentId` references in code (only a JSDoc comment explicitly noting "NOT documentId").
- **Batch insert:** ✅ `saveSuggestions` uses `.insert().values(data).returning()`
- **Bare-ID signatures:** ✅ No `userId` parameter — auth at caller level
- **Pure DB:** ✅ No `'use cache'`, no `cacheTag`, no auth imports (verified by grep)
- **Naming:** ✅ Compliant — camelCase functions, PascalCase types, kebab-case file
- **Types:** ✅ Strict — `Suggestion`, `NewSuggestion` from models.types, no `any`
- **No shortcuts:** ✅ No `@ts-ignore`, no `@ts-expect-error`, no `as any`, no `TODO`, no `console.log`
- **Architecture:** ✅ Correct layer (`lib/data/`), pure DB operations

### Validation
- `pnpm typecheck`: ✅ Clean (0 errors)
- `pnpm biome lint lib/data/suggestion.ts`: ✅ Clean (0 warnings)
- `pnpm biome format lib/data/suggestion.ts`: ✅ Clean (no fixes applied)

### Notes
- ~~⚠️ **Missing empty-array guard** — `saveSuggestions(data)` does not guard against `data.length === 0`. Drizzle's `.values([])` will throw a database error on an empty array. Compare with `saveMessages` in P1-T07 which does `if (newMessages.length === 0) return []`. This is a minor inconsistency and potential runtime error if callers pass empty arrays. Non-blocking for review but should be addressed.~~ **RESOLVED (Session 5):** Confirmed already fixed — guard present.

### Recommendations
- ✅ **Accept** — code is correct for non-empty inputs and meets all spec requirements
- ~~⚠️ Flag to orchestrator: consider adding empty-array guard to `saveSuggestions` for consistency with `saveMessages`~~ **RESOLVED (Session 5)**
