---
task: P4-T06
title: "Create handler registration via side-effect import"
phase: P4
agent: hephaestus
depends_on: [P4-T04, P4-T05, P3-T04]
status: done
review: pass
attempt_count: 1
started: "2026-03-04 13:00"
finished: "2026-03-04 13:15"
has_deviations: true
has_issues: false
has_findings: false
---

## Summary

Created `features/artifacts/handlers/index.ts` — a side-effect module that imports all 4 artifact handlers (text, code, sheet, image) and registers them into the handler registry at `lib/ai/artifact-handlers.ts` via `registerArtifactHandler()`. Registration happens at module evaluation time. Also updated `getArtifactHandler()` to throw `AppError.notFound` instead of a plain `Error` per acceptance criteria.

## Files Changed

- `features/artifacts/handlers/index.ts` — created: side-effect module importing all 4 handlers and calling `registerArtifactHandler()` for each kind (text, code, sheet, image)
- `lib/ai/artifact-handlers.ts` — modified: imported `AppError` and changed `getArtifactHandler()` to throw `AppError.notFound("not_found:artifact:artifact_not_found", ...)` for unknown kinds

## Validation

pnpm format    # ✅ pass (3 pre-existing warnings in UI components)
pnpm typecheck # ✅ pass
pnpm lint      # ✅ pass (3 pre-existing warnings in UI components)

## Deviations

**Spec:** P4-T06 output files list only `features/artifacts/handlers/index.ts`.
**Done:** Also modified `lib/ai/artifact-handlers.ts` to change `getArtifactHandler()` error from plain `Error` to `AppError.notFound`.
**Why:** Acceptance criterion #4 explicitly requires "getArtifactHandler throws AppError.notFound for unknown kinds". The original registry (P3-T04) used a plain `Error`. This change was necessary to satisfy the stated criterion and is the minimal modification — only the error throw was changed, plus one import added.
**Trade-offs:** None. The `AppError` class was already available and the `not_found:artifact:artifact_not_found` error code already existed in the error codes registry.

## Review

**Verdict:** ✅ PASS
**Reviewer:** momus
**Date:** 2026-03-04

### Validation Results

| Check | Result | Evidence |
|-------|--------|----------|
| `pnpm typecheck` | ✅ | `npx tsc --noEmit --pretty false` → exit 0, zero errors |
| `pnpm format` (task files) | ✅ | `npx @biomejs/biome check features/artifacts/handlers/index.ts lib/ai/artifact-handlers.ts` → "Checked 2 files in 9ms. No fixes applied." exit 0 |
| `pnpm lint` (task files) | ✅ | Same Biome check command covers lint — exit 0 |
| Shortcut scan | ✅ | Grep for `@ts-ignore`, `@ts-expect-error`, `as any`, `TODO`, `console.log`, `biome-ignore` — zero matches in both files |

### Task Log
- **Completeness:** All required YAML fields present (task, title, phase, agent, depends_on, status, review, attempt_count, started, finished, flags). Summary, Files Changed, Validation, and Deviations sections are filled.
- **Flags:**
  - `has_deviations: true` — **Accepted.** The deviation (modifying `lib/ai/artifact-handlers.ts` to use `AppError.notFound` instead of plain `Error`) is justified: acceptance criterion #5 explicitly says "getArtifactHandler throws AppError.notFound for unknown kinds". The change is minimal (one import added, one throw changed), and the error code `not_found:artifact:artifact_not_found` already existed in `lib/errors/codes.ts` (line 22/45). This is a necessary cross-file touch, not scope creep.
  - `has_issues: false` — Confirmed, no issues visible.
  - `has_findings: false` — Confirmed.

### Code Quality

**Intent match: ✅ Yes**
- Spec requires: side-effect module importing all 4 handlers and registering via `registerArtifactHandler()`. Done.
- All 4 kinds registered: `text`, `code`, `sheet`, `image` — matches `ArtifactKind = "text" | "code" | "image" | "sheet"` in `lib/types/models.types.ts:27`.
- Registration happens at module evaluation time (top-level calls, no wrapper function). Correct side-effect pattern.
- `getArtifactHandler` throws `AppError.notFound("not_found:artifact:artifact_not_found", ...)` for unknown kinds. Verified.

**Naming: ✅ Compliant**
- Files: `index.ts`, `artifact-handlers.ts` — kebab-case.
- Functions: `registerArtifactHandler`, `getArtifactHandler` — camelCase.
- Types: `ArtifactKind`, `ArtifactHandler` — PascalCase.
- Handler constants: `textHandler`, `codeHandler`, `sheetHandler`, `imageHandler` — camelCase.

**Size: ✅ Within limits**
- `features/artifacts/handlers/index.ts`: 30 lines. Small side-effect module.
- `lib/ai/artifact-handlers.ts`: 55 lines. Lean registry.

**Architecture: ✅ Compliant**
- Import boundaries respected: `lib/ai/artifact-handlers.ts` only imports from `@/lib/` (no upward dependency on `features/` or `app/`). `features/artifacts/handlers/index.ts` imports from `@/lib/` (downward) and sibling handlers (same module). Dependency inversion pattern intact.
- `registerArtifactHandler` has duplicate-registration guard (throws on re-registration).
- No forbidden patterns detected.

**Documentation:** Good JSDoc on both `registerArtifactHandler` and `getArtifactHandler`. Module-level doc comment in `index.ts` explains usage pattern (`import "@/features/artifacts/handlers"`) and why 4 handlers are registered despite 3-kind tool enum.

### Recommendations
- Accept as-is. Clean, minimal, spec-compliant implementation.
