/**
 * Logging Module
 * @module lib/logging
 * 
 * Public API for application logging:
 * - Logger class and default instance
 * - Log levels enum
 * - Context types
 */

export { Logger, logger, LogLevel } from './logger';
export type { LogContext } from './logger';
