/**
 * Motion Library
 *
 * Re-exports framer-motion for use throughout the application.
 * This allows for consistent imports and potential future customization.
 *
 * @module lib/motion
 */

export {
	AnimatePresence,
	type AnimationControls,
	type AnimationProps,
	type CustomValueType,
	type DragControls,
	domAnimation,
	domMax,
	domMin,
	type Easing,
	type HTMLMotionProps,
	type Inertia,
	type Keyframes,
	type MotionProps,
	type MotionStyle,
	type MotionValue,
	m,
	motion,
	type PanInfo,
	type ResolvedValues,
	type Spring,
	type Target,
	type TargetAndTransition,
	type Transition,
	useAnimation,
	useAnimationControls,
	useInView,
	useMotionTemplate,
	useMotionValue,
	useMotionValueEvent,
	useScroll,
	useSpring,
	useTime,
	useTransform,
	useVelocity,
	type Variant,
	type Variants,
	type VisualElement,
} from "framer-motion"
