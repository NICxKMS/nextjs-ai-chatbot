# Phase 5: Utilities Comparison

**Scope:** lib/ directory utilities
**Started:** 2026-02-15
**Completed:** 2026-02-15

## VERIFICATION SUMMARY

| Issue | Status | Timestamp |
|-------|--------|----------|
| P5-BRK-005 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-001 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-002 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-003 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-004 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-005 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-006 | Verified | 2026-02-16T12:00:00Z |
| P5-FNC-007 | False Positive (Re-verified) | 2026-02-16T12:00:00Z |
| P5-BUG-001 | Verified (Defect) | 2026-02-16T12:00:00Z |
| P5-BRK-001 | Verified (Defect) | 2026-02-16T12:00:00Z |
| P5-FNC-024 | Improvement | 2026-02-16T14:30:00Z |
| P5-FNC-025 | Verified (Defect) | 2026-02-16T14:30:00Z |
| P5-FNC-026 | Verified (Defect) | 2026-02-16T14:30:00Z |
| P5-FNC-027 | Verified (Defect) | 2026-02-16T14:30:00Z |
| P5-FNC-028 | Improvement | 2026-02-16T14:30:00Z |
| P5-FNC-029 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-030 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-031 | Verified | 2026-02-16T14:30:00Z |
| P5-BRK-002 | Verified (Defect) | 2026-02-16T12:00:00Z |
| P5-BRK-003 | Verified (Defect) | 2026-02-16T12:00:00Z |
| P5-BRK-004 | Verified (Defect) | 2026-02-16T23:45:00Z |
| P5-FNC-032 | Improvement | 2026-02-16T17:30:00Z |
| P5-FNC-033 | Verified | 2026-02-16T17:30:00Z |
| P5-FNC-034 | Improvement | 2026-02-16T17:30:00Z |
| P5-FNC-035 | Verified | 2026-02-16T17:30:00Z |
| P5-FNC-036 | Verified | 2026-02-16T17:30:00Z |
| P5-FNC-037 | Verified (Defect) | 2026-02-16T17:30:00Z |
| P5-FNC-038 | Verified (Defect) | 2026-02-16T17:30:00Z |
| P5-FNC-039 | False Positive (Re-verified) | 2026-02-16T17:30:00Z |
| P5-FNC-016 | False Positive (Re-verified) | 2026-02-16T14:30:00Z |
| P5-FNC-017 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-018 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-019 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-020 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-021 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-022 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-023 | Verified | 2026-02-16T14:30:00Z |
| P5-FNC-040 | False Positive (Re-verified) | 2026-02-16T18:30:00Z |
| P5-FNC-041 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-042 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-043 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-044 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-045 | False Positive (Re-verified) | 2026-02-16T18:30:00Z |
| P5-FNC-046 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-047 | False Positive | 2026-02-16T18:30:00Z |
| P5-FNC-008 | False Positive (Re-verified) | 2026-02-16T18:30:00Z |
| P5-FNC-009 | False Positive (Re-verified) | 2026-02-16T18:30:00Z |
| P5-FNC-010 | Improvement | 2026-02-16T18:30:00Z |
| P5-FNC-011 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-012 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-013 | Verified | 2026-02-16T18:30:00Z |
| P5-FNC-014 | False Positive (Re-verified) | 2026-02-16T18:30:00Z |
| P5-FNC-015 | Improvement | 2026-02-16T18:30:00Z |
| P5-FNC-056 | False Positive | 2026-02-16T22:30:00Z |
| P5-FNC-057 | Improvement | 2026-02-16T22:30:00Z |
| P5-FNC-058 | Verified | 2026-02-16T22:30:00Z |
| P5-FNC-059 | False Positive | 2026-02-16T22:30:00Z |
| P5-FNC-060 | Improvement | 2026-02-16T22:30:00Z |
| P5-FNC-061 | Verified | 2026-02-16T22:30:00Z |
| P5-FNC-062 | Verified | 2026-02-16T22:30:00Z |
| P5-FNC-063 | Verified (Already Resolved) | 2026-02-16T22:30:00Z |
| P5-FNC-064 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-065 | Improvement | 2026-02-16T21:00:00Z |
| P5-FNC-066 | Verified (Resolved) | 2026-02-16T21:00:00Z |
| P5-FNC-067 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-068 | Verified (Resolved) | 2026-02-16T21:00:00Z |
| P5-FNC-069 | False Positive | 2026-02-16T21:00:00Z |
| P5-FNC-048 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-049 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-050 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-051 | False Positive | 2026-02-16T21:00:00Z |
| P5-FNC-052 | False Positive | 2026-02-16T21:00:00Z |
| P5-FNC-053 | Verified | 2026-02-16T21:00:00Z |
| P5-FNC-054 | False Positive | 2026-02-16T21:00:00Z |
| P5-FNC-055 | Verified | 2026-02-16T21:00:00Z |

## Table of Contents
- [UI Inconsistencies](#ui-inconsistencies)
- [Bugs](#bugs)
- [Broken Code](#broken-code)
- [Functional Discrepancies](#functional-discrepancies)
- [Improvement Only](#improvement-only)

---

## UI Inconsistencies

_No UI inconsistency issues found in this phase._

---

## Bugs

### [P5-BUG-001] Missing Circuit Breaker for Redis Failures

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/cache/operations.ts`
**NEW File:** N/A - Not found
**Line Ref:** L36-89

**Description:**
OLD has circuit breaker implementation:
- `CIRCUIT_BREAKER_THRESHOLD = 5` consecutive failures
- `CIRCUIT_BREAKER_RESET_MS = 30000` (30 seconds)
- `isCircuitOpen()` - Check if operations should be skipped
- `recordCacheFailure()` - Track failures and open circuit
- `recordCacheSuccess()` - Reset failure count

When circuit is open, cache operations return `null` immediately without attempting Redis calls.

NEW doesn't have circuit breaker - Redis client has retry logic but no circuit breaker at operation level.

**Impact:**
During Redis outages, every cache operation will attempt connection and timeout, causing slow failures instead of fast-fail.

**Suggested Fix:**
Add circuit breaker to `lib/cache/client.ts` or `lib/cache/strategies.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T12:00:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed by code inspection. OLD (`archive/oldapp/lib/cache/operations.ts:36-89`) has a full circuit breaker implementation: module-scoped `consecutiveFailures` counter, `CIRCUIT_BREAKER_THRESHOLD = 5`, `CIRCUIT_BREAKER_RESET_MS = 30_000`, `circuitOpenedAt` timestamp, `isCircuitOpen()` function that auto-resets after timeout, `recordCacheFailure()` that opens circuit after threshold, and `recordCacheSuccess()` that resets the counter. NEW `lib/cache/client.ts` has only retry logic via `@upstash/redis` config (`retry: { retries: 3, backoff: exponential }`). Searched ALL files in `lib/cache/` — no circuit breaker pattern exists anywhere in `client.ts`, `strategies.ts`, `tiered-cache.ts`, `memory-cache.ts`, `keys.ts`, `invalidation.ts`, or `quota.ts`. The retry logic in the Redis client is not equivalent — it retries individual operations but does not fast-fail after repeated failures, meaning during sustained Redis outages every cache operation will attempt and timeout. This is a real defect that degrades resilience.

---

## Broken Code

### [P5-BRK-001] Missing `chat-completion.ts` - Core Chat Execution

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/chat-completion.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-290

**Description:**
The entire `chat-completion.ts` module is missing. This is the core chat execution function that:
- Executes AI chat completion with streaming
- Configures tools based on model capabilities
- Builds provider-specific options for reasoning models
- Handles timeout with `AbortSignal.timeout(55_000)`
- Uses `smoothStream` for better streaming UX
- Integrates with `tokenlens` for usage tracking
- Calls `onUsageCalculated` callback with usage data

**Key Functions Missing:**
- `executeChatCompletion(params: ChatCompletionParams)` - Main chat execution
- `getEnabledTools(model)` - Tool enablement based on model capabilities
- `buildProviderOptions(selectedModel)` - Provider-specific reasoning options

**Impact:**
Chat functionality will not work. This is the core function that processes chat messages and streams AI responses.

**Suggested Fix:**
Create `lib/ai/chat-completion.ts` with the full implementation, adapting to new provider/registry structure.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T12:00:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed. `lib/ai/chat-completion.ts` does not exist in the NEW codebase (file_search only returns the OLD archive path). The NEW chat route (`app/api/chat/route.ts`) delegates to `streamChatAction` in `features/chat/actions/stream-chat.action.ts`, which handles auth, rate limiting, message persistence, and returns a result — but it does **not** contain the actual AI streaming logic. There is no `streamText` call, no `executeChatCompletion` equivalent, no tool wiring to an AI model, no `smoothStream`, no `AbortSignal.timeout`, no provider options building, and no `onFinish`/usage callback. The `streamChatAction` only saves messages to DB and returns `{ success, chatId }` — it does not stream AI responses. The core AI completion orchestration from `chat-completion.ts` (tool enablement by model capabilities, reasoning model provider options, tokenlens integration, streaming merge) has no equivalent in the new codebase. This is a genuine critical defect — chat AI streaming cannot function without this module.

---

### [P5-BRK-002] Missing `prompts.ts` - System Prompts

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/prompts.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-215

**Description:**
The entire `prompts.ts` module is missing. This contains all system prompts used for AI interactions:

**Missing Prompts:**
1. `artifactsPrompt` - Instructions for using Artifacts UI
2. `regularPrompt` - Base assistant behavior guidelines
3. `codePrompt` - Python code generation instructions
4. `sheetPrompt` - CSV spreadsheet generation instructions
5. `updateDocumentPrompt(currentContent, type)` - Document update prompt builder
6. `systemPrompt({ selectedChatModel, requestHints, selectedModel, userSystemPrompt })` - Main system prompt builder
7. `getRequestPromptFromHints(requestHints)` - Location context from geo hints

**Missing Types:**
- `RequestHints` - Geographic request context (latitude, longitude, city, country)

**Impact:**
AI will have no system instructions. Chat responses will be unguided and inconsistent.

**Suggested Fix:**
Create `lib/ai/prompts.ts` with all prompt templates and `RequestHints` type.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T12:00:00Z |
| **Status** | Verified (Defect) |

**Findings:** 5 of 8 exports from OLD `archive/oldapp/lib/ai/prompts.ts` are genuinely missing in the NEW codebase. Only the artifact-specific prompts were relocated:

**Relocated (3/8):**
- `codePrompt` → `CODE_CREATE_SYSTEM_PROMPT` in `features/artifact/handlers/code.handler.ts:24-31`
- `sheetPrompt` → equivalent prompt in `features/artifact/handlers/sheet.handler.ts:38`
- `updateDocumentPrompt` → `getUpdateSystemPrompt()` in each handler (`text.handler.ts:24`, `code.handler.ts:38`, `sheet.handler.ts:38`)

**Missing (5/8):**
- `regularPrompt` — base assistant behavior guidelines. Grep for `regularPrompt` across `features/`, `lib/`, `app/` (non-archive) returns zero matches.
- `artifactsPrompt` — instructions for Artifacts UI (when to use createDocument/updateDocument). Zero matches outside archive.
- `systemPrompt()` — main builder that combines `regularPrompt` + `requestPrompt` + optional `userSystemPrompt` + conditional `artifactsPrompt` (excluded for reasoning models). Zero matches for a function of this signature outside archive.
- `RequestHints` type — `{ latitude, longitude, city, country }` from `@vercel/functions` Geo. Zero matches outside archive.
- `getRequestPromptFromHints()` — formats geo hints into prompt text. Zero matches outside archive.

The missing `systemPrompt()` builder is consumed by OLD `chat-completion.ts:183` (`system: systemPrompt({selectedChatModel, requestHints, selectedModel, userSystemPrompt})`). Since `chat-completion.ts` is also missing (P5-BRK-001), the consumer is absent too — but when chat completion is rebuilt, these prompts must be recreated. The `features/settings/types.ts:138` `systemPrompt` property holds the *user's custom* system prompt for the settings UI — it is NOT the `systemPrompt()` builder function.

**Re-verified 2026-02-16: OVERTURNED from False Positive to Verified (Defect).** Original FP determination was incorrect — 5 of 8 exports are genuinely missing, including the core chat system prompt infrastructure. Only artifact-specific prompts (3/8) were relocated.

---

### [P5-BRK-003] Missing `title-generation.ts` - Chat Title Generation

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/title-generation.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-67

**Description:**
The entire `title-generation.ts` module is missing. This contains:

**Missing Functions:**
1. `generateTitleFromUserMessage({ message })` - Async title generation using AI
   - Uses `generateText` with title model
   - Falls back to message text extraction on error
   - Returns max 80 character title
2. `generatePlaceholderTitle(message)` - Sync placeholder title
   - Extracts first 80 chars from message text
   - Used while async generation happens

**Impact:**
New chats will not have titles. Chat history will show "New Chat" for all conversations.

**Suggested Fix:**
Create `lib/ai/title-generation.ts` with both title generation functions.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T12:00:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed. `lib/ai/title-generation.ts` does not exist in the NEW codebase (file_search only returns archive path). Grep search for `generateTitleFromUserMessage` and `generatePlaceholderTitle` across all non-archive files returns zero matches. The OLD implementation (`archive/oldapp/lib/ai/title-generation.ts:1-67`) provides: (1) `generateTitleFromUserMessage({ message })` — async AI-based title generation using `generateText` with title model, with error fallback to text extraction, max 80 chars; (2) `generatePlaceholderTitle(message)` — sync placeholder extracting first 80 chars from message text parts. The OLD chat route uses both: placeholder on chat creation, async generation in background. The NEW `streamChatAction` accepts an optional `title` parameter but has no AI-based title generation. Without this module, new chats will require manual title-setting or will show generic titles. This is a genuine defect.

---

### [P5-BRK-004] Missing `tools/` Directory - AI Tool Definitions

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/tools/`
**NEW File:** N/A - Not found
**Line Ref:** All files

**Description:**
The entire `tools/` directory is missing with 4 tool implementations:

**Missing Tools:**
1. **`create-document.ts`** (91 lines)
   - Creates new documents, code snippets, spreadsheets
   - Uses `artifactKinds` and `documentHandlersByArtifactKind`
   - Streams document creation progress via `dataStream`
   - Returns document ID, title, kind

2. **`get-weather.ts`** (78 lines)
   - Gets weather by coordinates or city name
   - Uses Open-Meteo API (no API key needed)
   - Includes `geocodeCity()` helper for city lookup

3. **`request-suggestions.ts`** (106 lines)
   - Generates document improvement suggestions
   - Uses `streamObject` for streaming suggestions
   - Saves suggestions to database for authenticated users
   - Uses `artifact-model` for generation

4. **`update-document.ts`** (73 lines)
   - Updates existing documents
   - Looks up document via `documentData.get()`
   - Delegates to document handlers by kind

**Impact:**
AI cannot use any tools. Document creation, weather lookup, and suggestions will not work.

**Suggested Fix:**
Create `lib/ai/tools/` directory with all 4 tool implementations.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T23:45:00Z |
| **Status** | Verified (Defect) |
| **Re-verified** | Overturned from False Positive |

**Findings (Re-verification):** All 4 tool files DO exist at `features/chat/lib/tools/` — the relocation claim was correct. However, **3 of 4 tools have reduced capabilities**, and the factory is not wired into the chat pipeline:

**Tool-by-tool comparison:**

| Tool | OLD → NEW File | Present? | Functionally Equivalent? |
|------|---------------|----------|-------------------------|
| get-weather | `get-weather.ts` → `weather.tool.ts` | ✅ | ✅ Improved (timeouts, retries, AbortController) |
| create-document | `create-document.ts` → `create-document.tool.ts` | ✅ | ❌ Reduced — handler delegation removed |
| update-document | `update-document.ts` → `update-document.tool.ts` | ✅ | ❌ Reduced — handler delegation removed |
| request-suggestions | `request-suggestions.ts` → `suggestions.tool.ts` | ✅ | ❌ Reduced — only 1st suggestion persisted |

**Critical regression 1 — create-document & update-document lost handler delegation:**
OLD `create-document.ts:61-77` calls `documentHandler.onCreateDocument({ id, title, dataStream, session, chatId })` which triggers AI content generation and persistence via the handler. OLD `update-document.ts:46-62` calls `documentHandler.onUpdateDocument({ document, description, dataStream, session })`. NEW tools do NEITHER — they stream metadata events (data-kind, data-id, data-title, data-clear, data-finish) and return immediately. The NEW code comments at `create-document.tool.ts:116` explicitly acknowledge this: "The document handler pattern from v5 has been simplified in v6. The artifact is created when content is streamed via the data-stream handler." However, the artifact handlers at `features/artifact/handlers/` (text, code, sheet, image) are **never imported or called** by any route, action, or tool in the NEW codebase — `getArtifactHandler()` and `artifactHandlersByKind` have zero consumers outside their own definitions. The handlers exist but are completely disconnected from the tool execution pipeline.

**Critical regression 2 — suggestions tool saves only first suggestion:**
OLD `request-suggestions.ts:88-98` calls `saveSuggestions({ suggestions: suggestions.map(s => ({...s, userId, createdAt, documentCreatedAt})) })` — batch-inserts ALL generated suggestions to database. NEW `suggestions.tool.ts:171-183` calls `artifactService.addSuggestion(documentId, { firstSuggestion fields }, context)` — persists ONLY the first suggestion. Remaining suggestions (up to 4 more) are streamed to the client but never saved. Guest-handling is equivalent in both (skip persistence).

**Critical regression 3 — createChatTools() is unused:**
The `createChatTools()` factory in `index.ts:122-150` is defined but NEVER imported by any file outside `features/chat/lib/tools/` — grep across `app/`, `features/chat/actions/`, and `features/chat/components/` returns zero imports. The tools are not wired into the chat streaming pipeline. Without a consumer calling `createChatTools()` and passing the result to `streamText({ tools })`, the AI model cannot invoke any tools.

**Improvements in NEW (acknowledged):** ToolExecutionError class with structured error codes, timeout handling, retry logic on weather tool, model injection on suggestions tool, legacy exports for backward compatibility, TypeScript interfaces and JSDoc documentation.

**Verdict:** OVERTURNED from False Positive. While all 4 tool files exist at the new path, 3 of 4 have reduced capabilities (missing handler delegation, reduced suggestion persistence) and the factory function is not connected to any consumer. The tools are structural shells that don't deliver equivalent functionality.

---

### [P5-BRK-005] Missing Guest User Cache-Only Data Strategy

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/data/chat.ts`
**NEW File:** `lib/data/repositories/chat.repository.ts`
**Line Ref:** L85-169, L187-326

**Description:**
OLD data layer has sophisticated guest user handling:
- Guest users: cache-only operations (no DB writes)
- Authenticated users: DB-first with cache warming
- `ctx.isGuest` flag determines data strategy
- Cache miss for guests returns `null` (no DB fallback)

NEW repository pattern doesn't have guest-specific logic:
- All operations go through DB
- No cache-first strategy for guests
- `RepositoryContext` has `isGuest` but it's not used

**Impact:**
Guest users will have data persisted to database, losing the ephemeral guest experience. Performance impact from always hitting DB.

**Suggested Fix:**
Implement guest-aware data strategy in repositories or create separate `GuestDataRepository` for cache-only operations.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed. OLD `chatData.get()` in `archive/oldapp/lib/data/chat.ts:85-169` checks `ctx.isGuest` and returns `null` on cache miss for guests (no DB fallback). OLD `chatData.getWithMessages()` at L187-326 follows the same pattern. NEW `RepositoryContext` in `lib/data/types.ts:59-64` defines `isGuest: boolean` but it is **never checked** in `ChatRepository` methods (`lib/data/repositories/chat.repository.ts`) — all queries go through `doFindById` → DB unconditionally. `ChatService` (`lib/data/services/chat.service.ts`) has zero references to `isGuest`, `guest`, or any cache-first logic. All guest user operations will hit the database instead of being cache-only. The issue accurately describes a real functional regression.

---

## Functional Discrepancies

### Core Utilities (utils.ts)

#### [P5-FNC-001] Missing `fetcher` Function - SWR Data Fetcher

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L17-64

**Description:**
The OLD codebase contains a `fetcher` function used as an SWR data fetcher with comprehensive error handling:
- Parses error responses and extracts error codes
- Handles network offline detection
- Throws `ChatSDKError` with proper error codes
- Redirects to home page for `not_found:chat` errors

**Impact:**
SWR data fetching throughout the application will fail. Any component using `useSWR` with this fetcher pattern will not have proper error handling.

**Suggested Fix:**
Create `lib/utils/fetcher.ts` with the fetcher implementation or add to existing utils.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing as a shared utility. OLD exports `fetcher` from `archive/oldapp/lib/utils.ts:17-64` with `ChatSDKError` wrapping, error code parsing, offline detection, and `not_found:chat` redirect. NEW codebase has no shared `fetcher` export. `components/document/document-preview.tsx:31` defines a local `fetcher` with minimal error handling (`throw new Error("Failed to fetch")`) — no error code parsing, no offline detection, no redirects. `.apm/Memory` confirms it was flagged as missing (Task_4_4b). SWR usages in `features/artifact/` use inline async functions instead of a shared fetcher. The OLD fetcher's comprehensive error handling (ChatSDKError, offline, redirect) is not replicated anywhere in the new codebase.

---

#### [P5-FNC-002] Missing `fetchWithErrorHandlers` Function

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L66-104

**Description:**
The OLD codebase contains a `fetchWithErrorHandlers` function that wraps fetch with:
- Automatic error response parsing
- Error code extraction from JSON responses
- Offline detection with `navigator.onLine`
- Throws `ChatSDKError` for consistent error handling

**Impact:**
API calls throughout the application that need error handling will not have consistent error management.

**Suggested Fix:**
Implement `fetchWithErrorHandlers` in `lib/utils/` or equivalent location.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing. OLD `fetchWithErrorHandlers` in `archive/oldapp/lib/utils.ts:66-104` wraps `fetch()` with JSON error code extraction, `ChatSDKError` creation, `not_found:chat` redirect, and offline detection via `navigator.onLine`. OLD `chat.tsx:40` imports it and passes as `fetch: fetchWithErrorHandlers` to `DefaultChatTransport` (line 202). NEW `features/chat/components/chat.tsx:132-144` creates `DefaultChatTransport` without a custom `fetch`, using browser's native `window.fetch`. No equivalent function exists anywhere in the new codebase (grep confirmed zero matches outside archive/ and issue files). This is also cross-referenced by P3-BUG-001 in `issues/03-shared-components/issues.md` which independently confirmed the same defect.

---

#### [P5-FNC-003] Missing `generateUUID` Function

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L113-137

**Description:**
The OLD codebase contains a `generateUUID` function that:
- Uses native `crypto.randomUUID()` when available (2-5x faster)
- Falls back to `crypto.getRandomValues()` for cryptographic security
- Throws error if crypto API unavailable (instead of insecure fallback)

**Impact:**
Any code that needs to generate UUIDs client-side or server-side will need this utility.

**Suggested Fix:**
Add `generateUUID` function to `lib/utils/validation.ts` or create new `lib/utils/uuid.ts`.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing as a shared utility. OLD `generateUUID` in `archive/oldapp/lib/utils.ts:113-137` provides: (1) `crypto.randomUUID()` fast path, (2) `crypto.getRandomValues()` secure fallback with proper RFC 4122 v4 bit manipulation, (3) error throw if crypto unavailable. NEW codebase uses `crypto.randomUUID()` directly at call sites: `features/chat/lib/tools/suggestions.tool.ts:107,147`, `features/chat/lib/tools/create-document.tool.ts:84`, `lib/data/services/chat.service.ts:246`, `lib/data/services/auth.service.ts:199,288`, `lib/data/services/artifact.service.ts:283,469`, `lib/auth/session.ts:290`. These inline calls lack the `getRandomValues` fallback and error handling. While `crypto.randomUUID()` is available in Node 16+ and modern browsers, the old code's fallback provides broader compatibility and a security guarantee (throws instead of failing silently). No centralized `generateUUID` utility exists in the new code.

---

#### [P5-FNC-004] Missing `getMostRecentUserMessage` Function

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L142-145

**Description:**
Utility function to get the most recent user message from an array of messages. Used in chat processing logic.

**Impact:**
Chat message processing may fail if this utility is used elsewhere in the application.

**Suggested Fix:**
Add to `lib/utils/message.ts` or appropriate location.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing. OLD `getMostRecentUserMessage` in `archive/oldapp/lib/utils.ts:142-145` filters messages by `role === "user"` and returns the last one via `.at(-1)`. Grep found zero matches for `getMostRecentUserMessage` in the new codebase (app/, features/, lib/, components/). The new `stream-chat.action.ts` and `features/chat/` do not contain this function or any equivalent named utility. While `.at(-1)` is used inline in some places (`features/chat/components/chat.tsx:138`, `features/chat/hooks/use-chat.ts:156`), those access the last message of the full array — NOT filtering to user messages first, which is a different operation. The function is genuinely missing as a reusable utility.

---

#### [P5-FNC-005] Missing `getTrailingMessageId` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L162-174

**Description:**
Function to get the ID of the last message in a message array. Returns null if no messages exist.

**Impact:**
Message pagination and streaming logic may be affected.

**Suggested Fix:**
Add to `lib/utils/message.ts` or appropriate location.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing. OLD `getTrailingMessageId` in `archive/oldapp/lib/utils.ts:162-174` takes `{ messages: ResponseMessage[] }` and returns `messages.at(-1)?.id ?? null`. Grep found zero matches in the new codebase outside archive/ and issue files. No equivalent named function or inline pattern with the same destructured-parameter signature exists. The function is a simple utility but its absence means callers would need to inline the null-safe access pattern.

---

#### [P5-FNC-006] Missing `sanitizeText` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L176-178

**Description:**
Simple text sanitization function that removes `<has_function_call>` tokens from text.

**Note:** NEW has `sanitizeHtml` in `lib/utils/string.ts` which is different - it escapes HTML entities for XSS prevention.

**Impact:**
Text processing that expects `<has_function_call>` removal may not work correctly.

**Suggested Fix:**
Add `sanitizeText` function to `lib/utils/string.ts` alongside `sanitizeHtml`.

**Verification:**
- **Status:** Verified
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** Confirmed missing. OLD `sanitizeText` in `archive/oldapp/lib/utils.ts:176-178` does `text.replace("<has_function_call>", "")` — strips a specific AI model marker token. NEW codebase has `sanitizeHtml` in `lib/utils/string.ts:83-92` which performs full HTML entity encoding (`&` → `&amp;`, `<` → `&lt;`, etc.) — a fundamentally different operation. `sanitizeHtml` would corrupt markdown content containing `<`, `>`, `=`, backticks, etc. This is also cross-referenced by P3-BRK-020 in `issues/03-shared-components/issues.md` which independently confirmed that using `sanitizeHtml` instead of `sanitizeText` breaks AI response rendering. No `sanitizeText` equivalent exists in the new codebase.

---

#### [P5-FNC-007] Missing `convertToUIMessages` Function

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L180-201

**Description:**
Critical function that converts `MessageRow[]` from database to `ChatMessage[]` format for UI:
- Maps database message format to AI SDK UIMessage format
- Converts parts with proper typing (`UIMessagePart<CustomUIDataTypes, ChatTools>`)
- Adds metadata with createdAt timestamp
- Throws `ChatSDKError` if message is missing ID

**Impact:**
Chat display functionality will fail. Messages from database cannot be converted to UI format.

**Suggested Fix:**
Implement in `lib/utils/message.ts` or `features/chat/utils/` with proper type imports.

**Verification:**
- **Status:** False Positive
- **Timestamp:** 2026-02-16T12:00:00Z
- **Findings:** The function EXISTS in the new codebase. `app/(chat)/chat/[id]/page.tsx:37-50` defines `function convertToUIMessages(messages: Message[]): ChatMessage[]` as a local (non-exported) function. It maps `id`, `role` (cast to union), `parts` (cast to `ChatMessage["parts"]`), and `metadata.createdAt` — functionally equivalent to OLD. **Differences:** (1) OLD throws `ChatSDKError` if `message.id` is missing (L183-187); NEW does not validate message IDs. (2) OLD uses `UIMessagePart<CustomUIDataTypes, ChatTools>[]` type cast for parts; NEW uses `ChatMessage["parts"]`. (3) NEW is file-local, not exported as a shared utility. The issue's claim that the function is "Missing" and "not found" is incorrect — it exists and is used at L120. However, P1-BUG-002 in `issues/01-page-components/issues.md` separately tracks the missing ID validation, and the lack of a shared export is a code organization concern, not a missing-function issue.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. OLD had 2 consumers: `chat/[id]/page.tsx` and `api/chat/route.ts:163`. In NEW, the first consumer uses the local copy; the second (stream-chat.action.ts:209-227) uses a different inline mapping to `StreamMessage` (extracts text content only, different shape from `ChatMessage`), so `convertToUIMessages` is architecturally unnecessary for the AI pipeline. Missing ID validation is tracked by P1-BUG-002. Non-export is a code organization preference, not a missing function.

---

#### [P5-FNC-008] Missing `getTextFromMessage` Function

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L203-208

**Description:**
Extracts plain text content from a ChatMessage by filtering text parts and joining them.

**Impact:**
Message text extraction for display or processing will not work.

**Suggested Fix:**
Add to `lib/utils/message.ts` alongside other message utilities.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** The function EXISTS in the new codebase. `features/chat/components/message-editor.tsx:55-68` defines a local `function getTextFromMessage(message: ChatMessage): string` that filters parts by `type === "text"` with a proper TypeScript type guard `(part): part is TextPart`, maps to `.text`, joins with `\n`, and trims. This is functionally equivalent to OLD (`archive/oldapp/lib/utils.ts:203-208`) which joins with empty string `""`. The NEW version is actually improved: (1) adds a type guard for type safety, (2) handles missing `parts` with `return ""` fallback, (3) uses `\n` join for multi-part text readability. The function is used at line 92 of the same file — the only call site in both OLD and NEW codebases (OLD only used it in `message-editor.tsx` too). Not being a shared export is a code organization preference, not a missing function. The issue in `issues/03-shared-components/issues.md:166` already notes this local implementation exists.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. OLD had exactly 1 consumer (`archive/oldapp/components/message-editor.tsx:15`), NEW has exactly 1 consumer (`features/chat/components/message-editor.tsx:92`) — same component. No other consumers exist in either codebase. The NEW local copy is functionally superior (type guard, null safety, trim). Exporting as a shared utility would have zero additional consumers.

---

#### [P5-FNC-009] Missing `getLocalStorage` Function

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/utils.ts`
**NEW File:** N/A - Not found
**Line Ref:** L106-111

**Description:**
SSR-safe localStorage getter that returns empty array if window is undefined.

**Impact:**
Minor - can be implemented inline where needed.

**Suggested Fix:**
Add to `lib/utils/storage.ts` if needed, or implement inline.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** The simple imperative `getLocalStorage(key)` helper (OLD `archive/oldapp/lib/utils.ts:106-111`) returns `JSON.parse(localStorage.getItem(key) || "[]")` with SSR safety via `typeof window !== "undefined"`. The NEW codebase has **replaced this pattern** with a more capable React hook: `hooks/use-local-storage.ts` exports `useLocalStorage(key, initialValue, options)` which provides: (1) SSR-safe initialization, (2) JSON serialization/deserialization with custom serializer support, (3) reactive state updates via `useState`, (4) a `removeValue` function, (5) cross-tab synchronization potential. Additionally, `features/settings/hooks/use-settings.ts` uses `localStorage.getItem/setItem` directly for theme and model selection (lines 318, 327, 389, 398). Grep confirmed zero imports of `getLocalStorage` in the OLD codebase outside its definition — the function was defined but not demonstrably used. The NEW hook-based approach is the idiomatic React replacement.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. Exhaustive grep of the entire `archive/oldapp/` directory for `getLocalStorage` returned only 1 match — the definition at `lib/utils.ts:106`. Zero imports, zero consumers. The function was dead code in OLD. Non-React contexts in NEW (`features/settings/hooks/use-settings.ts`) use `localStorage.getItem/setItem` inline for simple cases, which is adequate. No scenario exists where the OLD helper would be needed.

---

### Error Utilities (errors.ts)

#### [P5-FNC-010] Missing `ChatSDKError` Class - Different Error Architecture

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/errors.ts`
**NEW File:** `lib/errors.ts`
**Line Ref:** L47-106

**Description:**
OLD uses `ChatSDKError` class with:
- `type: ErrorType` (bad_request, unauthorized, forbidden, not_found, rate_limit, offline)
- `surface: Surface` (chat, auth, api, stream, database, history, vote, document, suggestions, activate_gateway, ui)
- `code: ErrorCode` (template literal format `${ErrorType}:${Surface}${optional_suffix}`)
- `userType?: ErrorUserType` (guest, regular, unknown)
- `toResponse()` method that uses `visibilityBySurface` for error visibility control

NEW uses `AppError` class with:
- `code: ErrorCode` (different format - e.g., `VALIDATION_ERROR`, `CHAT_NOT_FOUND`)
- `statusCode: number`
- `details: Record<string, unknown>`
- `timestamp: Date`
- Multiple subclasses: `ValidationError`, `NotFoundError`, `UnauthorizedError`, `ForbiddenError`, `RateLimitError`, `InternalServerError`, `ServiceUnavailableError`

**Impact:**
All error handling throughout the application needs to be updated to use the new error class structure. Error codes are incompatible.

**Suggested Fix:**
Either:
1. Migrate all error handling to use new `AppError` system
2. Create adapter layer for backward compatibility
3. Keep `ChatSDKError` alongside `AppError` for gradual migration

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Improvement |

**Findings:** This is an intentional architectural redesign, confirmed by APM Memory (`Task_1_2_create_error_system.md:19` — "Reviewed archive/oldapp/lib/errors.ts for existing error patterns (ChatSDKError class)"). The NEW `AppError` system (`lib/errors.ts`) is a modernized replacement providing: (1) type-safe error subclasses (`ValidationError`, `NotFoundError`, `UnauthorizedError`, `ForbiddenError`, `RateLimitError`, `InternalServerError`, `ServiceUnavailableError`), (2) `toResponse()`, `toApiError()`, `toApiResponse()`, `toJSON()` methods, (3) `fromUnknownError()`, `hasErrorCode()`, `isAppError()` type guards, (4) `createEntityNotFoundError()` factory, (5) i18n-ready error messages in `lib/errors/messages.ts` with locale support. The NEW error codes use `SCREAMING_SNAKE_CASE` (`VALIDATION_ERROR`, `CHAT_NOT_FOUND`) instead of OLD template literals (`bad_request:database:unique_violation`). The NEW system does lack `ErrorUserType` (see P5-FNC-013) and `visibilityBySurface` (see P5-FNC-014), but these are tracked as separate issues. The overall error architecture is an improvement, not a defect.

---

#### [P5-FNC-011] Missing `toDatabaseError` Helper Function

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/errors.ts`
**NEW File:** N/A - Not found
**Line Ref:** L167-180

**Description:**
OLD has `toDatabaseError` function that:
- Maps PostgreSQL error codes to `ChatSDKError` codes
- Handles constraint violations (unique, foreign key, not null, check)
- Handles concurrency issues (deadlock, serialization failure)
- Handles connection/availability errors
- Provides detailed error messages for database issues

**Impact:**
Database error handling will not have granular error codes. Users will see generic error messages instead of specific database error explanations.

**Suggested Fix:**
Add `toDatabaseError` function to `lib/errors/` or `lib/db/` with PostgreSQL error code mapping.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `toDatabaseError(operation, err?, cause?)` in `archive/oldapp/lib/errors.ts:167-180` converts Postgres errors to typed `ChatSDKError` instances by: (1) checking if error is a Postgres error via `isPostgresError()` type guard, (2) mapping Postgres error code via `mapPostgresCodeToError()` to a granular `ErrorCode`, (3) constructing detailed cause string with operation name and Postgres error message. Used extensively in OLD data layer — 14+ call sites across `archive/oldapp/lib/data/chat.ts` and `archive/oldapp/lib/data/document.ts`. The NEW repositories (`lib/data/repositories/artifact.repository.ts`, etc.) catch database errors and wrap them in generic `throw new InternalServerError("Failed to X", { error })` — no Postgres error code inspection, no constraint violation differentiation, no operation context. A unique constraint violation (23505) and a connection failure (08006) both produce the same `InternalServerError` in the NEW system. This is a genuine defect that degrades error diagnostics and user messaging.

---

#### [P5-FNC-012] Missing `mapPostgresCodeToError` Function

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/errors.ts`
**NEW File:** N/A - Not found
**Line Ref:** L110-143

**Description:**
Maps PostgreSQL error codes to granular ChatSDKError codes:
- `23505` -> `bad_request:database:unique_violation`
- `23503` -> `bad_request:database:foreign_key_violation`
- `23502` -> `bad_request:database:not_null_violation`
- `40P01` -> `bad_request:database:deadlock_detected`
- `08006` -> `offline:database:connection_failure`
- And more...

**Impact:**
Database errors will not have specific error codes for proper handling and user messaging.

**Suggested Fix:**
Add PostgreSQL error code mapping to `lib/errors/` or create `lib/db/errors.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `mapPostgresCodeToError(code?)` in `archive/oldapp/lib/errors.ts:110-143` is a private function that maps 12 PostgreSQL error codes to granular `ErrorCode` values: constraint violations (`23505` → unique, `23503` → foreign key, `23502` → not null, `23514` → check), concurrency (`40P01` → deadlock, `40001` → serialization failure), permissions (`42501` → insufficient privilege, `42601` → syntax error, `42P01` → undefined table), and connectivity (`08006`/`08001` → connection failure, `57014`/`57000` → timeout). No equivalent mapping exists anywhere in the NEW codebase — grep for `23505|23503|23502|40P01|08006|postgres.*code` across `lib/` returns zero matches. This is tightly coupled to P5-FNC-011 (`toDatabaseError`); the mapping is a prerequisite for granular database error handling. Without it, ALL Postgres errors surface as generic `InternalServerError` / `DATABASE_ERROR` regardless of the underlying cause.

---

#### [P5-FNC-013] Missing Guest-Specific Error Messages

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/errors.ts`
**NEW File:** `lib/errors/messages.ts`
**Line Ref:** L186-210

**Description:**
OLD has `ErrorUserType` type and guest-specific error messages in `getMessageByErrorCode`:
- Guest users see contextual messages explaining guest limitations
- Example: "Chat not found. Guest chat history is temporary and may have expired. Sign in to save your chats permanently."

NEW error messages are generic and don't account for user type context.

**Impact:**
Guest users will see generic error messages instead of contextual messages that guide them to sign in.

**Suggested Fix:**
Add `userType` parameter to error message functions in `lib/errors/messages.ts` and implement guest-specific message variants.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `getMessageByErrorCode(errorCode, userType?)` in `archive/oldapp/lib/errors.ts:186-210` has an `if (userType === "guest")` branch with 6 guest-specific error messages: `not_found:chat` ("Guest chat history is temporary and may have expired. Sign in to save your chats permanently."), `not_found:document`, `rate_limit:chat` / `rate_limit:chat:daily_limit_exceeded` ("Sign in to increase your message allowance."), `offline:chat` ("Guest sessions are stored temporarily — sign in to ensure your chats are saved."), `forbidden:vote:guest_cannot_vote` ("Sign in to rate responses"), `bad_request:api:guest_requires_cache`. The NEW `lib/errors/messages.ts` defines `getErrorMessage(error, locale?)` which takes an `AppError` and optional locale — no user type parameter, no guest-specific message variants, no `ErrorUserType` type. The `errorMessages` constant maps error codes to `{ title, message, action }` sets with zero guest-aware branching. For example, `CHAT_NOT_FOUND` returns the same generic "The requested chat was not found" for both guest and authenticated users. All guest-specific UX guidance (encouraging sign-in) is lost.

---

#### [P5-FNC-014] Missing `visibilityBySurface` Configuration

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/errors.ts`
**NEW File:** N/A - Not found
**Line Ref:** L33-45

**Description:**
OLD has `visibilityBySurface` configuration that controls error visibility:
- `"response"` - error details sent in response
- `"log"` - generic message in response, details logged only
- `"none"` - no error details exposed

This provides security control over which errors expose internal details.

**Impact:**
All errors expose the same level of detail. No granular control over error visibility for security purposes.

**Suggested Fix:**
Add visibility configuration to error handling system or implement in `toResponse()` method.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** While the `visibilityBySurface` configuration structure exists in OLD (`archive/oldapp/lib/errors.ts:33-45`), **all 11 surfaces are set to `"response"`**: `database: "response"`, `chat: "response"`, `auth: "response"`, `stream: "response"`, `api: "response"`, `history: "response"`, `vote: "response"`, `document: "response"`, `suggestions: "response"`, `activate_gateway: "response"`, `ui: "response"`. No surface uses `"log"` or `"none"`. The `toResponse()` method (line 76) checks visibility and has code paths for `"log"` (returns generic message) and falls through to full error details for `"response"` — but the `"log"` path is **dead code** since no surface is configured for it. The OLD `ChatSDKError.toResponse()` and the NEW `AppError.toResponse()` both expose full error details in practice, making them behaviorally identical. The mechanism was designed for future use but never activated. No security gap exists from its absence.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. Re-examined extensibility concern: the config was designed to allow per-surface visibility control (`"response"`, `"log"`, `"none"`), but was never differentiated — all 11 surfaces used identical `"response"` value for the entire OLD codebase lifecycle. The `"log"` and `"none"` code paths in `toResponse()` were dead code. If future extensibility is needed, the NEW `AppError.toResponse()` can be enhanced to add visibility control without backward-compatibility constraints. No current or historical behavioral difference from its absence.

---

### Constants (constants.ts)

#### [P5-FNC-015] Different Default Pagination Limit

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/constants.ts`
**NEW File:** `lib/constants.ts`
**Line Ref:** L79 vs L106

**Description:**
OLD: `DEFAULT_PAGINATION_LIMIT = 10`
NEW: `PAGINATION.defaultPageSize = 20`

**Impact:**
API responses will return 20 items per page instead of 10. This could affect:
- Frontend pagination display
- API consumers expecting 10 items
- Performance if more data is transferred

**Suggested Fix:**
Verify this is intentional. If not, update `PAGINATION.defaultPageSize` to 10 for backward compatibility.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Improvement |

**Findings:** This is an intentional design change, confirmed by APM Memory (`Task_1_3_create_constants_module.md:21` — "PAGINATION - Default pagination values (defaultPageSize: 20, maxPageSize: 100)"). OLD `DEFAULT_PAGINATION_LIMIT = 10` (`archive/oldapp/lib/constants.ts:79`); NEW `PAGINATION.defaultPageSize = 20` (`lib/constants.ts:106`). The NEW value is used consistently: `app/api/history/route.ts:23` hardcodes `"20"` as the fallback limit, and line 28 uses `20` as the `NaN` fallback. The OLD `archive/oldapp/app/(chat)/api/history/route.ts:21` used `DEFAULT_PAGINATION_LIMIT` (10). The increase from 10→20 is a deliberate UX improvement that reduces pagination requests for chat history. The `maxPageSize` remains 100 in both codebases. Not a defect — a conscious behavioral change during the v6 redesign.

---

#### [P5-FNC-016] Missing `MAX_MESSAGES_LIMIT` Constant (Renamed)

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/constants.ts`
**NEW File:** `lib/constants.ts`
**Line Ref:** L73 vs L168

**Description:**
OLD: `MAX_MESSAGES_LIMIT = 1000` (flat constant)
NEW: `MESSAGE_CONSTANTS.maxMessagesLimit = 1000` (nested in object)

**Impact:**
Code importing `MAX_MESSAGES_LIMIT` directly will fail. Need to update imports to use `MESSAGE_CONSTANTS.maxMessagesLimit`.

**Suggested Fix:**
Add export alias: `export const MAX_MESSAGES_LIMIT = MESSAGE_CONSTANTS.maxMessagesLimit` for backward compatibility, or update all imports.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | False Positive |

**Findings:** The constant value is preserved identically (1000) as `MESSAGE_CONSTANTS.maxMessagesLimit` in `lib/constants.ts:168`. The issue claims "code importing `MAX_MESSAGES_LIMIT` directly will fail" — but grep confirms **zero** imports of `MAX_MESSAGES_LIMIT` exist anywhere in the NEW codebase outside `archive/`. The OLD imports were in `archive/oldapp/lib/data/chat.ts:15,226,230,234,307,311,317` which is archived code. The NEW data layer (`lib/data/repositories/chat.repository.ts`) does not reference `maxMessagesLimit` or `MAX_MESSAGES_LIMIT` at all — it doesn't enforce the message limit. `MESSAGE_CONSTANTS` is exported from `lib/index.ts:168` and consumed only internally by `sortMessagesByTimeAndRole()` and `getZScoreWithRoleOffset()` in `lib/constants.ts`. The rename is an intentional restructuring with no backward compatibility issue since no NEW code uses the old import. The non-enforcement of the limit in the NEW data layer is a separate concern (data layer issues).

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. Value is preserved (1000), accessible via `MESSAGE_CONSTANTS.maxMessagesLimit`, exported through `lib/index.ts:168`. The `MESSAGE_CONSTANTS` object is consumed by `lib/constants.ts` internal functions (`sortMessagesByTimeAndRole` at L309-310, `getZScoreWithRoleOffset` at L328) for `roleOrder`, though `maxMessagesLimit` specifically is not enforced in the data layer — that enforcement gap is a data layer concern, not a constants concern. No import breakage exists.

---

### File Utilities (files.ts)

#### [P5-FNC-017] Missing File Attachment Validation Utilities

**Severity:** Critical
**Status:** Open
**OLD File:** `archive/oldapp/lib/files.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-52

**Description:**
The entire `files.ts` module is missing from the NEW codebase. It contains:

1. **`ATTACHMENT_MAX_FILE_SIZE`** (5MB) - Maximum file size for attachments
2. **`ATTACHMENT_ALLOWED_MIME_TYPES`** - Set of allowed MIME types:
   - Images: jpeg, png, gif, webp
   - Documents: pdf, plain text, markdown, csv, json
   - Archives: zip
   - Office: excel, word, powerpoint (old and new formats)
3. **`ATTACHMENT_ALLOWED_TYPE_PREFIXES`** - Allowed type prefixes: `image/`, `audio/`, `video/`
4. **`getAllowedAttachmentMimeTypes()`** - Returns array of allowed MIME types
5. **`isAllowedAttachmentMimeType()`** - Validates if a MIME type is allowed

**Impact:**
- File upload functionality will have no validation
- No file size limits enforced
- No MIME type restrictions
- Security risk: any file type could be uploaded

**Suggested Fix:**
Create `lib/files/validation.ts` or `lib/utils/file.ts` with all file validation utilities.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. No centralized `lib/files.ts` or `lib/files/validation.ts` exists. File validation is scattered across two inline locations: (1) `app/api/files/upload/route.ts:16-27` defines `MAX_FILE_SIZE = 10 * 1024 * 1024` (10MB, doubled from OLD 5MB) and `ALLOWED_TYPES` with only 7 MIME types (jpeg, png, gif, webp, pdf, text/plain, text/markdown). (2) `features/input/types.ts:70-88` defines `DEFAULT_UPLOAD_CONFIG` with the same 7 types and 10MB limit. **Missing from both:** 10 MIME types from OLD (csv, json, zip, octet-stream, Excel old/new, Word old/new, PowerPoint old/new), `ATTACHMENT_ALLOWED_TYPE_PREFIXES` for audio/ and video/ prefix matching, the `isAllowedAttachmentMimeType()` function with null/undefined security guard, and the `getAllowedAttachmentMimeTypes()` helper for Zod schema integration. The route uses simple `ALLOWED_TYPES.includes(contentType)` with no prefix matching — audio and video files cannot be uploaded. Validation is duplicated between route and feature config with no single source of truth. Cross-referenced by P6-FNC issues in `issues/06-api-routes/issues.md:806,825` which independently confirm the reduced MIME set and doubled file size.

---

### AI Utilities (lib/ai/)

#### [P5-FNC-018] Missing `constants.ts` - AI Configuration Constants

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/constants.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-65

**Description:**
AI-specific constants are missing:

**Missing Constants:**
- `DEFAULT_MODEL_ID = "openai:gpt-4o-mini"`
- `DEFAULT_TEMPERATURE = 0.7`
- `DEFAULT_MAX_OUTPUT_TOKENS = 4096`
- `DEFAULT_TOP_P = 0.95`
- `MAX_CONTEXT_TOKENS = 128_000`
- `SYSTEM_PROMPT_RESERVE_TOKENS = 2000`
- `TITLE_GENERATION_MAX_TOKENS = 80`
- `MODEL_CACHE_TTL_MS = 60 * 60 * 1000` (1 hour)
- `MODEL_DISCOVERY_TIMEOUT_MS = 5000`
- `DEFAULT_MESSAGES_PER_MINUTE = 20`
- `DEFAULT_TOKENS_PER_MINUTE = 100_000`
- `STREAM_CHUNK_SIZE = 1024`
- `STREAM_TIMEOUT_MS = 30_000`
- `SUPPORTED_PROVIDERS` array
- `MODEL_CATEGORIES` object

**Impact:**
No centralized AI configuration. Magic numbers scattered or defaults not applied.

**Suggested Fix:**
Create `lib/ai/constants.ts` with all AI-specific constants.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. `lib/ai/constants.ts` does not exist — directory listing of `lib/ai/` shows only: `context-window.ts`, `index.ts`, `providers.ts`, `registry.ts`, `token-counter.ts`. Some OLD constant values have scattered inline equivalents: `temperature: 0.7` in `features/settings/types.ts:273`, `maxOutputTokens: 4096` in `features/settings/types.ts:275`, `DEFAULT_MODEL_ID` replaced by `getDefaultChatModel()` in `registry.ts` (priority-based, not a fixed constant). However, the following 9 constants from OLD have **no equivalent anywhere** in the NEW codebase: `SYSTEM_PROMPT_RESERVE_TOKENS` (2000), `TITLE_GENERATION_MAX_TOKENS` (80), `MODEL_CACHE_TTL_MS` (1 hour), `MODEL_DISCOVERY_TIMEOUT_MS` (5000), `DEFAULT_MESSAGES_PER_MINUTE` (20), `DEFAULT_TOKENS_PER_MINUTE` (100000), `STREAM_CHUNK_SIZE` (1024), `STREAM_TIMEOUT_MS` (30000), `MODEL_CATEGORIES` object. The `SUPPORTED_PROVIDERS` array is replaced by the dynamic `availableProviderIds` in `providers.ts:87` which is architecturally equivalent but not a constant. The model defaults being hardcoded inline rather than centralized violates DRY — changes require editing multiple files (`features/settings/types.ts`, potentially `features/chat/` etc.).

---

#### [P5-FNC-019] Missing `entitlements.ts` - User Model Entitlements

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/entitlements.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-31

**Description:**
User entitlements for model access are missing:

**Missing:**
- `Entitlements` type with `maxMessagesPerDay` and `availableChatModelIds`
- `entitlementsByUserType` configuration:
  - `guest`: 20 messages/day, all models
  - `regular`: 100 messages/day, all models

**Impact:**
No rate limiting based on user type. No model access control.

**Suggested Fix:**
Create `lib/ai/entitlements.ts` or integrate into `features/auth/` or `lib/rate-limit/`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. No `entitlements.ts` exists anywhere in the NEW codebase — not in `lib/ai/`, `lib/auth/`, or `features/auth/`. Grep for `entitlements` in `lib/` returns zero matches (only archive and spec references). The v6 spec (`functional-structure-v6.md:3515-3601`) prescribes `lib/auth/entitlements.ts` with `Entitlements` type, `entitlementsByUserType` record, and `getEntitlements()` function, but this was never implemented. **Functional gaps:** (1) No per-day message limit per user type — OLD had `guest: 20/day`, `regular: 100/day`; NEW `RATE_LIMITS.chat` in `lib/constants.ts` is `60/minute` for all users regardless of type. (2) No user-type-based model access control — OLD's `availableChatModelIds` per user type has no equivalent. (3) No `Entitlements` type for downstream consumption. The OLD `entitlements.ts` also imports `listChatModels()` from `model-registry` which maps to `listChatModels()` in `registry.ts` — the dependency exists but the consumer does not.

---

#### [P5-FNC-020] Missing `model-discovery.ts` - Dynamic Model Discovery

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-discovery.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-407

**Description:**
Dynamic model discovery from provider APIs is missing:

**Missing Functions:**
- `discoverProviders(options)` - Discovers models from all providers
- `discoverOpenAI(options)` - Fetches OpenAI model list
- `discoverGoogleGemini(options)` - Fetches Gemini model list
- `discoverOpenRouter(options)` - Fetches OpenRouter model list
- `discoverCloudflareWorkers(options)` - Fetches Cloudflare models
- `forceRefresh()` - Clears provider cache

**Missing Types:**
- `DiscoveryResult` - Results with catalogs and errors
- `DiscoveryOptions` - Options with signal and forceRefresh

**Missing Features:**
- 1-hour cache for model lists
- Parallel provider discovery with `Promise.allSettled`
- Error handling per provider

**Impact:**
Model list is static. New models from providers won't appear without code changes.

**Suggested Fix:**
Create `lib/ai/model-discovery.ts` or accept static model list as architectural decision.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. `lib/ai/model-discovery.ts` does not exist — directory listing of `lib/ai/` confirms absence. The OLD module (407 lines) provided: parallel provider discovery via `Promise.allSettled` for OpenAI, Google Gemini, OpenRouter, and Cloudflare Workers APIs; 1-hour in-memory cache via `providerCaches` Map with `withCache()` wrapper; `forceRefresh()` to clear cache; per-provider error isolation; `DiscoveryResult` type with catalogs + errors. The NEW `lib/ai/registry.ts` uses a static `curatedModels` array (9 models hardcoded) with no runtime refresh capability. `getModel(id)` falls back to `providerRegistry.languageModel(id)` which can resolve any model the provider SDK supports, but the UI can only show what's in `curatedModels`. The OLD `model-selector.tsx:213` called `forceRefreshModelCatalog()` to refresh; the NEW equivalent would have no effect. This is a genuine functional regression — new models from providers won't appear without code changes. Could be an intentional architectural simplification but should be documented as a decision.

---

#### [P5-FNC-021] Missing `model-catalog-types.ts` - Model Metadata Types

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-catalog-types.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-59

**Description:**
Detailed model metadata types are missing:

**Missing Types:**
- `ModelCapability` - Extended capabilities including:
  - `"image-generation"` and `"video-generation"`
- `ModelModality` - "text" | "vision" | "audio"
- `ReasoningType` - Chain-of-thought types:
  - `"openai-thinking"`
  - `"anthropic-thinking"`
  - `"gemini-thinking"`
  - `"deepseek-thinking"`
  - `"internal-thinking"`
  - `"none"`
- `ModelMetadata` - Extended metadata with:
  - `reasoningType?: ReasoningType`
  - `thinkingBudget?: number`
  - `source: "curated" | "discovered"`
  - `isCurated: boolean`
- `ProviderCatalog` - Provider with models array
- `ModelCatalogResponse` - Full catalog response

**Impact:**
No reasoning model support. No image/video generation capability tracking.

**Suggested Fix:**
Create `lib/ai/types.ts` with all model metadata types.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. `lib/ai/model-catalog-types.ts` does not exist. The NEW `lib/ai/registry.ts` defines `ModelCapabilities` (interface with 5 boolean flags: `chat`, `vision`, `tools`, `reasoning`, `code`) and `ModelDefinition` as replacements. **Critical missing types with no equivalent:** (1) `ReasoningType` union — OLD has 6 provider-specific values (`openai-thinking`, `anthropic-thinking`, `gemini-thinking`, `deepseek-thinking`, `internal-thinking`, `none`) used by `getReasoningTagName()` in `providers.ts` and `chat-completion.ts` for middleware wrapping; NEW has only `reasoning: boolean` which cannot distinguish extraction mechanisms. (2) `ModelModality` — OLD has `"text" | "vision" | "audio"`; NEW `vision` is a boolean with no `audio` type. (3) `thinkingBudget` — no equivalent for controlling reasoning token budget. (4) `image-generation` and `video-generation` capabilities — absent from NEW's boolean flags. (5) `ProviderCatalog` and `ModelCatalogResponse` — needed for discovery (P5-FNC-020). (6) `source: "curated" | "discovered"` — no equivalent since discovery doesn't exist. The boolean-flag approach in NEW is simpler but lossy — it cannot represent provider-specific reasoning mechanisms, which are needed for correct chain-of-thought extraction.

---

#### [P5-FNC-022] Missing `provider-info.ts` - Provider ID Types

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/provider-info.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-16

**Description:**
Provider ID type and display names are missing:

**Missing:**
- `ProviderId` type: "openai" | "google" | "openrouter" | "vercel-gateway" | "cloudflare-workers" | "cloudflare-ai-gateway"
- `PROVIDER_DISPLAY_NAMES` record

**Note:** NEW has similar but not identical provider handling in `providers.ts`.

**Impact:**
No centralized provider ID type. Display names not standardized.

**Suggested Fix:**
Add `ProviderId` type and display names to `lib/ai/providers.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. `lib/ai/provider-info.ts` does not exist. The NEW `lib/ai/providers.ts` defines a `providers` record with keys `openai`, `google`, `xai`, `openrouter`, `vercel-gateway` — but these are plain string keys with no `ProviderId` type union. `availableProviderIds` in `providers.ts:87` is typed as `string[]`, not a typed union. No `PROVIDER_DISPLAY_NAMES` record exists — model display names are embedded in `ModelDefinition.name` in `registry.ts` (e.g., `"GPT-4o"`, `"Gemini 2.5 Flash"`) which are model-level, not provider-level. OLD had `ProviderId` type with 6 providers (including `cloudflare-workers` and `cloudflare-ai-gateway` which are absent from NEW). The lack of a typed `ProviderId` union means provider string comparisons are untyped — typos like `"opanai"` won't be caught at compile time. The missing `PROVIDER_DISPLAY_NAMES` means there's no standardized way to show provider names in the UI (e.g., "Google Gemini" vs "google").

---

#### [P5-FNC-023] Missing Reasoning Model Support in providers.ts

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/providers.ts`
**NEW File:** `lib/ai/providers.ts`
**Line Ref:** L17-37, L77-86

**Description:**
NEW `providers.ts` is missing reasoning model support:

**Missing in NEW:**
1. `getReasoningTagName(reasoningType)` - Maps reasoning types to tag names:
   - `"openai-thinking"` -> `"think"`
   - `"anthropic-thinking"` -> `"thinking"`
   - `"gemini-thinking"` -> `"think"`
   - `"deepseek-thinking"` -> `"think"`
   - `"internal-thinking"` -> `"think"`

2. `extractReasoningMiddleware` and `wrapLanguageModel` - Wraps models for chain-of-thought extraction

3. Test environment handling with mock models

**Impact:**
Reasoning models (o1, Claude extended thinking, Gemini thinking) won't have chain-of-thought extraction.

**Suggested Fix:**
Add reasoning middleware support to `lib/ai/providers.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. Reasoning model middleware support is completely absent from the NEW codebase. (1) `extractReasoningMiddleware` from `ai` SDK — grep in `lib/` and `features/` returns zero matches outside `archive/`. (2) `wrapLanguageModel` — same, zero matches outside `archive/`. (3) `getReasoningTagName()` — does not exist. (4) `myProvider` — does not exist; references in `features/chat/lib/tools/suggestions.tool.ts:69` and `features/chat/lib/tools/index.ts:111` are in JSDoc comments only, not actual code. The NEW `getModel(id)` in `registry.ts:316-328` resolves models via `provider.languageModel(modelId)` and falls back to `providerRegistry.languageModel(id)` — but **never** wraps the model with reasoning middleware regardless of capabilities. The NEW `getReasoningModel()` in `registry.ts:354-368` returns a `ModelDefinition` (metadata) not a wrapped `LanguageModelV2`. Therefore, reasoning models (OpenAI o1/o3, Claude extended thinking, Gemini thinking, DeepSeek R1) will return raw responses without chain-of-thought extraction — the thinking/reasoning tokens won't be separated from the response content. This also breaks the OLD `artifact-model` virtual ID resolution pattern (OLD `myProvider` resolved `"artifact-model"` to `DEFAULT_ARTIFACT_MODEL`; no equivalent exists in NEW). Additionally, no test environment mock model handling exists in NEW `providers.ts` (OLD had `isTestEnvironment` check returning mock models).

---

#### [P5-FNC-024] Missing `myProvider` Export Pattern

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/providers.ts`
**NEW File:** `lib/ai/providers.ts`
**Line Ref:** L39-90

**Description:**
OLD exports `myProvider` object with `languageModel(id)` method that:
- Resolves "artifact-model" to default artifact model
- Wraps reasoning models with middleware
- Returns `LanguageModelV2` instances

NEW exports individual providers (`openai`, `google`, etc.) and `getModel(id)` function separately.

**Impact:**
Code expecting `myProvider.languageModel(id)` pattern will fail.

**Suggested Fix:**
Add `myProvider` export for backward compatibility, or update all usage sites.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Improvement |

**Findings:** The `myProvider` export pattern has been deliberately replaced by a different architecture in NEW. OLD `archive/oldapp/lib/ai/providers.ts:39-90` exports `myProvider` with a `languageModel(id)` façade that resolves `"artifact-model"` aliases, wraps reasoning models with `extractReasoningMiddleware`, and provides test-environment mock models. NEW `lib/ai/registry.ts` exports `getModel(id)` which performs equivalent model resolution: looks up `ModelDefinition` by ID, gets the provider via `getProvider(definition.provider)`, and calls `provider.languageModel(definition.modelId)`. Grep confirms no NEW code imports `myProvider` — the only references outside `archive/` are JSDoc comments in `features/chat/lib/tools/suggestions.tool.ts:69` and `features/chat/lib/tools/index.ts:111`. All NEW call sites use `getModel()` or `getProvider()` directly. This is a deliberate architectural simplification, not a missing function. However, two capabilities from the `myProvider` pattern are genuinely absent: (1) reasoning middleware wrapping (tracked in P5-FNC-023), (2) test environment mock model support. Since no NEW code depends on the `myProvider` export and the equivalent functionality exists via `getModel()`, this is an **Improvement** — the new API is cleaner and more explicit.

---

#### [P5-FNC-025] Missing Cloudflare AI Gateway Support

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-registry.ts`
**NEW File:** `lib/ai/providers.ts`
**Line Ref:** L98-160

**Description:**
NEW is missing Cloudflare AI Gateway provider support:

**Missing:**
- `cloudflare-ai-gateway` provider
- `createAiGateway` from `ai-gateway-provider`
- Fallback model configuration (primary + fallback)
- Supported Gemini models list for gateway

**Impact:**
Applications using Cloudflare AI Gateway cannot be deployed.

**Suggested Fix:**
Add Cloudflare AI Gateway support to `lib/ai/providers.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed. OLD `archive/oldapp/lib/ai/model-registry.ts:98-160` has a complete Cloudflare AI Gateway implementation: checks `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_AI_GATEWAY_NAME`, and `CLOUDFLARE_AI_GATEWAY_API_KEY` env vars; creates `aigateway` via `createAiGateway` from `ai-gateway-provider`; builds a custom `cloudflareAiGatewayProvider` with `languageModel(id)` that validates against a `supportedGeminiModels` list (`gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-2.5-pro`), creates a primary Google model + flash-lite fallback, and composes them via `aigateway([primaryModel, fallbackLite])`. The OLD curated models (`archive/oldapp/lib/ai/curated-models.ts:547-582`) include two `cloudflare-ai-gateway:*` models. NEW `lib/ai/providers.ts` has 5 providers: `openai`, `google`, `xai`, `openrouter`, `vercel-gateway` — NO `cloudflare-ai-gateway`. Grep across all `lib/` files confirms zero matches for `createAiGateway`, `cloudflare-ai-gateway`, `CLOUDFLARE_AI_GATEWAY_NAME`, or `ai-gateway-provider`. The `ai-gateway-provider` package would also need to be installed. This is a genuine defect for deployments that rely on Cloudflare AI Gateway for model routing with fallback.

---

#### [P5-FNC-026] Missing Cloudflare Workers AI Support

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-registry.ts`
**NEW File:** `lib/ai/providers.ts`
**Line Ref:** L85-96

**Description:**
NEW is missing Cloudflare Workers AI provider:

**Missing:**
- `cloudflare-workers` provider
- `createWorkersAI` from `workers-ai-provider`
- Environment variables: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_KEY`, `CLOUDFLARE_WORKER_AI`

**Impact:**
Applications using Cloudflare Workers AI cannot be deployed.

**Suggested Fix:**
Add Cloudflare Workers AI support to `lib/ai/providers.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed. OLD `archive/oldapp/lib/ai/model-registry.ts:85-96` registers `cloudflare-workers` provider: reads `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_KEY`, and `CLOUDFLARE_WORKER_AI` env vars; creates provider via `createWorkersAI(workersSettings)` from `workers-ai-provider` with either API key auth or worker binding auth; registers as `"cloudflare-workers"` in `baseProviders`. The `model-discovery.ts` also discovers Cloudflare Workers models dynamically via their REST API (`/client/v4/accounts/${accountId}/ai/models/search`). NEW `lib/ai/providers.ts` has zero Cloudflare-related code — grep confirms no matches for `createWorkersAI`, `workers-ai-provider`, `cloudflare-workers`, `CLOUDFLARE_ACCOUNT_ID`, or `CLOUDFLARE_API_KEY` anywhere in `lib/`. This is a genuine defect for deployments that use Cloudflare Workers AI.

---

#### [P5-FNC-027] Missing `refreshModelCatalog` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-registry.ts`
**NEW File:** `lib/ai/registry.ts`
**Line Ref:** L207-241

**Description:**
NEW registry is missing dynamic catalog refresh:

**Missing Functions:**
- `refreshModelCatalog(options)` - Refreshes model list from providers
- `forceRefreshModelCatalog()` - Forces cache refresh
- `getModelCatalog()` - Gets current model catalog
- `listProviderCatalogs()` - Lists catalogs by provider

**Impact:**
Model list cannot be refreshed at runtime. Requires restart to see new models.

**Suggested Fix:**
Add catalog refresh functions if dynamic discovery is implemented.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified (Defect) |

**Findings:** Confirmed. OLD `archive/oldapp/lib/ai/model-registry.ts:207-282` exports four catalog management functions: (1) `refreshModelCatalog(options?)` — calls `discoverProviders()` from `model-discovery.ts`, filters results by available providers, merges discovered models with curated models (curated take precedence for `isCurated` flag), falls back to curated-only on error; (2) `forceRefreshModelCatalog()` — calls `refreshModelCatalog({ force: true })`; (3) `getModelCatalog()` — returns current `modelCatalog` array; (4) `listProviderCatalogs()` — returns provider-grouped catalogs, falling back to curated models grouped by provider. NEW `lib/ai/registry.ts` has `listModels()` which simply calls `filterByAvailableProviders(curatedModels)` — a static filter of a hardcoded array. No `refreshModelCatalog`, no `forceRefreshModelCatalog`, no `getModelCatalog`, no `listProviderCatalogs` exist in the NEW codebase (grep confirmed zero matches outside `archive/`). The entire `model-discovery.ts` module is also absent from NEW. This means the model list is permanently static — adding new models requires a code change and redeployment. This is a genuine defect if dynamic model discovery is desired; if the team intentionally opted for static models, it's an accepted limitation.

---

#### [P5-FNC-028] Missing `getLanguageModel` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/model-registry.ts`
**NEW File:** `lib/ai/registry.ts`
**Line Ref:** L349-361

**Description:**
OLD has `getLanguageModel(id)` that:
- Looks up metadata to get actual `modelId`
- Resolves via `providerRegistry.languageModel()`
- Returns `LanguageModelV2` instance

NEW has `getModel(id)` with similar functionality but different return pattern.

**Impact:**
Minor - function exists with different name. Update call sites.

**Suggested Fix:**
Add `getLanguageModel` as alias for `getModel` for backward compatibility.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Improvement |

**Findings:** The function exists with a different name and equivalent behavior. OLD `archive/oldapp/lib/ai/model-registry.ts:349-361` exports `getLanguageModel(id)`: looks up `ModelMetadata` via `getModelById(id)`, resolves `${metadata.providerId}:${metadata.modelId}`, calls `providerRegistry.languageModel(resolvedId)`, returns `LanguageModelV2`. NEW `lib/ai/registry.ts:285-298` exports `getModel(id)`: looks up `ModelDefinition` via `getModelById(id)`, calls `getProvider(definition.provider).languageModel(definition.modelId)`, falls back to `providerRegistry.languageModel(id)`, returns `LanguageModelV2`. Both perform identical operations: metadata lookup → provider-specific model ID resolution → language model instantiation. Grep confirms zero imports of `getLanguageModel` in NEW code (only in `archive/`). All NEW code uses `getModel()` from `lib/ai/registry`. No backward-compatibility concern exists since no NEW code depends on the old name. This is a straightforward rename — the issue is a **False Positive** regarding breakage but an accurate observation about the name change. Marking as **Improvement** since the name is shorter and the function location moved from `model-registry.ts` to `registry.ts` as part of the architectural consolidation.

---

#### [P5-FNC-029] Reduced Curated Model List

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/ai/curated-models.ts`
**NEW File:** `lib/ai/registry.ts`
**Line Ref:** L68-240 (NEW) vs L1-582 (OLD)

**Description:**
NEW has significantly fewer curated models:

**OLD Models (582 lines, ~40+ models):**
- Title-specific model
- OpenAI: GPT-4o, GPT-4.1
- Google: Gemini 3.0 Pro, Gemma 3 family (1B, 4B, 12B, 27B), Gemini 2.5 family
- Google Image/Video: Imagen 4, Veo 3
- Anthropic: Claude 3.7 Sonnet, Claude 3.5 Sonnet
- DeepSeek: R1, V3
- Alibaba: Qwen Max
- Venice: Uncensored (free)
- OpenAI OSS: GPT-OSS 120B, 20B (free)
- GLM-4.5 Air (free)
- Kimi K2 (free)

**NEW Models (240 lines, ~9 models):**
- OpenAI: GPT-4o, GPT-4o-mini, o1
- Google: Gemini 2.0 Flash, 2.5 Flash, 2.5 Pro
- XAI: Grok 2
- Vercel Gateway: GPT-4o
- OpenRouter: Claude 3.5 Sonnet

**Missing in NEW:**
- Gemma 3 family (open source)
- Claude 3.7 Sonnet (latest)
- DeepSeek models (free reasoning)
- Free models via OpenRouter
- Image/video generation models

**Impact:**
Fewer model choices for users. No free model options.

**Suggested Fix:**
Expand curated model list in `lib/ai/registry.ts` or implement dynamic discovery.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD `archive/oldapp/lib/ai/curated-models.ts` has 582 lines with ~35+ model entries across 12 categories: Title model (gemini-flash-lite-latest), OpenAI (GPT-4o-latest, GPT-4.1), Google Gemini (3.0 Pro Preview, 2.5 Pro/Flash/Flash-Lite/Flash-Image), Gemma 3 family (1B/4B/12B/27B), Google Imagen (4 Generation/Fast/Ultra), Google Veo (3 Generation/Fast), Anthropic (Claude 3.7/3.5 Sonnet via OpenRouter), DeepSeek (R1 free, V3 free), Alibaba (Qwen Max, Qwen 3 Coder free), Venice (Uncensored free), OpenAI OSS (120B/20B free), GLM-4.5 Air (free), Kimi K2 (free), Cloudflare AI Gateway (2 models). NEW `lib/ai/registry.ts` hardcodes 9 models: OpenAI (GPT-4o, GPT-4o-mini, o1), Google (Gemini 2.0 Flash, 2.5 Flash, 2.5 Pro), XAI (Grok 2), Vercel Gateway (GPT-4o), OpenRouter (Claude 3.5 Sonnet). Missing from NEW: title-specific model, GPT-4.1, Gemini 3.0 Pro, Gemma 3 family (4 models), Imagen 4 (3 variants), Veo 3 (2 variants), Claude 3.7 Sonnet, DeepSeek R1/V3, Qwen Max, Qwen 3 Coder, Venice, GPT-OSS 120B/20B, GLM-4.5 Air, Kimi K2, Cloudflare AI Gateway models. The reduction eliminates ALL free models, ALL image/video generation models, ALL open-source models, and the latest Claude/GPT variants. Additionally the NEW `ModelDefinition` type uses a flat `ModelCapabilities` interface (boolean flags) vs OLD `ModelMetadata` which uses string arrays for `capabilities` and `modalities` — the OLD format is more extensible.

---

### API Utilities (lib/api/)

#### [P5-FNC-030] Missing `requireAuth` and `requireAuthForRoute` Functions

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/guards.ts`
**NEW File:** N/A - Not found in lib/api/
**Line Ref:** L50-101

**Description:**
OLD has centralized authentication guards in `lib/api/guards.ts`:
- `requireAuth(surface)` - Returns `{ session, ctx }` or throws `ChatSDKError`
- `requireAuthForRoute(surface)` - Returns `{ session, ctx }` or `Response` on error

NEW has auth guards in `lib/auth/guards.ts` instead:
- `requireAuth(options)` - Returns `{ userId }` or redirects
- `requireAuthAction()` - Returns `userId` or throws `UnauthorizedError`

**Key Differences:**
1. OLD guards return `AppSession` and `DataContext`, NEW returns just `userId`
2. OLD guards have `surface` parameter for error context, NEW doesn't
3. OLD has `requireAuthForRoute` that returns `Response`, NEW doesn't have route-specific variant
4. NEW guards are in `lib/auth/` not `lib/api/`

**Impact:**
Code expecting `requireAuth` to return `session` and `ctx` will fail. Route handlers expecting `Response` returns need adaptation.

**Suggested Fix:**
Either:
1. Create `lib/api/guards.ts` with route-specific guards that return `Response`
2. Update all call sites to use `lib/auth/guards.ts` pattern

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD `archive/oldapp/lib/api/guards.ts:50-101` exports `requireAuth(surface: Surface)` returning `{ session: AppSession, ctx: DataContext }` with `ChatSDKError` on failure, and `requireAuthForRoute(surface)` returning `AuthResult | Response`. NEW `lib/auth/guards.ts` exports: (1) `requireAuth(options?: GuardOptions)` returning `{ userId: string }` — no session object, no DataContext, no `surface` parameter; (2) `requireAuthAction()` returning `string` (userId only) — actively used by 20+ imports across `app/api/`, `features/chat/actions/`, `features/artifact/actions/`. Key regressions: (a) No `requireAuthForRoute` variant — API route handlers in `app/api/` currently catch errors manually instead of getting `Response` returns; (b) `requireAuth` returns only `userId`, not `{ session, ctx }` — callers needing session details (user type, guest status) or DataContext must make additional calls; (c) No `surface` parameter means error messages lack context about which surface (chat, document, vote, etc.) triggered the auth failure, reducing error diagnostic quality. The OLD `lib/api/guards.ts` was a unified auth+context builder; NEW splits this into `lib/auth/guards.ts` (auth only) + separate session/context retrieval. The functionality exists but with a significantly different and less ergonomic API.

---

#### [P5-FNC-031] Missing `requireRateLimit` and `requireRateLimitForRoute` Functions

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/guards.ts`
**NEW File:** N/A - Not found
**Line Ref:** L118-204

**Description:**
OLD has rate limiting guards integrated with API utilities:
- `requireRateLimit(limiterType, identifier, surface)` - Throws `ChatSDKError` if exceeded
- `requireRateLimitForRoute(...)` - Returns `RateLimitResult` or `Response`
- `requireCustomRateLimit(config, surface)` - Custom rate limit config
- `requireCustomRateLimitForRoute(...)` - Returns `Response` variant

NEW has rate limiting in `lib/rate-limit/` but no integration with API response builders.

**Impact:**
API routes need to manually integrate rate limiting with response handling. No unified pattern for rate limit errors.

**Suggested Fix:**
Create `lib/api/rate-limit.ts` with route-aware rate limit guards that return `Response` objects.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T14:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD `archive/oldapp/lib/api/guards.ts:118-204` exports four rate-limiting guards: (1) `requireRateLimit(limiterType, identifier, surface)` — one-liner that checks rate + throws `ChatSDKError` on failure; (2) `requireRateLimitForRoute(...)` — returns `RateLimitResult | Response`; (3) `requireCustomRateLimit(config, surface)` — custom config variant; (4) `requireCustomRateLimitForRoute(...)` — custom with Response. These were used extensively in OLD routes (grep: 13 files importing from `lib/api/guards`). NEW `lib/rate-limit/` provides: `RateLimiter` class with `consumeToken(key)` returning `RateLimitResult`, convenience functions (`checkChatLimit`, `checkApiLimit`, etc.), and predefined limiters (`chatLimiter`, `apiLimiter`, `authLimiter`, `uploadLimiter`). However, NEW has NO `requireRateLimit` guard — no single function that checks + throws on failure. Instead, rate limiting is applied at two levels: (a) `middleware.ts:83-86` applies `applyRateLimit` at the middleware layer for all routes; (b) server actions (e.g., `features/chat/actions/delete-chat.action.ts:87-91`) manually call `checkApiLimit(userId)`, check `!result.success`, call `getRetryAfter(result.reset)`, then throw `new RateLimitError(...)` — requiring ~5 lines of boilerplate per call site vs OLD's single `await requireRateLimit("standard", userId, "chat")`. No `requireRateLimitForRoute` returning `Response` exists anywhere. The middleware-level rate limiting partially mitigates the missing guards, but action-level rate limiting lacks the ergonomic unified pattern.

---

#### [P5-FNC-032] Missing `verifyOwnership` and `verifyOwnershipForRoute` Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/guards.ts`
**NEW File:** `lib/auth/guards.ts` (similar but different)
**Line Ref:** L229-260

**Description:**
OLD has ownership verification in API guards:
- `verifyOwnership(resource, session, surface)` - Throws if mismatch
- `verifyOwnershipForRoute(...)` - Returns `Response` on failure

NEW has `requireOwnership(resourceOwnerId, userId?)` in `lib/auth/guards.ts`:
- Throws `ForbiddenError` if mismatch
- No route-specific variant that returns `Response`

**Impact:**
API routes need to catch errors and convert to responses manually.

**Suggested Fix:**
Add `requireOwnershipForRoute` function to `lib/api/` that returns `Response`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Improvement |

**Findings:** The issue claims `verifyOwnership` and `verifyOwnershipForRoute` are missing, but NEW has a functional equivalent: `requireOwnership(resourceOwnerId: string, userId?: string)` in `lib/auth/guards.ts:178-194`. It throws `ForbiddenError` instead of `ChatSDKError` and auto-resolves the current userId from the session if not provided. Additionally, NEW provides `requireChatAccess(chat)` and `requireChatModification(chat)` (L282-316) for domain-specific ownership guards with visibility-aware access control (public chats readable by non-owners). The `ForRoute` variant that returns `Response` is architecturally unnecessary — NEW API routes (`app/api/chat/route.ts`, `app/api/history/route.ts`) all use try/catch with `error()` response builder at the route boundary, making throw-based guards the correct pattern. The only genuine gap is the loss of the `surface` parameter for error context granularity (e.g., `forbidden:chat:owner_mismatch` vs generic "You do not have access to this resource"). Marking as **Improvement** — the function exists with a better API design, ForRoute is architecturally unnecessary.

---

#### [P5-FNC-033] Missing `requireResource` and `requireResourceForRoute` Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/guards.ts`
**NEW File:** N/A - Not found
**Line Ref:** L277-304

**Description:**
OLD has resource existence checks:
- `requireResource<T>(resource, surface)` - Returns `T` or throws `ChatSDKError`
- `requireResourceForRoute<T>(...)` - Returns `T` or `Response`

**Impact:**
No standardized way to handle "not found" cases in API routes with proper error responses.

**Suggested Fix:**
Add `requireResource` helper to `lib/api/validation.ts` or create `lib/api/guards.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. No `requireResource<T>` generic guard exists in the NEW codebase. OLD provides a clean generic utility (`archive/oldapp/lib/api/guards.ts:277-305`) that takes any `T | null | undefined`, throws `ChatSDKError` with `not_found:${surface}`, and returns narrowed `T`. NEW code handles resource-not-found checks inline — e.g., `app/api/history/route.ts` checks `if (!result.success) return error(...)`, and `ChatService.getWithMessages` returns `null` (caller handles). Grep for `requireResource` across `lib/`, `app/`, `features/`, `components/` returned zero non-archive matches. The `ForRoute` variant is architecturally unnecessary (NEW uses try/catch), but the base `requireResource<T>` convenience utility providing null-narrowing and consistent error codes would reduce boilerplate across services and routes. Verified as a genuine missing utility.

---

#### [P5-FNC-034] Missing `requireNonGuest` and `requireNonGuestForRoute` Functions

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/guards.ts`
**NEW File:** `lib/auth/guards.ts` (similar)
**Line Ref:** L320-352

**Description:**
OLD has guest user restrictions:
- `requireNonGuest(session, surface, action)` - Throws if guest
- `requireNonGuestForRoute(...)` - Returns `Response` if guest

NEW has `requireAuthenticatedUser()` which serves similar purpose but:
- No `action` parameter for contextual error messages
- No route-specific variant

**Impact:**
Minor - similar functionality exists with different API.

**Suggested Fix:**
Add `action` parameter to `requireAuthenticatedUser` for better error messages.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Improvement |

**Findings:** The core functionality EXISTS in NEW but with a simplified API. OLD `requireNonGuest(session, surface, action)` at `archive/oldapp/lib/api/guards.ts:320-336` takes 3 parameters and throws `ChatSDKError` with contextual error code `forbidden:${surface}:guest_cannot_${action}` and message `Guest users cannot ${action}`. NEW has two near-equivalents: (1) `requireNonGuest()` at `lib/auth/guards.ts:347-354` — zero parameters, auto-fetches session internally, throws generic `UnauthorizedError("Please sign in to continue")`; (2) `requireAuthenticatedUser()` at L119-128 — also zero params, returns `string` (userId), same generic error. **Functional differences:** (a) OLD is synchronous (session passed in), NEW is async (fetches session); (b) OLD provides action-specific error messages ("Guest users cannot vote"), NEW gives generic message; (c) OLD throws `ChatSDKError` (forbidden type), NEW throws `UnauthorizedError` (unauthorized type — arguably wrong, guest IS authenticated but restricted, so `ForbiddenError` would be semantically correct); (d) `ForRoute` variant is architecturally unnecessary in NEW. Marking **Improvement** — the guard exists with a better self-contained API, but loses contextual error messaging and uses wrong error class (Unauthorized vs Forbidden).

---

#### [P5-FNC-035] Missing `parseTimestamp` and `parseTimestampForRoute` Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/validators.ts`
**NEW File:** N/A - Not found
**Line Ref:** L87-120

**Description:**
OLD has timestamp parsing with error handling:
- `parseTimestamp(value, paramName, surface)` - Returns `Date` or throws
- `parseTimestampForRoute(...)` - Returns `Date` or `Response`

**Impact:**
No standardized timestamp validation with proper error responses.

**Suggested Fix:**
Add timestamp parsing helpers to `lib/api/validation.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `parseTimestamp(value, paramName, surface)` at `archive/oldapp/lib/api/validators.ts:87-101` validates a string as ISO date, returns `Date`, or throws `ChatSDKError` with `bad_request:${surface}:invalid_timestamp` and message `Parameter ${paramName} must be a valid date`. NEW `lib/api/validation.ts` provides Zod-based schema validation (`validateQuery`, `validateBody`, `validateParams`) but no dedicated timestamp parsing helper. A developer using New would need to write `z.coerce.date()` or `z.string().datetime()` in a Zod schema — valid but more verbose for a single timestamp parameter. Grep for `parseTimestamp` across non-archive files returned zero matches. The `ForRoute` variant at OLD L108-120 is architecturally unnecessary. Verified — no convenience timestamp parsing function exists in NEW.

---

#### [P5-FNC-036] Missing `requireQueryParam` and `requireQueryParamForRoute` Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/api/validators.ts`
**NEW File:** N/A - Not found
**Line Ref:** L137-170

**Description:**
OLD has required query param extraction:
- `requireQueryParam(searchParams, paramName, surface)` - Returns string or throws
- `requireQueryParamForRoute(...)` - Returns string or `Response`

NEW has `validateQuery` with Zod schema but no simple "required param" helper.

**Impact:**
Need to define Zod schema for simple required param extraction.

**Suggested Fix:**
Add `requireQueryParam` helper to `lib/api/validation.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `requireQueryParam(searchParams, paramName, surface)` at `archive/oldapp/lib/api/validators.ts:137-152` extracts a single required query parameter from `URLSearchParams`, returns the string value or throws `ChatSDKError` with `bad_request:${surface}:missing_${paramName}`. NEW `lib/api/validation.ts` provides `validateQuery(searchParams, schema)` which requires defining a full Zod schema object (e.g., `z.object({ id: z.string().min(1) })`) even for extracting a single required parameter. Grep for `requireQueryParam` across non-archive files returned zero matches. NEW API routes handle query params ad-hoc: `app/api/history/route.ts:25-27` manually does `searchParams.get("limit") || "20"` with `parseInt`, no validation. The `ForRoute` variant is architecturally unnecessary. The Zod approach is more powerful for multi-param validation but adds verbosity for the common case of "extract one required string param". Verified as missing.

---

### Data Layer (lib/data/)

#### [P5-FNC-037] Missing `saveWithContext` Optimized Batch Operation

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/data/chat.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1015-1180

**Description:**
OLD has `messageData.saveWithContext()` that:
- Batches messages + context update in single operation
- Reduces cache operations from ~6 to ~2
- Handles both new chat creation and existing chat updates
- Updates `lastContext` for usage tracking
- Increments quota counter for user messages

NEW has `SaveWithContextParam` in `message.repository.ts` but:
- Simpler implementation without cache optimization
- No quota tracking integration

**Impact:**
Performance degradation from multiple separate operations instead of batched update.

**Suggested Fix:**
Implement batched `saveWithContext` in `MessageRepository` or `ChatService`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Verified (Defect) |

**Findings:** The function EXISTS in NEW (`MessageRepository.saveWithContext` at `lib/data/repositories/message.repository.ts:572-625`) but with **significant functional regressions** compared to OLD (`archive/oldapp/lib/data/chat.ts:1015-1175`):

1. **Missing guest-aware logic**: OLD checks `ctx.isGuest` and uses cache-only path for guests (no DB writes). NEW ignores `_context` parameter entirely (note the underscore prefix) and always writes to DB. Connected to P5-BRK-005.
2. **Missing new chat creation**: OLD handles `isNewChat` flag to atomically create chat + insert messages in one transaction. NEW only inserts messages and updates context — it does not create the chat record. The `isNewChat`, `title`, `visibility`, `createdAt` fields in `SaveWithContextParams` (L50-66) are accepted but **never read** in the implementation (L572-625).
3. **Missing `onConflictDoNothing`**: OLD uses `.onConflictDoNothing({ target: message.id })` to safely handle message re-sends (idempotency). NEW uses plain `.insert()` which will throw on duplicate message IDs.
4. **Missing quota tracking**: OLD increments user message counter via `incrementUserMessageCountAsync(ctx.userId, userMessageCount)` for rate limiting. NEW has zero quota/rate-limit integration.
5. **Missing userId filter on context update**: OLD filters `where(and(eq(chat.id, chatId), eq(chat.userId, ctx.userId)))` for IDOR protection. NEW only filters by `eq(chat.id, chatId)` — any authenticated user could theoretically update another user's chat context (though upstream guards likely prevent this).
6. **Missing fire-and-forget cache warming**: OLD updates cache after DB success (fire-and-forget for performance). NEW only invalidates cache (deletes keys), requiring the next read to re-fetch from DB.

These are genuine functional defects, not just style differences.

---

#### [P5-FNC-038] Missing `deleteAfterTimestamp` for Message Regeneration

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/data/chat.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1190-1254

**Description:**
OLD has `messageData.deleteAfterTimestamp()`:
- Deletes messages at or after a timestamp
- Used for message regeneration and chat editing
- Uses transaction to prevent race conditions
- Deletes associated votes within same transaction
- Updates cache in parallel with DB

**Impact:**
Message regeneration feature cannot work without this functionality.

**Suggested Fix:**
Add `deleteMessagesAfterTimestamp` method to `MessageRepository`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | Verified (Defect) |

**Findings:** The method EXISTS in NEW (`MessageRepository.deleteAfterTimestamp` at `lib/data/repositories/message.repository.ts:641-685`) but has **critical differences** from OLD (`archive/oldapp/lib/data/chat.ts:1190-1254`):

1. **Missing transaction**: OLD wraps the entire delete operation in `db.transaction()` to prevent race conditions between selecting message IDs and deleting them. NEW uses a single `db.delete().where()` call — safe for messages alone but creates problems with related records.
2. **Missing vote cleanup**: OLD first selects message IDs to delete, then within the same transaction deletes associated votes (`tx.delete(vote).where(and(eq(vote.chatId, chatId), inArray(vote.messageId, messageIds)))`) before deleting messages. NEW only deletes messages. **Result:** Orphaned vote records remain in the database for deleted messages, violating referential integrity. The votes table likely has a foreign key to messages (CASCADE would handle this only if the FK is set up with ON DELETE CASCADE — checking OLD schema: `vote.messageId` references `message.id`, but without `onDelete: "cascade"` in the OLD schema definition at `archive/oldapp/lib/db/schema.ts:112-118`). This means vote cleanup MUST be done manually.
3. **Missing guest-specific path**: OLD checks `ctx.isGuest` and skips DB operations for guests (cache-only deletion). NEW ignores context entirely.
4. **Missing parallel cache + DB execution**: OLD runs cache deletion and DB deletion in parallel (`Promise.all([cachePromise, dbPromise])`). NEW deletes from DB first, then invalidates cache sequentially.
5. **Return type difference**: OLD returns `Promise<void>`, NEW returns `Promise<number>` (count of deleted messages) — this is actually an improvement in NEW.

The vote orphaning is the most critical defect. Message regeneration (the primary use case) will leave stale votes in the database.

---

#### [P5-FNC-039] Missing `ChatWithMessages` Return Type

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/data/chat.ts`
**NEW File:** `lib/data/repositories/chat.repository.ts`
**Line Ref:** L187-326 vs L478-500

**Description:**
OLD `chatData.getWithMessages()` returns:
```typescript
ChatWithMessages {
  chat: Chat;
  messages: MessageRow[];
}
```

NEW `chatRepository.findWithMessages()` returns similar but:
- Uses `Message[]` type instead of `MessageRow[]`
- Different type definitions

**Impact:**
Minor type mismatch when migrating code that expects `MessageRow[]`.

**Suggested Fix:**
Verify type compatibility or add type alias for backward compatibility.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T17:30:00Z |
| **Status** | False Positive |

**Findings:** The type issue is not a real defect. OLD defines `ChatWithMessages = { chat: Chat; messages: MessageRow[] }` in `archive/oldapp/lib/data/base.ts:56-59` where `MessageRow = InferSelectModel<typeof message>` (schema.ts:96). NEW defines `ChatWithMessages = { chat: Chat; messages: Message[] }` in `lib/data/repositories/chat.repository.ts:66-72` and `lib/data/services/chat.service.ts:39-44`, where `Message = InferSelectModel<typeof message>` (lib/db/schema.ts:189). Both `MessageRow` and `Message` resolve to the **exact same Drizzle `InferSelectModel` type** derived from the same table definition (same columns: id, chatId, role, parts, attachments, createdAt). NEW also exports `MessageRow` as an alias at `lib/db/schema.ts:186` for backward compatibility. The table name is identical (`Message_v2`), column types are identical. The interface shape `{ chat: Chat; messages: Message[] }` is structurally equivalent. Code expecting `MessageRow[]` can use `Message[]` interchangeably — they are the same type under different names. **Note:** The `getWithMessages` implementation has real behavioral differences (OLD has dedup, sorting, limiting, cache-first, guest handling vs NEW's simple DB query) but those are implementation concerns tracked by other issues (P5-BRK-005 for guest strategy, P5-FNC-037 for caching), not type issues.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. The OLD `ChatWithMessages` includes `messages: MessageRow[]` and the NEW includes `messages: Message[]` — both resolve to `InferSelectModel<typeof message>` with identical fields (id, chatId, role, parts, attachments, createdAt). The messages array is present in both interfaces. The schema table (`Message_v2`) is identical. `MessageRow` is explicitly aliased in `lib/db/schema.ts:186`. This is purely a rename, no structural difference. Behavioral differences in `getWithMessages` are tracked by P5-BRK-005 and P5-FNC-037.

---

#### [P5-FNC-040] Missing Document Versioning Support

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/data/document.ts`
**NEW File:** `lib/data/repositories/artifact.repository.ts`
**Line Ref:** L180-260, L378-469

**Description:**
OLD `documentData` has versioning support:
- `getAll(documentId, ctx)` - Get all versions of a document
- `deleteAfterTimestamp()` - Delete versions after timestamp (rollback)
- Versions stored as array in cache

NEW `ArtifactRepository` has:
- `findVersions()` - Similar to `getAll`
- `saveVersion()` - Save new version
- Different structure without rollback support

**Impact:**
Document version history and rollback features need adaptation.

**Suggested Fix:**
Add `deleteVersionsAfterTimestamp` method to `ArtifactRepository` for rollback support.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** The issue claims the NEW codebase has "Different structure without rollback support" but this is **incorrect**. The NEW `ArtifactRepository` (`lib/data/repositories/artifact.repository.ts`) provides full versioning support:
- `findAllVersions(artifactId, context)` — equivalent to OLD `documentData.getAll()`, returns all versions chronologically ordered
- `findLatestVersion(artifactId, context)` — returns the most recent version
- `saveVersion(params, context)` — creates a new version with the same artifact ID
- `deleteVersionsAfterTimestamp(artifactId, timestamp, context)` — rollback by deleting versions after a timestamp, INCLUDING suggestion cleanup via `suggestion.artifactCreatedAt`
- `ArtifactService.rollbackToTimestamp()` in `lib/data/services/artifact.service.ts:409` wraps `deleteVersionsAfterTimestamp` for business logic

**Differences from OLD (not defects):** (1) No guest-aware cache-first strategy — NEW goes directly to DB (guest data strategy is a separate issue P5-BRK-005). (2) No explicit DB transaction wrapping — OLD wraps suggestion+artifact deletes in `db.transaction()`, NEW executes them sequentially (minor atomicity concern but not a missing feature). (3) Renamed from `document` to `artifact` following v6 naming convention. All core versioning operations exist and are functional.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. Full comparison of versioning operations: (1) `getAll` → `findAllVersions` at L555-598 (with `orderBy(asc(artifact.createdAt))`) — equivalent. (2) `save` → `doUpdate` at L360-413 (creates new version row with same ID, returns `Artifact`) — equivalent. (3) `deleteAfterTimestamp` → `deleteVersionsAfterTimestamp` at L647-700 (deletes suggestions first via `gt(suggestion.artifactCreatedAt, timestamp)` then artifact versions via `gt(artifact.createdAt, timestamp)` with userId guard, returns deleted records) — equivalent with improved userId IDOR protection. (4) `ArtifactService.rollbackToTimestamp()` at L409 wraps the repository method. Missing transaction wrapping is a minor atomicity concern (if artifact delete fails after suggestion delete, orphaned suggestion deletions occur), but the core `createVersion → getVersions → deleteVersionsAfter → rollback` workflow is fully covered.

---

### Cache Layer (lib/cache/)

#### [P5-FNC-041] Missing ZSET-Based Message Storage

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/cache/types.ts`, `archive/oldapp/lib/cache/operations.ts`
**NEW File:** N/A - Different approach
**Line Ref:** types.ts L17-30, operations.ts L93-106

**Description:**
OLD uses Redis Sorted Sets (ZSET) for message storage:
- `chat:{chatId}:{userId}:msgs` - ZSET with timestamp scores
- O(log N) message append via ZADD
- O(log N + M) range deletion via ZREMRANGEBYSCORE
- Natural time-based ordering

NEW uses simpler key-value approach without ZSET optimization.

**Impact:**
Performance degradation for chats with many messages. Range operations (delete after timestamp) less efficient.

**Suggested Fix:**
Consider implementing ZSET-based message storage in cache strategies.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD (`archive/oldapp/lib/cache/operations.ts:93-560`) implements full ZSET-based message storage: `setChatInCache` uses `pipeline.zadd(msgsKey, { score, member })` for each message, `appendMessageToCache` uses `ZADD` via Lua scripts for O(log N) atomic append, `appendMessagesToCache` for bulk append, `deleteMessagesFromCacheAfterTimestamp` uses `ZREMRANGEBYSCORE` for O(log N + M) range deletion, `getLastMessagesFromCache` uses `ZRANGE` with negative indices for efficient tail fetch, and `getMessageCountFromCache` uses `ZCARD` for O(1) count. NEW `lib/cache/keys.ts` defines ZSET-ready keys (`chatMessagesKey` with JSDoc mentioning "Redis ZSET with timestamp scores") but the actual cache layer (`lib/cache/strategies.ts`, `lib/cache/tiered-cache.ts`) contains ONLY generic key-value operations (`get/set/delete/has`) with no `zadd`, `zrange`, `zremrangebyscore`, or `zcard` calls. The gap means: (1) message append is O(N) instead of O(log N), (2) range deletion requires full data read-modify-write instead of O(log N + M), (3) no efficient message count or tail fetch. This is a genuine architectural simplification that loses the ZSET performance benefits for message-heavy chats.

---

#### [P5-FNC-042] Missing `warmChatCache` and `warmDocumentCache` Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/cache/operations.ts`
**NEW File:** `lib/cache/tiered-cache.ts` (different approach)
**Line Ref:** L600-700

**Description:**
OLD has cache warming functions:
- `warmChatCache(chatId, userId, chat, messages)` - Populates cache after DB fetch
- `warmDocumentCache(documentId, userId, documents)` - Populates document cache

NEW has `warmCache` and `createCacheWarmer` but:
- Generic approach, not entity-specific
- No automatic warming on cache miss

**Impact:**
Cache misses require repeated DB queries until data is cached.

**Suggested Fix:**
Implement entity-specific cache warming in repositories.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD (`archive/oldapp/lib/cache/operations.ts:768-810`) provides entity-specific cache warming: `warmChatCache(chatId, userId, chat: Chat, messages: DBMessage[])` converts domain objects to `CachedChat` via `chatToCache()` (which maps `Chat` + `DBMessage[]` to `CachedChatMeta` + `CachedMessage[]`) then calls `setChatInCache` to populate Redis with ZSET messages and JSON metadata. `warmDocumentCache(documentId, userId, documents: Document[])` converts via `documentsToCache()` to `CachedDocument` with versions array. Both are called from data access layers (`chat.ts:149`, `document.ts:149`) after DB reads for cache-miss scenarios. NEW `lib/cache/tiered-cache.ts` exports `warmCache(cache, keys, fetcher)`, `warmCacheWithEntries(cache, entries)`, and `createCacheWarmer(cache, fetcher)` — all generic key-value utilities that take string keys and generic values. They don't provide: (1) domain-object-to-cache conversion (Chat→CachedChat), (2) entity-specific key generation, (3) ZSET-aware data population. The repositories use `cacheThrough` reads and `invalidateCache` writes, not explicit warming. The cache-miss path relies on `cacheThrough` to lazily populate, which works for simple entities but doesn't support the multi-key ZSET structure (meta + msgs) the OLD system used.

---

#### [P5-FNC-043] Missing `CachedChat`, `CachedMessage`, `CachedDocument` Types

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/cache/types.ts`
**NEW File:** N/A - Not found
**Line Ref:** L33-80

**Description:**
OLD has detailed cache type definitions:
- `CachedChatMeta` - Chat metadata without messages
- `CachedChat` - Full chat with messages
- `CachedMessage` - Message with parts, attachments
- `CachedDocument` - Document with versions array
- `DocumentVersion` - Single document version
- `CacheKeys` - Key generator functions

NEW has `CacheKeys` in `lib/cache/keys.ts` but:
- No cached entity type definitions
- Different key structure

**Impact:**
No type safety for cached data structures.

**Suggested Fix:**
Add cache type definitions to `lib/cache/` or `lib/data/types.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD (`archive/oldapp/lib/cache/types.ts:1-97`) defines 6 entity-specific cache types: `CachedChatMeta` (chat metadata without messages — id, userId, title, visibility, timestamps, lastContext, version), `CachedChat` (extends CachedChatMeta with `messages: CachedMessage[]`), `CachedMessage` (id, chatId, role, parts: MessagePart[], attachments: MessageAttachment[], createdAt), `CachedDocument` (id, userId, chatId, versions: DocumentVersion[]), `DocumentVersion` (title, content, kind, createdAt, updatedAt), `UserChatListItem` (chatId, title, updatedAt for ZSET score). These types enforce structure on cached data and are used throughout `operations.ts` and the data access layer. NEW `lib/cache/` has NO entity-specific type definitions. `keys.ts` defines `CacheKeys` object (key generators only). `tiered-cache.ts` uses generic `TieredCache<T = unknown>`. `strategies.ts` uses generic `Fetcher<T>` and `CacheResult<T>`. `lib/data/types.ts` defines `RepositoryContext` (userId, isGuest) but no cached entity shapes. Grep for `CachedChat|CachedMessage|CachedDocument` in `lib/` returns zero matches outside archive. This means all cached data is untyped (`unknown`), losing type safety for cache operations.

---

### Auth Utilities (lib/auth/)

#### [P5-FNC-044] Different Authentication Provider (Supabase vs NextAuth)

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/auth/session.ts`
**NEW File:** `lib/auth/session.ts`, `lib/auth/index.ts`
**Line Ref:** L1-269 vs L1-200

**Description:**
OLD uses Supabase Auth:
- `getSupabaseSessionFromCookies()` - JWT verification with Supabase secret
- `getSupabaseSessionFromToken(accessToken)` - Parse session from token
- `getSupabaseAccessTokenCookieName()` - Cookie name config
- JWT verification with audience and issuer checks

NEW uses NextAuth:
- `auth()` from NextAuth for session
- Different session structure
- `AppSession` type abstraction layer

**Impact:**
Complete authentication system change. All session handling code needs adaptation.

**Suggested Fix:**
Ensure `AppSession` type provides sufficient abstraction for any auth provider.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed architectural change. OLD (`archive/oldapp/lib/auth/session.ts`) uses Supabase Auth: `getSupabaseSessionFromCookies()` verifies JWT with `SUPABASE_JWT_SECRET` using `jose.jwtVerify()` with audience (`"authenticated"`) and issuer (`SUPABASE_URL/auth/v1`) checks. `getSupabaseSessionFromToken(accessToken)` parses session from raw token. Guest sessions use JWT-signed tokens with `GUEST_JWT_SECRET`. `getAppSession()` checks Supabase → Guest priority.

NEW (`lib/auth/`) uses NextAuth v5: `auth()` from NextAuth for session via `lib/auth/index.ts`. `config.ts` configures Credentials provider with email/password against `user` table directly. Session uses JWT strategy with `NEXTAUTH_SECRET`. Guest sessions use plain `guest:UUID` strings in cookies (no JWT). `getSession()` checks NextAuth → Guest priority.

Key differences: (1) Auth provider: Supabase SSO → NextAuth Credentials (fundamentally different auth flow). (2) JWT verification: Supabase JWT with audience/issuer → NextAuth internal JWT handling. (3) Guest security: JWT-signed tokens → plain cookie strings. (4) Session structure: Both use `AppSession` abstraction with `user.id`, `user.type`, `user.email`. (5) No `@supabase/ssr` or `@supabase/supabase-js` dependencies in NEW. This is a deliberate v6 architectural decision. The `AppSession` type provides adequate provider abstraction.

---

#### [P5-FNC-045] Missing `getSupabaseSessionFromToken` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/auth/session.ts`
**NEW File:** N/A - Not found
**Line Ref:** L100-135

**Description:**
OLD can parse session from a raw token:
- Used when token is immediately available after setting cookie
- Avoids cookie read timing issues in Next.js
- Validates JWT with audience and issuer

**Impact:**
Cannot parse session from token in scenarios where cookie read isn't reliable.

**Suggested Fix:**
Add `getSessionFromToken` function if needed for cookie timing scenarios.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** This function is **Supabase-specific** and not applicable to the NextAuth architecture. OLD `getSupabaseSessionFromToken(accessToken)` (`archive/oldapp/lib/auth/session.ts:100-135`) was used exclusively by the Supabase auth exchange route (`archive/oldapp/app/api/auth/exchange/route.ts:59`) to parse a session from a raw Supabase access token immediately after cookie-setting, avoiding Next.js cookie-read timing issues.

In the NEW NextAuth architecture, token exchange is handled internally by NextAuth's callback mechanism: the `jwt` callback in `lib/auth/config.ts:167-173` adds `user.id` to the token on sign-in, and the `session` callback (L178-183) populates the session from the token. NextAuth manages the entire token lifecycle (creation, storage, verification, rotation) internally. There is no scenario where the NEW code needs to manually parse a session from a raw token string — NextAuth's `auth()` function handles session resolution from cookies/headers automatically. The cookie timing issue is also not applicable since NextAuth uses a different cookie management approach. Grep confirms `getSupabaseSessionFromToken` and `getSessionFromToken` have zero matches in the NEW codebase and no callers need this pattern.

**Re-verification (2026-02-16):** Confirmed False Positive under stricter scrutiny. Verified NextAuth covers all token scenarios: (1) `auth()` in `lib/auth/index.ts` handles session from cookies/headers automatically. (2) `jwt` callback (`lib/auth/config.ts:176-182`) adds `user.id` on sign-in — no manual token parsing needed. (3) `session` callback (L183-188) exposes session fields. (4) The sole OLD consumer (`/api/auth/exchange`) was a Supabase-specific endpoint that does not exist in NEW. (5) No NEW code path requires raw JWT parsing — NextAuth's `authorized` callback in middleware (L143-163) handles route protection. The Supabase→NextAuth migration makes this function architecturally irrelevant with zero usage gap.

---

#### [P5-FNC-046] Missing `createGuestSession` Function

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/auth/session.ts`
**NEW File:** `lib/auth/session.ts` (different implementation)
**Line Ref:** L212-250

**Description:**
OLD `createGuestSession()`:
- Creates JWT-signed guest token
- Uses `GUEST_JWT_SECRET` environment variable
- Sets cookie with `getSecureCookieOptions()`
- Returns `AppSession` with `guest` type

NEW has `createGuestSession()` but implementation differs:
- Uses simpler cookie-based approach
- No JWT signing for guest tokens

**Impact:**
Guest session security model changed. OLD had cryptographically signed guest tokens.

**Suggested Fix:**
Verify guest session security requirements and adjust implementation if needed.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed security model difference. OLD `createGuestSession()` (`archive/oldapp/lib/auth/session.ts:212-250`) uses cryptographic JWT signing: generates `guest:UUID` ID, creates JWT with `sub`, `type: "guest"`, `iat`, `exp` claims, signs with `GUEST_JWT_SECRET` using HS256 via `jose.SignJWT`, uses separate JWT TTL (1 hour via `GUEST_TOKEN_TTL_SECONDS` for security) and cookie TTL (7 days via `GUEST_CACHE_TTL_SECONDS` for UX), enables token rotation via middleware.

NEW `createGuestSession()` (`lib/auth/session.ts:261-280`) uses plain cookie storage: generates `guest:UUID` via `crypto.randomUUID()`, stores the raw string directly in `guest_id` cookie with `httpOnly/secure/sameSite:lax/maxAge:7d` — NO JWT signing, NO expiration within the token, NO rotation mechanism.

Security implications: (1) **Tamper risk** — OLD JWT is integrity-verified on read (`verifyJwt` checks signature); NEW cookie value can be modified by client (change UUID portion) to potentially assume another guest's session. (2) **No token expiration** — OLD JWT expires in 1 hour, forcing re-creation; NEW guest ID persists for 7 days with same value. (3) **No rotation** — OLD supports middleware-based token rotation; NEW has no rotation. (4) **Validation** — OLD `getGuestSessionFromCookies()` verifies JWT signature before trusting ID; NEW only validates the `guest:` prefix format. The core `createGuestSession` function exists and works, but the security downgrade from JWT-signed to plain cookies is a legitimate concern for applications where guest session integrity matters.

---

#### [P5-FNC-047] Missing Supabase Browser Client

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/auth/client.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-23

**Description:**
OLD has `getSupabaseBrowserClient()`:
- Creates Supabase client for browser-side auth
- Singleton pattern to avoid re-initialization
- Uses `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`

NEW uses NextAuth which handles client-side auth differently.

**Impact:**
No impact if NextAuth client-side auth is properly configured.

**Suggested Fix:**
Ensure NextAuth client-side session handling is properly implemented.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T18:30:00Z |
| **Status** | False Positive |

**Findings:** The Supabase browser client is **not needed** because the auth provider changed from Supabase to NextAuth. OLD `getSupabaseBrowserClient()` (`archive/oldapp/lib/auth/client.ts:1-23`) created a `@supabase/ssr` `createBrowserClient` singleton for client-side Supabase auth operations (e.g., `getSupabaseBrowserClient().auth.signOut()` in `archive/oldapp/components/sidebar-user-nav.tsx:136`).

NEW uses NextAuth, which handles client-side auth differently: (1) Server-side session checks via `getSession()` in server components (`app/layout.tsx:148`, `app/(chat)/layout.tsx:43`, `app/(chat)/chat/[id]/page.tsx:76`). (2) NextAuth provides `SessionProvider` and `useSession` from `next-auth/react` for client-side access, and server actions for `signIn`/`signOut`. (3) Route protection via NextAuth's `authorized` callback in middleware (`lib/auth/config.ts:143-163`). (4) No `@supabase/ssr` or `NEXT_PUBLIC_SUPABASE_URL` dependencies exist in NEW.

The issue itself acknowledges "No impact if NextAuth client-side auth is properly configured." NextAuth's client-side capability replaces this Supabase-specific function entirely. There is no functional gap — the auth mechanism changed, and the equivalent client-side patterns exist in the NextAuth ecosystem.

**Re-verified 2026-02-16: Confirmed False Positive.** No real-time subscriptions or client-side DB queries via Supabase exist in the OLD codebase — `getSupabaseBrowserClient()` was used exclusively for `auth.signOut()`. NextAuth fully covers this use case.

---

### Database Layer (lib/db/)

#### [P5-FNC-048] Missing `batch.ts` - Batch Operations Module

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/batch.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-371

**Description:**
The entire `batch.ts` module is missing. This provides efficient batch operations:

**Missing Functions:**
1. `batchInsert(table, records, options)` - Batch insert with automatic chunking
   - Configurable chunk size (default 100, max 1000)
   - Optional continueOnError flag
   - OpenTelemetry span attributes

2. `batchUpdate(table, updates, options)` - Batch update by ID
   - Deduplication by ID (keeps last update)
   - Transaction-wrapped updates
   - Performance tracking

3. `batchDelete(table, ids, options)` - Batch delete by IDs
   - Safety limit (default 10,000)
   - ID deduplication
   - Chunked execution

4. `batchUpsert(table, records, options)` - Batch upsert
   - Insert with conflict ignore
   - Returns inserted records

**Impact:**
No efficient bulk operations. Each operation will require individual queries, causing:
- Increased network overhead
- Poor connection pool utilization
- No atomicity for batch updates

**Suggested Fix:**
Create `lib/db/batch.ts` with all batch operation functions.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `archive/oldapp/lib/db/batch.ts` (371 lines) exports 4 batch operation functions: (1) `batchInsert<T>(table, records, options)` — batch insert with auto-chunking (default 100, max 1000), `continueOnError` flag, OTel span attributes, per-chunk error tracking; (2) `batchUpdate<T>(table, updates, options)` — batch update by ID with deduplication (Map-based, keeps last update per ID), transaction-wrapped chunk execution; (3) `batchDelete(table, ids, options)` — batch delete with safety limit (default 10,000), ID deduplication via `Set`, chunked `inArray` execution; (4) `batchUpsert<T>(table, records, options)` — batch insert with `onConflictDoNothing()`, chunked with `returning()`. All 4 functions include OpenTelemetry span integration (`span.setAttribute`, `span.addEvent`, `span.recordException`). Grep for `batchInsert|batchUpdate|batchDelete|batchUpsert` across `lib/`, `app/`, `features/` returns zero matches — no equivalent exists anywhere in the NEW codebase. The NEW repositories use individual `db.insert().values(records)` calls without chunking, safety limits, or OTel integration. While the OLD batch module was NOT actively imported by OLD data layer code (it uses `db.insert().values(messages)` directly in `chat.ts:1088`), the module provides reusable infrastructure for bulk operations that has no NEW equivalent.

---

#### [P5-FNC-049] Missing `pagination.ts` - Cursor-Based Pagination

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/pagination.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-329

**Description:**
The entire `pagination.ts` module is missing. This provides efficient cursor-based pagination:

**Missing Types:**
- `PaginationDirection` - "forward" | "backward"
- `PaginationOptions<T>` - limit, cursor, direction, sortField, sortOrder
- `CursorPaginatedResult<T>` - data, nextCursor, prevCursor, hasMore, totalCount

**Missing Functions:**
1. `buildCursorQuery(table, options)` - Build cursor condition for query
2. `paginate(query, table, options)` - Execute paginated query
3. `paginateWithCount(query, table, countQuery, options)` - Include total count
4. `createPaginationResponse(result, baseUrl)` - API response helper

**Missing Features:**
- O(1) query complexity vs O(N) for offset
- Consistent results (no missed/duplicate records)
- Encrypted cursors (base64url encoded)
- Bidirectional pagination
- OpenTelemetry integration

**Impact:**
No standardized pagination. API endpoints will need to implement pagination individually, likely using inefficient offset-based approach.

**Suggested Fix:**
Create `lib/db/pagination.ts` with cursor-based pagination utilities.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `archive/oldapp/lib/db/pagination.ts` (329 lines) provides a complete cursor-based pagination framework: `PaginationDirection` type (`"forward" | "backward"`), `PaginationOptions<T>` (limit, cursor, direction, sortField, sortOrder), `CursorPaginatedResult<T>` (data, nextCursor, prevCursor, hasMore, totalCount), `CursorCodec` (base64url encode/decode for obfuscated cursors), `buildCursorQuery<T>()` (builds SQL cursor condition with direction-aware `gt`/`lt` operators), `paginate<T>()` (executes paginated query with limit+1 fetch for hasMore detection, bi-directional cursor generation), `paginateWithCount<T>()` (parallel count query), and `createPaginationResponse<T>()` (API response with `links.next`/`links.prev` URLs). All functions include OTel span integration. NEW `lib/data/types.ts` defines a minimal `PaginationParams` (limit, startingAfter?, endingBefore?) and `PaginatedResult<T>` (items, hasMore) — but these are **type definitions only** with no implementation. The NEW `ChatRepository.findByUserId()` uses simple `offset`-based pagination via `limit(params.limit + 1)` and `where(lt(chat.createdAt, cursor))` — a single-use inline implementation, not a reusable module. No `CursorCodec`, no `buildCursorQuery`, no `paginate()`, no `createPaginationResponse` exist in the NEW codebase. The NEW `lib/api/response.ts` has a `paginated()` helper but it constructs response objects — it doesn't execute queries. Verified as a genuine missing module.

---

#### [P5-FNC-050] Missing `transactions.ts` - Transaction Wrapper

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/transactions.ts`
**NEW File:** `lib/db/client.ts` (simpler version)
**Line Ref:** L1-107

**Description:**
OLD has a sophisticated transaction wrapper:

**OLD Features:**
1. `withTransaction(fn, operation)` - Wrapped transaction with:
   - Automatic rollback on errors
   - OpenTelemetry span attributes
   - Performance tracking
   - Slow transaction warning (>500ms)
   - Proper error conversion via `toDatabaseError`

2. `withSequentialTransactions(operations)` - Sequential execution
   - Runs transactions one at a time
   - Avoids potential conflicts
   - Returns array of results

**NEW Features:**
- `withTransaction(fn)` - Simple wrapper without:
  - Operation name for logging
  - Performance tracking
  - Slow transaction warnings
  - OpenTelemetry integration

**Impact:**
No performance monitoring for transactions. No warning for slow transactions.

**Suggested Fix:**
Enhance `withTransaction` in `lib/db/client.ts` with operation name, performance tracking, and slow transaction warnings.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed. Both OLD and NEW have `withTransaction`, but the NEW version is significantly stripped down. OLD `archive/oldapp/lib/db/transactions.ts:37-88` provides: (1) `operation` parameter for named logging/tracing, (2) OTel span attributes (`db.transaction`, `db.operation`, `db.transaction.duration_ms`, `db.transaction.success`), (3) `performance.now()` timing, (4) slow transaction warning at 500ms (`logWarn`), (5) success info logging, (6) error conversion via `toDatabaseError()` with operation name in cause string, (7) `span.recordException()` on failure. Also exports `withSequentialTransactions<T>(operations)` that runs named transactions sequentially to avoid conflicts. NEW `lib/db/client.ts:192-196` is a single-line wrapper: `return db.transaction(fn)` — no operation name parameter, no performance tracking, no slow transaction warnings, no OTel integration, no `toDatabaseError` wrapping, no `withSequentialTransactions`. The NEW version is actively used (`lib/data/services/chat.service.ts:283,383,454` imports `withTransaction` from `@/lib/db/client`), confirming it's the production implementation. The missing observability features (OTel spans, slow query logging) make transaction debugging significantly harder in production.

---

#### [P5-FNC-051] Missing `queries.ts` - Direct Query Functions

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/queries.ts`
**NEW File:** N/A - Moved to repositories
**Line Ref:** L1-215

**Description:**
OLD has direct query functions in `queries.ts`:

**OLD Functions:**
1. `getUser(email)` - Get user by email
2. `getUserById(id)` - Get user by ID
3. `getMessageById({ id })` - Get message by ID
4. `voteMessage({ chatId, messageId, type, userId })` - Upsert vote
5. `getVotesByChatId({ id })` - Get votes for chat
6. `getVotesByChatIdAndUserId({ chatId, userId })` - Get user's votes
7. `saveSuggestions({ suggestions })` - Batch insert suggestions
8. `getSuggestionsByDocumentId({ documentId, userId })` - Get suggestions with IDOR protection

**NEW Approach:**
- Queries moved to repositories in `lib/data/repositories/`
- No direct query exports from `lib/db/`

**Impact:**
Code expecting direct query functions from `lib/db/queries` will fail. Need to import from repositories instead.

**Suggested Fix:**
Add backward-compatible re-exports from `lib/db/index.ts` pointing to repository functions, or update all import sites.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | False Positive |

**Findings:** The issue describes a deliberate architectural refactoring, not a missing module. OLD `archive/oldapp/lib/db/queries.ts` (215 lines) is a monolithic query file exporting 8 functions: `getUser`, `getUserById`, `getMessageById`, `voteMessage`, `getVotesByChatId`, `getVotesByChatIdAndUserId`, `saveSuggestions`, `getSuggestionsByDocumentId`. It also doubles as the DB client module (exports `db` via `drizzle(client)`). All 8 query functions have been migrated to the v6 Repository + Service pattern: (1) `getUser → UserRepository.findByEmail()` (`lib/data/repositories/user.repository.ts:316`), (2) `getUserById → UserRepository.doFindById()` (L116) + `AuthService.getUserById()` (`lib/data/services/auth.service.ts:237`), (3) `getMessageById → MessageRepository.doFindById()` (`lib/data/repositories/message.repository.ts`), (4) `voteMessage → VoteRepository` upsert + `ChatService.voteMessage()` (`lib/data/services/chat.service.ts:571`), (5) `getVotesByChatId → VoteRepository.findByChatId()` (`lib/data/repositories/vote.repository.ts`), (6) `getVotesByChatIdAndUserId → VoteRepository.findByIds()` (L494), (7) `saveSuggestions → SuggestionRepository.saveMany()` (`lib/data/repositories/suggestion.repository.ts`), (8) `getSuggestionsByDocumentId → SuggestionRepository.findByArtifactId()`. The DB client is now in `lib/db/client.ts`. No NEW code imports from `lib/db/queries` — all consumers use the repository/service layer. The issue's claim "code expecting direct query functions from `lib/db/queries` will fail" is accurate for OLD code but irrelevant because no NEW code uses the OLD import path. This is an intentional v6 architecture change following the Repository pattern, not a defect.

**Re-verified 2026-02-16: Confirmed False Positive.** All 8 OLD query functions verified with Repository equivalents: `getUser`→`UserRepository.findByEmail()`, `getUserById`→`UserRepository.doFindById()`, `getMessageById`→`MessageRepository.doFindById()`, `voteMessage`→`VoteRepository.upsert()`, `getVotesByChatId`→`VoteRepository.findByChatId()`, `getVotesByChatIdAndUserId`→`VoteRepository.doFindMany()`, `saveSuggestions`→`SuggestionRepository.doCreate()`, `getSuggestionsByDocumentId`→`SuggestionRepository.findByArtifactId()`. No functions lost in migration.

---

#### [P5-FNC-052] Missing `helpers/` Directory - DB Helpers

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/helpers/01-core-to-parts.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-250

**Description:**
OLD has a helpers directory with message part conversion:

**Missing:**
- `coreToParts(coreMessage)` - Convert core message to parts array
- Handles various message content types
- Used for message serialization

**Impact:**
Minor - if message format handling is implemented elsewhere.

**Suggested Fix:**
Verify message part conversion is handled in repositories or create `lib/db/helpers/` if needed.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | False Positive |

**Findings:** The OLD `archive/oldapp/lib/db/helpers/01-core-to-parts.ts` is **entirely commented out** — every single line (250 lines) is prefixed with `//`. The file is a legacy migration helper for converting old message format (`MessageDeprecated` with `content: ContentPart[]`) to new message format (`Message_v2` with `parts: UIMessagePart[]`). It was a one-time data migration script (calls `migrateMessages()` → `process.exit(0)`) that: batch-processes chats, sorts messages by time and role rank, groups into user-assistant sections, calls `appendResponseMessages()` from `ai` SDK, deduplicates parts, sanitizes reasoning parts, and batch-inserts converted messages+votes. Since the file is entirely commented out in the OLD codebase itself: (1) it was not production code, (2) it was a one-time migration utility for transitioning from the deprecated message schema, (3) there is no runtime functionality to migrate. The NEW equivalent for schema migration is in `drizzle/seed.ts` and the migrations directory. No functional gap exists from the absence of this commented-out migration script.

**Re-verified 2026-02-16: Confirmed False Positive.** File verified as 218 lines of entirely commented-out code (every line prefixed with `//`). Contains functions `getMessageRank`, `dedupeParts`, `sanitizeParts`, `migrateMessages` — all commented out. Dead code, no functional loss.

---

#### [P5-FNC-053] Schema lastContext Type Change

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/schema.ts`
**NEW File:** `lib/db/schema.ts`
**Line Ref:** L53 vs L114

**Description:**
OLD: `lastContext: jsonb("last_context").$type<AppUsage | null>()`
NEW: `lastContext: jsonb("last_context").$type<unknown | null>()`

The type changed from `AppUsage` to `unknown`. This is likely intentional as `AppUsage` type was in `lib/usage.ts` which may not exist in NEW.

**Impact:**
Loss of type safety for `lastContext` field. Type casting required when accessing.

**Suggested Fix:**
Define `AppUsage` type in NEW or document the intentional type relaxation.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed type regression. OLD `archive/oldapp/lib/db/schema.ts:53` defines `lastContext: jsonb("last_context").$type<AppUsage | null>()` where `AppUsage` is imported from `../usage` and resolves to `LanguageModelUsage & UsageData & { modelId?: string }` — a rich intersection type with AI SDK token fields (`promptTokens`, `completionTokens`, `totalTokens`) and tokenlens cost data. NEW `lib/db/schema.ts:114` defines `lastContext: jsonb("last_context").$type<unknown | null>()`. The JSDoc comment says "typed as AppUsage | null" but the actual type is `unknown | null`, which is effectively `unknown`. The NEW `features/chat/types.ts:179-188` defines a standalone `AppUsage` interface (`totalTokens?, promptTokens?, completionTokens?, model?`) — but this type is NOT imported by the schema file. Any code reading `chat.lastContext` from the DB must cast from `unknown` to `AppUsage` manually, losing compile-time type safety. For example, `features/chat/components/chat.tsx:37` accepts `initialLastContext?: AppUsage` but the schema type doesn't enforce this constraint at the data layer. The fix is straightforward: import `AppUsage` from `features/chat/types` (or define it in `lib/types/`) and use `.$type<AppUsage | null>()` in the schema.

---

#### [P5-FNC-054] Missing `migrations/` Directory Structure

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/db/migrations/`
**NEW File:** `drizzle/` (different location)
**Line Ref:** All files

**Description:**
OLD has migrations in `lib/db/migrations/`:
- `0000_glorious_sandman.sql` through `0003_supreme_joshua_kane.sql`
- `meta/` directory with snapshots and journal
- `old_db_migration/` subdirectory with legacy migrations

NEW has migrations in `drizzle/` directory (standard Drizzle location).

**Impact:**
Migration path changed. `migrate.ts` updated to use `./drizzle/migrations`.

**Suggested Fix:**
No fix needed - this is architectural refactoring that preserves functionality.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | False Positive |

**Findings:** This is a standard Drizzle ORM convention change, not a defect. OLD `archive/oldapp/lib/db/migrations/` contains 4 migration files (`0000_glorious_sandman.sql` through `0003_supreme_joshua_kane.sql`), a `meta/` directory with snapshots and journal, and an `old_db_migration/` subdirectory with legacy migration scripts. NEW migrations are in `drizzle/migrations/` (1 migration `0000_material_sway.sql`, plus `meta/`). The path change is correctly configured: `drizzle.config.ts:10` sets `out: "./drizzle/migrations"` and `lib/db/migrate.ts:47` uses `migrationsFolder: "./drizzle/migrations"`. The standard Drizzle ORM convention is to use a top-level `drizzle/` directory. The OLD path (`lib/db/migrations/`) was non-standard. The NEW setup has fewer migrations because the v6 schema was generated fresh (single `0000_material_sway.sql` for the entire schema including `Artifact` table renaming). No migration history was lost — the OLD migrations are preserved in the archive. Both the migration runner and the config are properly aligned. No fix needed.

**Re-verified 2026-02-16: Confirmed False Positive.** Both migration directories are properly configured. `drizzle.config.ts:10` → `out: "./drizzle/migrations"`, `lib/db/migrate.ts:47` → `migrationsFolder: "./drizzle/migrations"`. OLD migration history preserved in archive. No files lost.

---

### Middleware Layer (lib/middleware/)

#### [P5-FNC-055] Missing `deduplication.ts` - Request Deduplication

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/deduplication.ts`
**NEW File:** N/A - Not found
**Line Ref:** L1-386

**Description:**
The entire `deduplication.ts` module is missing. This prevents duplicate request processing:

**Missing Types:**
- `DeduplicationConfig` - key, windowSeconds, cacheResponse, metadata
- `DeduplicationResult<T>` - isFirst, isDuplicate, cachedResponse, expiresIn

**Missing Class:**
- `RequestDeduplicator` with:
  - `checkDuplication(config)` - Check if request is duplicate
  - `storeResponse(key, response, windowSeconds)` - Cache response
  - `registerInFlight(key, promise)` - Track in-flight requests
  - `clear(key)` - Clear deduplication state

**Missing Functions:**
1. `deduplicateRequest(config, handler)` - Auto-dedup with caching
2. `generateRequestFingerprint(method, url, body, userId)` - Request key
3. `withDeduplication(config, handler)` - Middleware wrapper

**Missing Presets:**
- `DeduplicationPresets.short` (2s)
- `DeduplicationPresets.standard` (5s)
- `DeduplicationPresets.long` (30s)
- `DeduplicationPresets.idempotent` (60s)

**Impact:**
- No protection against double-submit on slow networks
- No race condition prevention for concurrent requests
- Wasted resources processing duplicate requests

**Suggested Fix:**
Create `lib/middleware/deduplication.ts` with request deduplication functionality.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `archive/oldapp/lib/middleware/deduplication.ts` (386 lines) provides a complete request deduplication system: `DeduplicationConfig` type (key, windowSeconds, cacheResponse, metadata), `DeduplicationResult<T>` type (isFirst, isDuplicate, cachedResponse, expiresIn), `RequestDeduplicator` class (singleton) with: (1) `checkDuplication<T>(config)` — checks in-flight requests (same-process Map) first, then Redis (`dedup:${key}` keys with TTL), marks new requests as in-progress; (2) `storeResponse<T>(key, response, windowSeconds)` — caches response for duplicate requesters; (3) `registerInFlight(key, promise)` — tracks in-flight with auto-cleanup on `.finally()`; (4) `clear(key)` — clears dedup state on error. Convenience functions: `deduplicateRequest<T>(config, handler)` — auto-dedup with response caching and error cleanup; `generateRequestFingerprint(method, url, body?, userId?)` — deterministic key from request properties; `withDeduplication<T>(config, handler)` — middleware wrapper. Presets: `short` (2s), `standard` (5s), `long` (30s), `idempotent` (60s). All with OTel span integration. NEW `lib/middleware/` contains only: `auth.ts`, `compose.ts`, `index.ts`, `rate-limit.ts` — no deduplication file. Grep for `dedup|deduplicat` in `lib/` (non-archive) returns zero meaningful matches (only `cn.ts` mentioning CSS class deduplication). No request-level dedup exists. The `middleware.ts` root file applies rate limiting and auth but no deduplication. This means double-submit on slow networks, concurrent identical requests, and non-idempotent mutation retries have no protection. Verified as a genuine missing module.

---

#### [P5-FNC-056] Missing `edge-rate-limit.ts` - Edge-Compatible Rate Limiting

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/edge-rate-limit.ts`
**NEW File:** `lib/rate-limit/rate-limiter.ts` (different approach)
**Line Ref:** L1-191

**Description:**
OLD has edge-compatible rate limiting for Next.js Edge Runtime:

**OLD Features:**
1. `checkEdgeRateLimit(options)` - Edge runtime compatible
   - Uses Upstash Redis HTTP API directly
   - No server-only imports
   - `failClosed` option for auth endpoints

2. `EdgeRateLimiters` presets:
   - `api(identifier)` - 100/60s, fail open
   - `strict(identifier)` - 10/60s, fail open
   - `auth(identifier)` - 20/60s, fail closed

3. `getClientIP(request)` - Extract client IP from headers

**NEW Approach:**
- Rate limiting in `lib/rate-limit/` uses `@upstash/ratelimit`
- Same underlying library but different API
- `RateLimiter` class with `consumeToken()` method

**Impact:**
Different API for rate limiting. Code using `EdgeRateLimiters` pattern needs update.

**Suggested Fix:**
Verify `lib/rate-limit/` works in Edge runtime. Add edge-compatible presets if needed.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | False Positive |

**Findings:** The edge rate limiting functionality is NOT missing — it has been consolidated into `lib/rate-limit/rate-limiter.ts`, which is directly used by the Edge middleware (`middleware.ts:19-22` imports `apiLimiter, authLimiter, chatLimiter, uploadLimiter` from `@/lib/rate-limit`). The OLD had a separate `edge-rate-limit.ts` that created its own Redis client via `getEdgeRedis()` using `@upstash/redis` HTTP API. The NEW `rate-limiter.ts` uses the same `@upstash/ratelimit` library (Edge-compatible) via `getRedisClient()` from `lib/cache/client.ts`. The `middleware.ts` `applyRateLimit()` function (L186-202) applies route-specific limiters via `getLimiterForRoute()` (L208-227) calling `.consumeToken(identifier)` — functionally equivalent to OLD's `EdgeRateLimiters.api/strict/auth(identifier)`. The `failClosed` option exists in NEW (`authLimiter` uses `failClosed: true` in `limits.ts:47`). The `getClientIP()` function exists in both OLD (`edge-rate-limit.ts:170-183`) and NEW (`rate-limiter.ts:282-295`). Note: `lib/cache/client.ts:10` has `import "server-only"` which prevents browser imports but does NOT block Edge Runtime (Edge is server-side). The API difference (class-based vs function-based) is tracked separately by P5-FNC-060.

**Re-verified 2026-02-16: Confirmed False Positive.** Edge runtime support verified: (1) `@upstash/ratelimit` uses HTTP-based REST API, natively Edge-compatible; (2) `server-only` import in `lib/cache/client.ts` blocks browser imports only, NOT Edge Runtime (Edge is server-side); (3) `middleware.ts` actively imports and uses these rate limiters in Edge context; (4) `getClientIP()` preserved; (5) `failClosed` option preserved. All Edge-specific behaviors are maintained in the consolidated module.

---

#### [P5-FNC-057] Missing `rate-limit-config.ts` - Centralized Rate Limit Config

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/rate-limit-config.ts`
**NEW File:** `lib/constants.ts` (different location)
**Line Ref:** L1-127

**Description:**
OLD has centralized rate limit configuration:

**OLD Constants:**
```typescript
RATE_LIMITS = {
  EDGE_API: { limit: 100, window: 60, prefix: "edge:api" },
  EDGE_STRICT: { limit: 10, window: 60, prefix: "edge:strict" },
  EDGE_AUTH: { limit: 20, window: 60, prefix: "edge:auth" },
  STRICT: { limit: 10, window: 60, namespace: "strict" },
  STANDARD: { limit: 100, window: 60, namespace: "standard" },
  GENEROUS: { limit: 1000, window: 60, namespace: "generous" },
  CHAT: { limit: 50, window: 60, namespace: "chat" },
  UPLOAD: { limit: 10, window: 3600, namespace: "upload" },
  AUTH_EXCHANGE: { limit: 10, window: 60, namespace: "auth_exchange" },
  AUTH_GUEST: { limit: 20, window: 60, namespace: "guest_auth" },
}
```

**NEW Constants:**
In `lib/constants.ts`:
```typescript
RATE_LIMITS = {
  chat: { requests: 60, window: 60 },
  auth: { requests: 10, window: 60 },
  upload: { requests: 20, window: 60 },
  api: { requests: 100, window: 60 },
}
```

**Impact:**
Different structure and some limits changed:
- Chat: 50 -> 60 requests
- Upload: 10/3600 -> 20/60 (hourly -> per minute!)
- Missing: EDGE_API, EDGE_STRICT, EDGE_AUTH, STRICT, STANDARD, GENEROUS, AUTH_EXCHANGE, AUTH_GUEST

**Suggested Fix:**
Add missing rate limit presets to `lib/constants.ts` and verify upload limit change is intentional.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Improvement |

**Findings:** The OLD `rate-limit-config.ts` has 10 presets; the NEW `lib/constants.ts:65-87` has 4 presets. The reduction is intentional and architecturally justified: (1) EDGE_* presets (EDGE_API, EDGE_STRICT, EDGE_AUTH) are unnecessary because the NEW consolidates edge and server rate limiting into one module — `middleware.ts` uses `chatLimiter/authLimiter/uploadLimiter/apiLimiter` directly from `lib/rate-limit`. (2) STANDARD (100/60s) is identical to NEW `api` (100/60s). (3) AUTH_EXCHANGE is Supabase-specific — no token exchange endpoint exists in the NextAuth architecture. (4) AUTH_GUEST is covered by `authLimiter` applied to `/api/auth/guest` routes in `middleware.ts:218-221`. Missing without equivalent: STRICT (10/60s for non-auth destructive operations) and GENEROUS (1000/60s for high-volume endpoints) — these are convenience presets trivially creatable via `createRateLimiter()` but have no current consumers in the NEW code. The value changes (chat 50→60, upload 10/3600→20/60) are tracked separately by P5-FNC-062. The structural change from a separate config file to inline constants in `lib/constants.ts` is an organizational simplification. Marking as **Improvement** — the consolidation simplifies configuration while two convenience presets were dropped with no active consumers.

---

#### [P5-FNC-058] Missing `rate-limit.ts` - Full Rate Limiting Module

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/rate-limit.ts`
**NEW File:** `lib/middleware/rate-limit.ts` (simpler), `lib/rate-limit/` (separate module)
**Line Ref:** L1-475

**Description:**
OLD has comprehensive rate limiting with multiple algorithms:

**OLD Algorithms:**
1. **Token Bucket** (`TokenBucketLimiter`)
   - Allows bursts while maintaining average rate
   - Refills tokens based on time elapsed
   - Best for chat/AI endpoints

2. **Sliding Window** (`SlidingWindowLimiter`)
   - Precise rate limiting using sorted sets
   - Lua script for atomicity
   - Best for general API

3. **Fixed Window** (`FixedWindowLimiter`)
   - Simple counter-based
   - Efficient but can allow bursts at window edges

**OLD Functions:**
- `checkRateLimit(config)` - Check with strategy selection
- `createRateLimiter(config)` - Middleware factory

**OLD Presets:**
- `RateLimiters.strict(identifier)` - 10/60s
- `RateLimiters.standard(identifier)` - 100/60s
- `RateLimiters.generous(identifier)` - 1000/60s
- `RateLimiters.chat(userId)` - 50/60s (token bucket)
- `RateLimiters.upload(userId)` - 10/3600s (fixed window)

**NEW Approach:**
- Uses `@upstash/ratelimit` library
- Only sliding window algorithm
- Simpler `RateLimiter` class

**Impact:**
- No token bucket for burst handling in chat
- No fixed window for uploads
- Less flexibility in rate limiting strategies

**Suggested Fix:**
Add token bucket algorithm support to `lib/rate-limit/` for chat endpoints, or document architectural decision to use single algorithm.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD `archive/oldapp/lib/middleware/rate-limit.ts` (475 lines) implements 3 rate limiting algorithms: (1) `TokenBucketLimiter` class (L68-133) — allows bursts via token refill based on elapsed time, used for chat endpoints (`RateLimiters.chat` uses `strategy: "token_bucket"`); (2) `SlidingWindowLimiter` class (L139-218) with custom Lua script for atomic ZREMRANGEBYSCORE + ZCARD + ZADD operations; (3) `FixedWindowLimiter` class (L223-275) — simple counter-based with window alignment, used for uploads (`RateLimiters.upload` uses `strategy: "fixed_window"` with 3600s window). All three have OpenTelemetry integration via `trace.getActiveSpan()` for span attributes (L305-316). NEW `lib/rate-limit/rate-limiter.ts` (337 lines) uses ONLY `Ratelimit.slidingWindow()` from `@upstash/ratelimit` — no token bucket, no fixed window, no strategy selection, no OTel tracing. The token bucket loss matters for chat: it allows users to send burst messages (e.g., rapid follow-ups) without being rate-limited, as long as their average rate stays under the limit — sliding window lacks this burst tolerance. The fixed window loss matters for uploads: OLD enforced a strict 10/hour bucket that reset at window boundaries — NEW's 20/minute sliding window is fundamentally different behavior (tracked by P5-FNC-062). The loss of `checkRateLimit(config)` with strategy selection reduces flexibility for future endpoints needing different algorithms. This is a genuine functional regression in rate limiting sophistication.

---

### Root Middleware (middleware.ts)

#### [P5-FNC-059] No Root Middleware in OLD Archive

**Severity:** Low
**Status:** Open
**OLD File:** N/A - Not found in archive/oldapp/
**NEW File:** `middleware.ts`
**Line Ref:** L1-286

**Description:**
OLD archive does not have a root `middleware.ts` file. The NEW codebase has a comprehensive root middleware:

**NEW Features:**
1. Route classification helpers:
   - `isAuthCallbackRoute(pathname)` - Skip NextAuth routes
   - `isHealthRoute(pathname)` - Skip health checks
   - `isProtectedPageRoute(pathname)` - Chat pages require auth
   - `isAuthPageRoute(pathname)` - Redirect logged-in users

2. Rate limiting:
   - `applyRateLimit(pathname, identifier)` - Route-specific limiters
   - `getLimiterForRoute(pathname)` - Select limiter by route
   - `createRateLimitResponse(result)` - 429 response builder

3. User context:
   - `setUserContextHeaders(response, userId, isAuthenticated)` - Headers for downstream
   - `getGuestId(request)` - Guest ID from cookies or generate

**Impact:**
This is a NEW feature, not a migration issue. The root middleware provides centralized request handling that wasn't present in OLD.

**Suggested Fix:**
No fix needed - this is an improvement in NEW.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | False Positive |

**Findings:** Confirmed as stated — `archive/oldapp/` directory listing shows no `middleware.ts` at the project root. The OLD handled edge/middleware concerns differently: `lib/middleware/edge-rate-limit.ts` was a utility module imported by route handlers or a minimal middleware that is not present in the archive. The NEW `middleware.ts` (286 lines) is a comprehensive improvement centralizing: route classification (`isAuthCallbackRoute`, `isHealthRoute`, `isProtectedPageRoute`, `isAuthPageRoute`), rate limiting (`applyRateLimit` dispatching to `getLimiterForRoute` → `chatLimiter/authLimiter/uploadLimiter/apiLimiter`), auth session checking via `auth()` from NextAuth, protected route redirects, login-redirect for already-authenticated users, user context header injection (`x-user-id`, `x-is-authenticated`), and guest ID management. The issue itself correctly states "This is a NEW feature, not a migration issue" and "No fix needed." No missing functionality — this is an enhancement in the NEW architecture.

**Re-verified 2026-02-16: Confirmed False Positive.** OLD archive has no root `middleware.ts` — this is unambiguously a NEW feature addition, not a migration loss. Cannot be "missing" if it never existed.

---

### Rate Limit Module (lib/rate-limit/)

#### [P5-FNC-060] Different Rate Limiter API

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/rate-limit.ts`
**NEW File:** `lib/rate-limit/rate-limiter.ts`
**Line Ref:** L1-475 vs L1-337

**Description:**
The rate limiter API has changed significantly:

**OLD API:**
```typescript
await checkRateLimit({
  strategy: "sliding_window", // or "token_bucket", "fixed_window"
  limit: 100,
  window: 60,
  identifier: "user:123",
  namespace: "api",
  errorMessage: "Custom message"
});
// Returns: { allowed, remaining, limit, resetIn, retryAfter }
```

**NEW API:**
```typescript
const limiter = createRateLimiter({
  limit: 100,
  window: 60,
  prefix: "ratelimit:api"
});
const result = await limiter.consumeToken("user:123");
// Returns: { success, limit, remaining, reset }
```

**Key Differences:**
1. OLD returns `allowed`, NEW returns `success`
2. OLD returns `resetIn` (seconds), NEW returns `reset` (timestamp ms)
3. OLD has `retryAfter`, NEW calculates via `getRetryAfter(reset)`
4. OLD has strategy selection, NEW only sliding window
5. NEW has `checkLimit()` (no consume), `resetLimit()`, `getRemaining()`
6. NEW has `failClosed` option in constructor

**Impact:**
Code using OLD API needs to be updated to use NEW class-based approach.

**Suggested Fix:**
Add backward-compatible `checkRateLimit` function that wraps `RateLimiter`, or update all call sites.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Improvement |

**Findings:** The API change is a deliberate, architecturally superior redesign. OLD: `checkRateLimit({strategy, limit, window, identifier, namespace, errorMessage}) → {allowed, remaining, limit, resetIn, retryAfter}` — imperative function with inline config. NEW: `const limiter = createRateLimiter(config); limiter.consumeToken(key) → {success, limit, remaining, reset}` — class-based with reusable instances and instance methods (`checkLimit()` for non-consuming checks, `resetLimit()` for admin operations, `getRemaining()`). Field renames: `allowed` → `success` (standard convention), `resetIn` (seconds) → `reset` (unix timestamp ms — directly usable for `X-RateLimit-Reset` HTTP header without conversion), `retryAfter` → `getRetryAfter(reset)` utility function. `namespace` → `prefix` aligns with `@upstash/ratelimit` conventions. NEW additions: `failClosed` option per-limiter instance, `createRateLimitHeaders(result)` utility for HTTP headers, `isRateLimitAvailable()` health check. Grep confirms zero imports of `checkRateLimit` or `RateLimiters.` in the NEW codebase outside `archive/` — no backward-compatibility concern. The only genuine regression — loss of strategy selection — is tracked by P5-FNC-058. All NEW code uses the class-based API consistently (`middleware.ts`, `limits.ts`, server actions). Marking as **Improvement**.

---

#### [P5-FNC-061] Missing `strict` and `generous` Rate Limiters

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/rate-limit.ts`
**NEW File:** `lib/rate-limit/limits.ts`
**Line Ref:** L428-454 vs L23-67

**Description:**
OLD has `strict` and `generous` limiter presets:
- `RateLimiters.strict` - 10/60s for destructive operations
- `RateLimiters.generous` - 1000/60s for high-volume endpoints

NEW only has:
- `chatLimiter` - 60/60s
- `authLimiter` - 10/60s
- `uploadLimiter` - 20/60s
- `apiLimiter` - 100/60s

**Impact:**
No pre-configured limiters for strict (10/min) or generous (1000/min) use cases.

**Suggested Fix:**
Add `strictLimiter` and `generousLimiter` to `lib/rate-limit/limits.ts`.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed missing. OLD `RateLimiters.strict` (`archive/oldapp/lib/middleware/rate-limit.ts:424-431`) provides 10/60s sliding window for "destructive or sensitive operations." OLD `RateLimiters.generous` (L435-442) provides 1000/60s for "high-volume endpoints." NEW `lib/rate-limit/limits.ts` exports 4 presets: `chatLimiter` (60/60s), `authLimiter` (10/60s with `failClosed: true`), `uploadLimiter` (20/60s), `apiLimiter` (100/60s). While `authLimiter` (10/60s) numerically matches `strict`'s rate, it is semantically different — configured with `failClosed: true` specifically for authentication endpoints, inappropriate for general destructive operations (delete chat, modify settings) where failing open is acceptable. No equivalent to `generous` (1000/60s) exists for high-volume endpoints (search, autocomplete, polling). The gap means: (1) non-auth destructive endpoints must either use `apiLimiter` (100/60s, too lenient for destructive ops) or create ad-hoc limiters per call site; (2) high-throughput read endpoints have no pre-configured generous limiter. While both can be trivially created via `createRateLimiter()`, the absence of pre-configured instances reduces consistency and developer convenience across the codebase.

---

#### [P5-FNC-062] Upload Rate Limit Window Changed

**Severity:** High
**Status:** Open
**OLD File:** `archive/oldapp/lib/middleware/rate-limit-config.ts`
**NEW File:** `lib/constants.ts`
**Line Ref:** L88-94 vs L35-38

**Description:**
Upload rate limit window changed dramatically:

**OLD:**
```typescript
UPLOAD: {
  limit: 10,
  window: 3600, // 1 hour
  namespace: "upload",
}
```

**NEW:**
```typescript
upload: {
  requests: 20,
  window: 60, // 1 minute
}
```

**Impact:**
- OLD: 10 uploads per hour (very strict)
- NEW: 20 uploads per minute (much more lenient)

This is a 12x increase in allowed uploads (from 10/hour to 1200/hour potential).

**Suggested Fix:**
Verify this change is intentional. If upload abuse prevention is important, consider reverting to hourly window.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Verified |

**Findings:** Confirmed significant change. OLD (`archive/oldapp/lib/middleware/rate-limit-config.ts:88-94`): `UPLOAD: { limit: 10, window: 3600, namespace: "upload" }` — 10 uploads per hour using `fixed_window` strategy. NEW (`lib/constants.ts:79-82`): `upload: { requests: 20, window: 60 }` — 20 uploads per minute using sliding window. The effective rate change: OLD = 10/hour max; NEW = 20/minute = up to 1,200/hour potential — a **120x increase** in allowed upload throughput. Additionally, the algorithm changed from `fixed_window` (OLD, simpler Redis ops, hard hourly boundary) to `sliding_window` (NEW, smoother distribution but higher per-minute burst). APM Memory logs do not document this as an intentional decision. Combined with the file size increase from 5MB → 10MB (tracked by P5-FNC-017), the upload abuse surface is significantly enlarged: OLD maximum = 50MB/hour (10 files × 5MB); NEW theoretical maximum = 24GB/hour (1,200 files × 20MB window × 10MB limit). Whether this is acceptable depends on downstream controls (storage quotas, CDN limits), but the rate limit change alone is a legitimate concern that should be explicitly confirmed as intentional or reverted.

---

### Part 5: Remaining Utilities

#### [P5-FNC-063] request-context.ts: No issues found - functionally equivalent

**Severity:** N/A
**Status:** Resolved
**OLD File:** `archive/oldapp/lib/request-context.ts`
**NEW File:** `lib/api/context.ts`
**Line Ref:** Full file

**Description:**
NEW `lib/api/context.ts` is a superset of OLD `request-context.ts` with all functionality preserved and enhanced:

**Preserved Functions:**
- `generateRequestId()` - UUID generation
- `getRequestContext()` - AsyncLocalStorage access
- `getRequestId()` - Get current request ID

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T22:30:00Z |
| **Status** | Verified (Already Resolved) |

**Findings:** Confirmed as originally resolved. OLD `archive/oldapp/lib/request-context.ts` (147 lines) provides: `RequestContext` type (requestId, userId, startTime, path, method), `AsyncLocalStorage` instance, `generateRequestId()`, `getRequestContext()`, `getRequestId()`, `runWithRequestContext()`, `runWithRequestContextAsync()`, `updateRequestContext()`, `getRequestDuration()`, `createContextFromHeaders()`, `formatRequestContext()`. Has `import "server-only"`. NEW `lib/api/context.ts` (378 lines) is a strict superset containing ALL OLD functions plus: additional context fields (`isGuest`, `clientIp`, `userAgent`), `ContextOptions` and `ApiContext` interfaces, `getOrCreateRequestId()` with UUID format validation (`isValidRequestId()`), `getCurrentUserId()`, `setRequestUser(userId, isGuest)`, `createRequestContext(request, options)` from Request object, `getApiContext()` with helper methods (`getDuration()`, `isAuthenticated()`, `isGuestUser()`), `getClientIp()` with x-vercel-forwarded-for support, `getSearchParams()`, `validateOrigin()` for CSRF protection, `withRequestContext(handler)` route handler wrapper. All OLD functions have structurally identical implementations in NEW. No `import "server-only"` in NEW — minor difference (AsyncLocalStorage would fail at runtime in browser regardless). No regression detected.
- `runWithRequestContext()` - Sync context wrapper
- `runWithRequestContextAsync()` - Async context wrapper
- `updateRequestContext()` - Update context fields
- `getRequestDuration()` - Elapsed time calculation
- `formatRequestContext()` - Context formatting for logs

**NEW Enhancements:**
- Additional context fields: `isGuest`, `clientIp`, `userAgent`
- `getApiContext()` - Context with helper methods
- `getClientIp()` - Extract client IP from headers
- `getSearchParams()` - URL search params helper
- `validateOrigin()` - CSRF origin validation
- `setRequestUser()` - Convenience for setting user
- `getOrCreateRequestId()` - With validation
- `withRequestContext()` - Route handler wrapper

**Impact:**
None - full functional equivalence with improvements.

---

#### [P5-FNC-064] Different Logging Architecture - Console vs OpenTelemetry

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/log.ts`
**NEW File:** `lib/log.ts`
**Line Ref:** L1-185 vs L1-510

**Description:**
OLD and NEW have fundamentally different logging approaches:

**OLD Approach (OpenTelemetry-based):**
- Logs to active OpenTelemetry span via `trace.getActiveSpan()`
- `span.addEvent("log", attributes)` for structured events
- `span.recordException()` for errors
- `span.setStatus()` for error status
- Client-side is no-op (returns early)
- Auto-injects request context from `getRequestContextFn`

**NEW Approach (Console-based):**
- Logs to console with structured formatting
- JSON format option via `LOG_FORMAT=json` env var
- `LogEntry` interface with timestamp, level, message, context, error
- `Logger` class for context-aware logging
- `timeAsync()` and `timeSync()` for performance timing
- Environment-aware log level filtering
- Works on both client and server

**Missing in NEW:**
- OpenTelemetry span integration
- `span.recordException()` for error tracking
- `span.setStatus()` for error status propagation

**NEW Features Not in OLD:**
- `logDebug()` function
- `Logger` class with `child()` method
- `timeAsync()` and `timeSync()` utilities
- JSON log format option
- Log level filtering via `LOG_LEVEL` env

**Impact:**
- Distributed tracing won't have log events correlated with spans
- Error tracking won't automatically set span status
- Need to configure separate OTel instrumentation for tracing

**Suggested Fix:**
Either:
1. Add OTel integration to NEW logger as optional transport
2. Accept console-based logging as architectural decision
3. Use both: console for logs, OTel for traces separately

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed architectural difference. OLD (`archive/oldapp/lib/log.ts:1-185`) is OpenTelemetry-native: imports `{ Attributes, SpanStatusCode, trace }` from `@opentelemetry/api`, gets active span via `trace.getActiveSpan()`, writes structured events via `span.addEvent("log", attributes)`, records exceptions via `span.recordException(detail)`, sets span error status via `span.setStatus({ code: SpanStatusCode.ERROR })`, and is a no-op on client-side. NEW (`lib/log.ts:1-510`) is console-based: uses `console.debug/info/warn/error`, has `LogEntry` interface with timestamp/level/message/context/error, supports JSON format via `LOG_FORMAT=json` env, log level filtering via `LOG_LEVEL` env, `Logger` class with `child()` for context-scoped logging, and `timeAsync()`/`timeSync()` performance utilities. Both share `injectRequestContextGetter()` for request context injection and have identical public API signatures (`logInfo`, `logWarn`, `logError`, `logPerf`). However, NEW's `getCurrentRequestContext()` stores the context getter but never writes to OTel spans — the function is present but the OTel backend is not connected. The project has `instrumentation.ts` at root but log events won't correlate with OTel traces. NEW provides broader functionality (client+server, Logger class, timing utils, level filtering) but loses distributed tracing correlation. This is an intentional architectural change per the v6 redesign, not a defect.

---

#### [P5-FNC-065] Missing MotionProvider Export

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/motion.tsx`
**NEW File:** N/A - Not found
**Line Ref:** L1-14

**Description:**
OLD has a dedicated motion module that:
- Exports `MotionProvider` component with `LazyMotion` for performance
- Re-exports `AnimatePresence` and `motion` (as `m`) from framer-motion
- Uses `domAnimation` features for reduced motion bundle

NEW doesn't have a dedicated motion provider file. The `features/chat/components/toolbar.tsx` imports directly from framer-motion without LazyMotion optimization.

**Impact:**
- Larger framer-motion bundle size without LazyMotion
- No centralized motion configuration
- Each component imports framer-motion directly

**Suggested Fix:**
Create `lib/motion.tsx` with `MotionProvider` and re-exports, or add to `components/` as shared utility.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Improvement |

**Findings:** The issue is partially inaccurate. NEW **does** have a motion module: `lib/motion.ts` (not `.tsx`) exists and re-exports `AnimatePresence`, `m`, `motion`, `domAnimation`, `domMax`, `domMin`, plus ~30 hooks and types from `framer-motion` — significantly more comprehensive than OLD which only re-exported `AnimatePresence` and `m as motion`. `components/version-footer.tsx:17` imports `motion` from `@/lib/motion`, confirming the module is actively used. However, two elements are genuinely absent from NEW: (1) `MotionProvider` component — OLD wraps children in `<LazyMotion features={domAnimation} strict>` for bundle size optimization; NEW has no equivalent React component wrapper. (2) `LazyMotion` usage — OLD's `LazyMotion` enables tree-shaking of framer-motion features, loading only `domAnimation` subset (~17KB vs ~34KB for full `motion`). Without `LazyMotion`, components using `motion.div` import the full animation engine. The NEW module re-exports `domAnimation` as a value but no component uses `<LazyMotion>`. Grep confirms zero matches for `MotionProvider` or `LazyMotion` in non-archive files. This is an optimization gap, not a functional break — animations work without `LazyMotion`, but the bundle is larger. Marking **Improvement** since the motion module exists with superior re-exports, and the `LazyMotion` wrapping is a performance optimization, not required functionality.

---

#### [P5-FNC-066] types.ts: No issues found - functionally equivalent

**Severity:** N/A
**Status:** Resolved
**OLD File:** `archive/oldapp/lib/types.ts`
**NEW File:** `features/chat/types.ts`
**Line Ref:** Full file

**Description:**
NEW `features/chat/types.ts` is a superset of OLD `lib/types.ts` with all functionality preserved:

**Preserved Types:**
- `ChatMessage` - UIMessage with custom types
- `CustomUIDataTypes` - Custom data types for streaming
- `MessageMetadata` - Message metadata with createdAt
- `ChatTools` - Tool definitions
- `UserVote` - Vote type
- `Attachment` - File attachment type
- `StreamingSuggestion` - Partial suggestion during streaming
- `ArtifactKind` - "text" | "code" | "image" | "sheet"
- `DataChatTitlePart`, `DataAppendMessagePart`, `DataUsagePart` - Data parts
- Type guards: `isDataChatTitlePart`, `isDataAppendMessagePart`, `isDataUsagePart`

**NEW Enhancements:**
- `WeatherOutput`, `DocumentOutput`, `SuggestionOutput` - Tool output types
- `AppUsage` defined inline (simpler than OLD's tokenlens import)
- Better organized with JSDoc comments
- Feature-scoped instead of global lib

**Impact:**
None - full functional equivalence with improvements.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified (Resolved) |

**Findings:** Confirmed resolved. NEW `features/chat/types.ts` provides full functional equivalence with OLD `archive/oldapp/lib/types.ts`. All critical types are present: `ChatMessage` (UIMessage with generics), `CustomUIDataTypes` (with all data keys including `textDelta`, `imageDelta`, `sheetDelta`, `codeDelta`, `suggestion`, `appendMessage`, `usage`, `chatTitle`), `MessageMetadata` (with `createdAt`), `ChatTools` (getWeather, createDocument, updateDocument, requestSuggestions), `UserVote`, `Attachment`, `StreamingSuggestion`, `ArtifactKind` ("text" | "code" | "image" | "sheet"), and all three data part types (`DataChatTitlePart`, `DataAppendMessagePart`, `DataUsagePart`) with their type guards (`isDataChatTitlePart`, `isDataAppendMessagePart`, `isDataUsagePart`). NEW adds enhancements: `WeatherOutput`, `DocumentOutput`, `SuggestionOutput` tool output types, `AppendMessageData` type alias, and inline `AppUsage` interface. The only notable difference is `AppUsage`: OLD uses intersection type with `tokenlens/helpers` (tracked separately in P5-FNC-067), NEW uses a simplified interface. No functional regression.

---

#### [P5-FNC-067] Missing tokenlens Integration for Usage Tracking

**Severity:** Medium
**Status:** Open
**OLD File:** `archive/oldapp/lib/usage.ts`
**NEW File:** `features/chat/types.ts` (inline definition)
**Line Ref:** L1-5 vs L179-188

**Description:**
OLD has dedicated usage type with tokenlens integration:

**OLD:**
```typescript
import type { LanguageModelUsage } from "ai";
import type { UsageData } from "tokenlens/helpers";

export type AppUsage = LanguageModelUsage & UsageData & { modelId?: string };
```

This merges:
- `LanguageModelUsage` from AI SDK (promptTokens, completionTokens)
- `UsageData` from tokenlens (cost calculation helpers)
- Custom `modelId` field

**NEW:**
```typescript
export interface AppUsage {
  totalTokens?: number;
  promptTokens?: number;
  completionTokens?: number;
  model?: string;
}
```

NEW has a simplified interface without tokenlens integration.

**Impact:**
- No automatic cost calculation from tokenlens
- No standardized usage data structure from AI SDK
- Simpler but less feature-rich

**Suggested Fix:**
Either:
1. Add tokenlens package and restore full `AppUsage` type
2. Accept simplified usage tracking as architectural decision
3. Create custom cost calculation if needed

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified |

**Findings:** Confirmed. OLD `archive/oldapp/lib/usage.ts:1-5` defines `AppUsage = LanguageModelUsage & UsageData & { modelId?: string }` — an intersection type merging AI SDK's `LanguageModelUsage` (promptTokens, completionTokens, totalTokens) with tokenlens's `UsageData` (cost fields, model pricing data) plus custom `modelId`. NEW `features/chat/types.ts:179-188` defines a simplified `AppUsage` interface with only 4 optional fields: `totalTokens`, `promptTokens`, `completionTokens`, `model`. The `tokenlens` package IS installed in NEW (`package.json:110`, version 1.3.0) and IS partially used: `components/ai-elements/context.tsx:14` imports `getUsage` from `tokenlens` for UI display of token costs. However, tokenlens is NOT integrated into the `AppUsage` type (no `UsageData` intersection) and NOT used in the AI pipeline. OLD's `chat-completion.ts:250` calls `getUsage()` from `tokenlens/helpers` in the `onFinish` callback to merge cost data into `AppUsage`, which then flows to clients via `data-usage` streaming events. Since `chat-completion.ts` doesn't exist in NEW (P5-BRK-001), the tokenlens pipeline integration is also absent. The component-level integration exists but the data model and pipeline integration are missing. The simplified `AppUsage` loses: `UsageData` cost fields, `LanguageModelUsage` type safety (which includes `totalTokens` as required, not optional), and the `modelId` field name (changed to `model`). This is a genuine functional gap — cost tracking data won't flow through the system even though the library is installed.

---

#### [P5-FNC-068] artifacts/server.ts: No issues found - functionally equivalent

**Severity:** N/A
**Status:** Resolved
**OLD File:** `archive/oldapp/lib/artifacts/server.ts`
**NEW File:** `features/artifact/handlers/`
**Line Ref:** Full file

**Description:**
NEW `features/artifact/handlers/` provides equivalent functionality with improved architecture:

**Preserved Functionality:**
- `DocumentHandler` interface with `kind`, `onCreateDocument`, `onUpdateDocument`
- `createDocumentHandler()` factory function
- Handler registry by artifact kind
- Document persistence after AI generation

**NEW Architecture Improvements:**
- `BaseArtifactHandler` abstract class for OOP approach
- Separate handler files: `text.handler.ts`, `code.handler.ts`, `sheet.handler.ts`, `image.handler.ts`
- `getArtifactHandler(kind)` helper function
- `supportedArtifactKinds` array derived from registry
- Better separation of concerns with feature-based structure

**Mapping:**
- OLD `documentHandlersByArtifactKind` -> NEW `artifactHandlersByKind`
- OLD `artifactKinds` -> NEW `supportedArtifactKinds`
- OLD `createDocumentHandler` -> NEW `createArtifactHandler`
- OLD `DocumentHandler` -> NEW `ArtifactHandler`

**Impact:**
None - full functional equivalence with architectural improvements.

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | Verified (Resolved) |

**Findings:** Confirmed resolved. NEW `features/artifact/handlers/` provides full functional equivalence with OLD `archive/oldapp/lib/artifacts/server.ts`. The handler architecture has been improved: OLD used a factory function pattern (`createDocumentHandler<Kind>(config)`), NEW uses a class-based pattern (`createArtifactHandler(config)`) with separate handler files for each artifact kind (`text.handler.ts`, `code.handler.ts`, `sheet.handler.ts`, `image.handler.ts`). All mappings are correct: `documentHandlersByArtifactKind` → `artifactHandlersByKind`, `artifactKinds` → `supportedArtifactKinds`, `DocumentHandler` → `ArtifactHandler`. The NEW adds `image.handler.ts` (P6-IMP-003) as an enhancement. The handler registration pattern, factory function, and kind-based routing are functionally equivalent. Note: individual handler implementations have separate issues (P6-BRK-003/004/005 for placeholder model strings) which are tracked under Phase 6.

---

#### [P5-FNC-069] Missing Response Utilities with Request ID Headers

**Severity:** Low
**Status:** Open
**OLD File:** `archive/oldapp/lib/api-context.ts`
**NEW File:** `lib/api/context.ts`
**Line Ref:** L69-132

**Description:**
OLD has response utilities that include request ID headers:

**Missing in NEW:**
1. `createResponseWithRequestId(body, init, requestId)` - Creates Response with X-Request-ID header
2. `jsonResponseWithRequestId(data, init, requestId)` - JSON response with request ID header
3. `addRequestContextToResponse(response, context)` - Add request ID and duration headers to existing response
4. `ResponseContext` type - `{ requestId: string; duration?: number }`
5. `setRequestUserId(userId)` - Convenience function (NEW has `setRequestUser` which is equivalent)

**NEW has:**
- `withRequestContext()` - Route handler wrapper (same)
- `setRequestUser(userId, isGuest)` - Enhanced with isGuest flag
- `formatRequestContext()` - Context formatting (same)

**Impact:**
- No standardized way to add request ID to response headers
- Clients can't correlate requests by X-Request-ID header
- No X-Response-Time header for duration tracking

**Suggested Fix:**
Add response utilities to `lib/api/response.ts` or `lib/api/context.ts`:
- `createResponseWithRequestId()`
- `jsonResponseWithRequestId()`
- `addRequestContextToResponse()`

**Verification:**
| Field | Value |
|-------|-------|
| **Verified By** | ouroboros-qa |
| **Timestamp** | 2026-02-16T21:00:00Z |
| **Status** | False Positive |

**Findings:** The issue claims "No standardized way to add request ID to response headers" — this is **incorrect**. NEW `lib/api/response.ts` provides comprehensive request ID support across all response builders:

1. **`createHeaders(requestId?)`** (L30-40) — internal helper that sets `X-Request-ID` on all responses when provided.
2. **Every response builder** accepts `{ requestId?: string }` option: `success(data, { requestId })`, `error(err, { requestId })`, `notFound(resource, id, { requestId })`, `unauthorized(msg, { requestId })`, `forbidden(msg, { requestId })`, `rateLimit(retryAfter, { requestId })`, `paginated(data, meta, { requestId })`, `stream(stream, { requestId })`, `json(data, { requestId })`, `validationError(msg, fields, { requestId })`, `successNoContent({ requestId })`.
3. **`withRequestId(response, requestId)`** (L590-598) — wraps an existing Response with X-Request-ID header, functionally equivalent to OLD `addRequestContextToResponse` but without `X-Response-Time`.

The **only genuinely missing element** is `X-Response-Time` duration header. OLD's `addRequestContextToResponse(response, { requestId, duration })` sets both `X-Request-ID` and `X-Response-Time: ${duration}ms`. NEW's `withRequestId()` only sets `X-Request-ID`. The issue is exported and used via `lib/api/index.ts:54`. The claim that "Clients can't correlate requests by X-Request-ID header" is false — all builders support it. The missing `X-Response-Time` is a minor gap, not the absence of the entire utility family described in the issue.

**Re-verified 2026-02-16: Confirmed False Positive.** All 11 response builders verified to accept `{ requestId?: string }` via `createHeaders()` internal helper. `withRequestId(response, requestId)` wraps existing responses. Only `X-Response-Time` is omitted — a minor monitoring header, not a functional gap.

---

## Improvement Only

_No improvement-only issues found in this phase. All issues represent missing functionality that needs to be addressed._

---

## NEW Improvements (Not in OLD)

The NEW codebase has several improvements not present in OLD:

### Core Utilities
1. **Date Utilities** (`lib/utils/date.ts`):
   - `isToday()`, `isYesterday()`, `startOfDay()`, `endOfDay()`, `addDays()`, `differenceInDays()`

2. **Format Utilities** (`lib/utils/format.ts`):
   - `formatDate()`, `formatRelativeTime()`, `formatFileSize()`, `formatDuration()`, `formatNumber()`

3. **String Utilities** (`lib/utils/string.ts`):
   - `truncate()`, `slugify()`, `capitalize()`, `sanitizeHtml()`

4. **Validation Utilities** (`lib/utils/validation.ts`):
   - `isValidEmail()`, `isValidUrl()`, `isValidUuid()`

5. **Error Message i18n** (`lib/errors/messages.ts`):
   - Internationalization-ready error messages with title, message, and action

6. **Feature Flags** (`lib/constants.ts`):
   - `FEATURE_FLAGS` object for feature toggles

7. **Rate Limiting Configuration** (`lib/constants.ts`):
   - `RATE_LIMITS` object with endpoint-specific limits

### AI Utilities
1. **Context Window Management** (`lib/ai/context-window.ts`)
   - `truncateMessages()` with multiple strategies
   - `validateContext()` for budget checking
   - `getContextWindowSize()` and `getMaxOutputTokens()`
   - `TokenBudget` interface

2. **Token Counting** (`lib/ai/token-counter.ts`)
   - `countTokens()` with provider-specific multipliers
   - `countMessagesTokens()` for conversation context
   - `countToolsTokens()` for tool definitions
   - `calculateTokenBudget()` for request planning

3. **Cleaner Provider Exports** (`lib/ai/providers.ts`)
   - Individual provider exports (`openai`, `google`, `xai`, etc.)
   - `isProviderAvailable()` helper
   - `getDefaultProvider()` with priority order

### API Utilities
1. **Request Context with AsyncLocalStorage** (`lib/api/context.ts`)
   - `runWithRequestContext()` - Context propagation across async boundaries
   - `getRequestContext()` - Access context anywhere in request
   - `getRequestDuration()` - Timing for performance monitoring

2. **Response Builders** (`lib/api/response.ts`)
   - `success()`, `error()`, `notFound()`, `unauthorized()`, `forbidden()`, `rateLimit()`
   - `paginated()` - Paginated response helper
   - `stream()` - Streaming response helper
   - Consistent response format with `ApiResponse<T>`

3. **Validation Utilities** (`lib/api/validation.ts`)
   - `validateBody()`, `validateQuery()`, `validateParams()`, `validateFormData()`
   - Safe variants that return results instead of throwing
   - Common schema helpers

### Data Layer
1. **Repository Pattern** (`lib/data/repositories/`)
   - `BaseRepository<T, NewT, UpdateT>` with CRUD operations
   - Consistent interface across all entities
   - Cache-through strategy built-in

2. **Service Layer** (`lib/data/services/`)
   - Business logic separated from data access
   - `ChatService`, `ArtifactService`, `AuthService`

3. **Query Layer** (`lib/data/queries/`)
   - Complex queries separated from repositories
   - `ChatQueries`, `UserQueries`

### Cache Layer
1. **Tiered Cache** (`lib/cache/tiered-cache.ts`)
   - L1 (memory) + L2 (Redis) caching
   - Automatic fallback when L2 unavailable

2. **LRU Memory Cache** (`lib/cache/memory-cache.ts`)
   - In-memory caching for hot data
   - Configurable max size and TTL

3. **Cache Strategies** (`lib/cache/strategies.ts`)
   - `cacheAside`, `cacheThrough`, `writeThrough`, `writeBehind`
   - Flexible caching patterns

4. **Cache Invalidation** (`lib/cache/invalidation.ts`)
   - Entity-specific invalidation functions
   - Pattern-based invalidation

5. **Redis Health Check** (`lib/cache/client.ts`)
   - `checkRedisHealth()` - Verify connectivity
   - `requireRedisClient()` - Throw if unavailable

### Auth Utilities
1. **Authorization Guards** (`lib/auth/guards.ts`)
   - `requireAuth()`, `requireAuthAction()`, `requireAuthenticatedUser()`
   - `requireOwnership()`, `canAccessChat()`
   - `optionalAuth()` - Get userId if available

2. **Session Utilities** (`lib/auth/session.ts`)
   - `requireSession()` - Throw if no session
   - `isAuthenticated()`, `isGuest()` - Helper functions
   - `getSessionUser()`, `getUserId()` - Convenience functions

---

## Migration Checklist

### Core Utilities
- [ ] Implement `fetcher` function for SWR
- [ ] Implement `fetchWithErrorHandlers` function
- [ ] Implement `generateUUID` function
- [ ] Implement `getMostRecentUserMessage` function
- [ ] Implement `getTrailingMessageId` function
- [ ] Implement `sanitizeText` function
- [ ] Implement `convertToUIMessages` function
- [ ] Implement `getTextFromMessage` function
- [ ] Migrate error handling to new `AppError` system or create adapter
- [ ] Implement `toDatabaseError` helper
- [ ] Implement PostgreSQL error code mapping
- [ ] Add guest-specific error messages
- [ ] Implement error visibility configuration
- [ ] Create file validation utilities module
- [ ] Verify pagination limit change is intentional
- [ ] Add backward compatibility exports for renamed constants

### AI Utilities
- [ ] Create `lib/ai/chat-completion.ts` with `executeChatCompletion`
- [ ] Create `lib/ai/prompts.ts` with all system prompts
- [ ] Create `lib/ai/title-generation.ts` with title functions
- [ ] Create `lib/ai/tools/` directory with 4 tool files
- [ ] Create `lib/ai/constants.ts` with AI configuration
- [ ] Create `lib/ai/entitlements.ts` or integrate elsewhere
- [ ] Create `lib/ai/types.ts` with model metadata types
- [ ] Add `ProviderId` type to `lib/ai/providers.ts`
- [ ] Add reasoning middleware support to providers
- [ ] Add `myProvider` export for backward compatibility
- [ ] Add Cloudflare AI Gateway support
- [ ] Add Cloudflare Workers AI support
- [ ] Expand curated model list or implement discovery
- [ ] Add `getLanguageModel` alias function

### API Utilities
- [ ] Create `lib/api/guards.ts` with route-specific guards
- [ ] Add `requireRateLimitForRoute` function
- [ ] Add `requireOwnershipForRoute` function
- [ ] Add `requireResourceForRoute` function
- [ ] Add `parseTimestampForRoute` function
- [ ] Add `requireQueryParamForRoute` function

### Data Layer
- [ ] Implement guest-aware data strategy in repositories
- [ ] Add `saveWithContext` batch operation
- [ ] Add `deleteMessagesAfterTimestamp` method
- [ ] Add document version rollback support
- [ ] Verify `ChatWithMessages` type compatibility

### Cache Layer
- [ ] Add circuit breaker for Redis failures
- [ ] Consider ZSET-based message storage
- [ ] Implement entity-specific cache warming
- [ ] Add cache entity type definitions

### Auth Utilities
- [ ] Verify NextAuth integration is complete
- [ ] Add `getSessionFromToken` if needed
- [ ] Verify guest session security model
- [ ] Ensure client-side auth is properly configured

### Database Layer
- [ ] Create `lib/db/batch.ts` with batch operations
- [ ] Create `lib/db/pagination.ts` with cursor-based pagination
- [ ] Enhance `withTransaction` with operation name and performance tracking
- [ ] Add backward-compatible query function exports or update imports
- [ ] Verify message part conversion is handled
- [ ] Define `AppUsage` type or document type relaxation for `lastContext`

### Middleware Layer
- [ ] Create `lib/middleware/deduplication.ts` with request deduplication
- [ ] Verify `lib/rate-limit/` works in Edge runtime
- [ ] Add missing rate limit presets to `lib/constants.ts`
- [ ] Verify upload rate limit change is intentional (hourly -> per minute)
- [ ] Add token bucket algorithm support or document architectural decision

### Rate Limit Module
- [ ] Add backward-compatible `checkRateLimit` function wrapper
- [ ] Add `strictLimiter` and `generousLimiter` presets
- [ ] Verify upload rate limit window change is intentional

---

**Summary:**
- Total Issues: 69
- By Category: UI (0), BUG (1), BRK (5), FNC (63), IMP (0)
- By Severity: Critical (10), High (21), Medium (26), Low (12)
