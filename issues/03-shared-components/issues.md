# Phase 3: Shared Components - Issues

**Phase Name:** Shared Components
**Comparison Scope:** Chat, Messages, Message, Sidebar Components, Artifact Components
**Date Started:** 2026-02-14
**Date Completed:** 2026-02-14

---

## Issue Counts

| Category | Count |
|----------|-------|
| UI Inconsistencies | 5 |
| Bugs | 15 |
| Broken Code | 24 |
| Functional Discrepancies | 10 |
| Improvement Only | 5 |
| **Total** | **59** |

### By Severity

| Severity | Count |
|----------|-------|
| Critical | 9 |
| High | 17 |
| Medium | 22 |
| Low | 6 |

## Table of Contents

- [Issue Counts](#issue-counts)
- [UI Inconsistencies](#ui-inconsistencies)
- [Bugs](#bugs)
- [Broken Code](#broken-code)
- [Functional Discrepancies](#functional-discrepancies)
- [Improvement Only](#improvement-only)

---

## VERIFICATION SUMMARY

| Issue | Status | Timestamp |
|-------|--------|-----------|
| P3-UI-001 | Verified | 2026-02-16T00:00:00Z |
| P3-UI-002 | Verified | 2026-02-16T12:00:00Z |
| P3-UI-003 | Verified | 2026-02-16T12:00:00Z |
| P3-UI-004 | Verified | 2026-02-16T12:00:00Z |
| P3-UI-005 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-001 | Defect | 2026-02-16T12:00:00Z |
| P3-BUG-002 | Defect | 2026-02-16T12:00:00Z |
| P3-BUG-003 | Defect | 2026-02-16T12:00:00Z |
| P3-BUG-004 | Defect | 2026-02-16T12:00:00Z |
| P3-BUG-005 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-006 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-007 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-008 | Defect | 2026-02-16T12:00:00Z |
| P3-BUG-009 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-010 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-011 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-012 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-013 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-014 | Verified | 2026-02-16T12:00:00Z |
| P3-BUG-015 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-001 | Defect | 2026-02-16T12:00:00Z |
| P3-BRK-002 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-003 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-004 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-005 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-006 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-007 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-008 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-009 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-010 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-011 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-012 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-013 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-014 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-015 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-016 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-017 | Defect | 2026-02-16T12:00:00Z |
| P3-BRK-018 | Defect | 2026-02-16T12:00:00Z |
| P3-BRK-019 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-020 | Defect | 2026-02-16T12:00:00Z |
| P3-BRK-021 | Verified | 2026-02-16T12:00:00Z |
| P3-BRK-022 | Verified | 2026-02-16T18:00:00Z |
| P3-BRK-023 | Verified | 2026-02-16T18:00:00Z |
| P3-BRK-024 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-001 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-002 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-003 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-004 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-005 | Verified | 2026-02-16T18:00:00Z |
| P3-FNC-006 | Verified | 2026-02-16T19:00:00Z |
| P3-FNC-007 | Verified | 2026-02-16T19:00:00Z |
| P3-FNC-008 | Verified | 2026-02-16T19:00:00Z |
| P3-FNC-009 | Verified | 2026-02-16T19:00:00Z |
| P3-FNC-010 | Verified | 2026-02-16T19:00:00Z |
| P3-IMP-001 | Improvement | 2026-02-16T19:00:00Z |
| P3-IMP-002 | Improvement | 2026-02-16T19:00:00Z |
| P3-IMP-003 | Improvement | 2026-02-16T19:00:00Z |
| P3-IMP-004 | Improvement | 2026-02-16T18:00:00Z |
| P3-IMP-005 | Improvement | 2026-02-16T18:00:00Z |


## UI Inconsistencies

### [P3-UI-001] Chat Component Missing AnimatePresence for ThinkingMessage

| Field | Value |
|-------|-------|
| **Issue ID** | P3-UI-001 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/messages.tsx:170-176` |
| **NEW Path** | `features/chat/components/messages.tsx:205-209` |

**Description:**
The new Messages component doesn't use AnimatePresence for the ThinkingMessage, losing the smooth enter/exit animations.

**Impact:**
The "Thinking..." message appears/disappears abruptly instead of with a smooth fade animation. This creates a less polished user experience.

**Suggested Fix:**
Import AnimatePresence from motion library and wrap the ThinkingMessage for smooth animations.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T00:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (messages.tsx Footer callback) wraps the ThinkingMessage in `<AnimatePresence mode="wait">` imported from `@/lib/motion`. New code (messages.tsx:205-209) renders the ThinkingMessage with a bare conditional `{status === "submitted" && (<div>...</div>)}` — no AnimatePresence wrapper. The ThinkingMessage appears/disappears without enter/exit animations. Additionally, the old code uses Virtuoso with proper Header/Footer components that include the AnimatePresence in the Footer, while the new code uses simple map rendering with inline footer content. The animation regression is confirmed.

---

### [P3-UI-002] Message Component Missing Motion Animations

| Field | Value |
|-------|-------|
| **Issue ID** | P3-UI-002 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:61-67, 364-371` |
| **NEW Path** | `features/chat/components/message.tsx:98-101` |

**Description:**
The new Message component uses CSS animation classes instead of Framer Motion for enter/exit animations.

**Impact:**
Animations are less smooth and controllable. The ThinkingMessage doesn't have the exit animation with custom duration. The animation consistency across the app is reduced.

**Suggested Fix:**
Import motion from @/lib/motion and use motion.div for message animations.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/message.tsx`) imports `motion` from `@/lib/motion` and uses `<motion.div>` with `initial={{ opacity: 0 }}` and `animate={{ opacity: 1 }}` for the message wrapper (line 61-67), providing Framer Motion enter animations. The `ThinkingMessage` additionally has `exit={{ opacity: 0, transition: { duration: 0.5 } }}` and `transition={{ duration: 0.2 }}` (lines 364-371), enabling smooth exit animations. New code (`features/chat/components/message.tsx`) uses a plain `<div>` with CSS classes `animate-in fade-in-0` (line 98-101) for both `PurePreviewMessage` and `ThinkingMessage`. The CSS-based animation lacks exit animation support entirely — ThinkingMessage has no exit transition at all. The framer-motion `AnimatePresence` wrapper in the old Messages component was required for exit animations to work, so this is a confirmed regression in animation quality.

---

### [P3-UI-003] MessageEditor Missing UI Components

| Field | Value |
|-------|-------|
| **Issue ID** | P3-UI-003 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-editor.tsx:61-77` |
| **NEW Path** | `features/chat/components/message-editor.tsx:171-197` |

**Description:**
The new MessageEditor uses plain HTML elements instead of the Button and Textarea UI components.

**Impact:**
Styling inconsistencies with the rest of the app. The buttons and textarea may not match the design system. Focus states, disabled states, and hover effects may differ.

**Suggested Fix:**
Import and use Button and Textarea components from @/components/ui.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/message-editor.tsx`) imports `Button` from `./ui/button` (line 16) and `Textarea` from `./ui/textarea` (line 17), using them in the editor form (lines 61-77). These design system components provide consistent styling, focus states, disabled states, and hover effects across the app. New code (`features/chat/components/message-editor.tsx`) uses raw HTML `<textarea>` (line 171) and `<button>` (lines 183, 192) elements with manually applied Tailwind classes. While the manual classes approximate the design system styling, they are not guaranteed to stay in sync with the Button/Textarea component updates and miss features like the Button's proper variant system and the Textarea's built-in resize behavior. The old code also imports `deleteTrailingMessages` server action and `getTextFromMessage` utility from proper shared locations, while the new code has its own inline `getTextFromMessage` implementation and a `console.log` placeholder for `deleteTrailingMessages` (overlaps with P3-BUG-013).

---

### [P3-UI-004] Different Empty State Message in SidebarHistory

| Field | Value |
|-------|-------|
| **Issue ID** | P3-UI-004 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:514-516` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx:312-314` |

**Description:**
The empty state message differs between old and new apps. Old: "Your conversations will appear here once you start chatting!" New: "No chats yet. Start a new conversation!"

**Impact:**
Minor UX difference. The old message was more conversational.

**Suggested Fix:**
Use the original message for consistency.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx`) line 514 contains: `"Your conversations will appear here once you start chatting!"`. New code (`features/sidebar/components/sidebar-history.tsx`) line 312 contains: `"No chats yet. Start a new conversation!"`. The old message is more conversational and encouraging. Additionally, the old code checks `hasEmptyChatHistory && optimisticChats.length === 0` before showing the empty state, while the new code only checks `chats.length === 0`, missing the optimistic chats guard entirely (which is covered by P3-BRK-004).

---

### [P3-UI-005] Different Close Button Icon in Artifact Panel

| Field | Value |
|-------|-------|
| **Issue ID** | P3-UI-005 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact-close-button.tsx:3,25` |
| **NEW Path** | `features/artifact/components/artifact-close.tsx:11,44` |

**Description:**
The close button uses a different icon source (lucide-react X vs custom CrossIcon).

**Impact:**
Visual difference only. The icon may look slightly different.

**Suggested Fix:**
None required if the visual appearance is acceptable.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/artifact-close-button.tsx`) imports `CrossIcon` from `./icons` (line 3) and renders `<CrossIcon size={18} />` (line 25). New code (`features/artifact/components/artifact-close.tsx`) imports `X` from `lucide-react` (line 11) and renders `<X size={18} />` (line 44). Both render an "X" close icon but from different icon systems. The CrossIcon is a custom SVG while X is from the lucide-react library. The visual appearance may differ slightly in stroke width, line cap, and proportions. Additionally, the new component adds an optional `onClose` prop (enhanced with P3-IMP-004) that allows custom close behavior, which is an improvement over the old component.

---

## Bugs

### [P3-BUG-001] Chat Component Missing fetchWithErrorHandlers

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-001 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/chat.tsx:200-202` |
| **NEW Path** | `features/chat/components/chat.tsx:132-144` |

**Description:**
The new Chat component uses the default fetch instead of the custom fetchWithErrorHandlers that handles authentication errors and session expiry.

**Impact:**
Authentication errors and session expiry won't be handled properly. Users with expired sessions won't be redirected to login.

**Suggested Fix:**
Import and use fetchWithErrorHandlers from lib/utils or lib/api for proper error handling.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a defect — missing error handling for auth/session expiry. Old code (`archive/oldapp/components/chat.tsx`) imports `fetchWithErrorHandlers` from `@/lib/utils` and passes it to `DefaultChatTransport` via `fetch: fetchWithErrorHandlers` (line ~200). This custom fetch wrapper handles authentication errors (401/403), session expiry, and redirects users to login when their session is invalid. New code (`features/chat/components/chat.tsx` lines 132-144) creates `DefaultChatTransport` without a `fetch` property, falling back to the browser's native `window.fetch`. This means 401/403 responses from `/api/chat` will not trigger login redirects. Users with expired sessions will see a generic error instead of being redirected. Additionally, `generateId` uses `crypto.randomUUID()` instead of importing `generateUUID` from `@/lib/utils` — a minor deviation but functionally equivalent.

---

### [P3-BUG-002] Chat Component Missing Data Stream Handlers

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-002 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/chat.tsx:216-251` |
| **NEW Path** | `features/chat/components/chat.tsx:146-152` |

**Description:**
The new Chat component's onData handler only handles "data-usage" type, missing handlers for artifact streaming, title updates, and message appending.

**Impact:**
Artifacts won't stream in real-time, chat titles won't update optimistically, and dynamically appended messages won't appear.

**Suggested Fix:**
Add all data type handlers from the old app, including artifact streaming, title updates, and message appending.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a defect — three critical data stream handlers are missing. Old code (`archive/oldapp/components/chat.tsx` lines 216-251) handles four data part types in `onData`: (1) artifact streaming via `setDataStream` (gated by `settings.streamArtifacts`), (2) `data-usage` via `setUsage`, (3) `isDataChatTitlePart` → `updateOptimisticChatTitle(id, dataPart.data)` for real-time sidebar title updates, (4) `isDataAppendMessagePart` → parse and `setMessages` for dynamically appended messages. New code (`features/chat/components/chat.tsx` lines 146-152) only handles `data-usage`. Missing handlers cause: (a) Artifacts won't stream in real-time during AI responses, (b) Chat titles in the sidebar won't update until page refresh, (c) Server-appended messages (e.g., follow-up system messages) won't appear. All three are functional regressions affecting core chat features.

---

### [P3-BUG-003] Messages Component Missing useDataStream Hook

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-003 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/messages.tsx:46` |
| **NEW Path** | `features/chat/components/messages.tsx` |

**Description:**
The new Messages component doesn't call useDataStream() hook which is required for artifact streaming to work.

**Impact:**
Artifact streaming won't work. The data stream context won't be consumed, so real-time artifact updates during AI response streaming won't be displayed.

**Suggested Fix:**
Import and call useDataStream hook from the data-stream-provider.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a defect — artifact streaming context not consumed. Old code (`archive/oldapp/components/messages.tsx`) calls `useDataStream()` at line 46 inside `PureMessages` and imports it from `./data-stream-provider` (line 12). This hook subscribes the Messages component to the data stream context, which is necessary for artifact streaming updates to propagate to child components (via the `DataStreamProvider` context). New code (`features/chat/components/messages.tsx`) has no import or call to `useDataStream` anywhere. Without this hook, the data stream context set by `Chat.onData → setDataStream` is never consumed at the Messages level, meaning real-time artifact updates during AI streaming will not be displayed. Note: even though P3-BUG-002 prevents data from entering the stream in the first place (missing `setDataStream` call), this issue is independently valid — even after BUG-002 is fixed, consuming the stream in Messages is still required.

---

### [P3-BUG-004] Messages Component Missing Auto-Scroll Setting Check

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-004 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/messages.tsx:47,57-66` |
| **NEW Path** | `features/chat/components/messages.tsx:89-94` |

**Description:**
The new Messages component doesn't check the autoScroll setting from user preferences before auto-scrolling.

**Impact:**
Users who have disabled auto-scroll in settings will still have the view scrolled automatically. This ignores user preferences.

**Suggested Fix:**
Import useSettingsSnapshot and check the autoScroll setting before auto-scrolling.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a defect — user preference for auto-scroll is ignored. Old code (`archive/oldapp/components/messages.tsx`) imports `useSettingsSnapshot` from `@/lib/ui/settings-store` and destructures `autoScroll` (line 47). The auto-scroll effect (lines 57-66) gates scrolling with `if (status === "submitted" && autoScroll)`, respecting the user's setting. New code (`features/chat/components/messages.tsx` lines 89-94) has `useEffect(() => { if (status === "submitted") { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }) } }, [status])` — always auto-scrolls without checking any setting. No import of `useSettingsSnapshot` exists. Users who disable auto-scroll in settings will still have the view forcefully scrolled, violating user preference. Additionally, the old code uses `requestAnimationFrame` wrapping and Virtuoso's `scrollToIndex` API which is smoother than `scrollIntoView`.

---

### [P3-BUG-005] Message Component Missing Tool Implementations

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-005 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:188-311` |
| **NEW Path** | `features/chat/components/message.tsx:248-263` |

**Description:**
The new Message component has placeholder implementations for all tool invocations instead of the full implementations from the old app.

**Impact:**
Weather display, document creation/updates, and suggestion requests won't render properly. Users will see generic "Tool result placeholder" instead of actual tool outputs.

**Suggested Fix:**
Implement all tool invocation UIs with their respective components (Weather, DocumentPreview, DocumentToolResult).

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/message.tsx:188-311`) has full implementations for four tools: `tool-getWeather` (renders `Weather` component wrapped in `Tool`/`ToolHeader`/`ToolContent`/`ToolInput`/`ToolOutput`), `tool-createDocument` (renders `DocumentPreview` with error handling), `tool-updateDocument` (renders `DocumentPreview` with `isUpdate` flag and error handling), and `tool-requestSuggestions` (renders `DocumentToolResult` with error handling). New code (`features/chat/components/message.tsx:248-263`) replaces ALL of these with a single generic placeholder: `if (type.startsWith("tool-")) { return <div>Tool: {type} / Tool result placeholder</div> }`. None of the old imports (`Weather`, `DocumentPreview`, `DocumentToolResult`, `Tool`, `ToolHeader`, `ToolContent`, `ToolInput`, `ToolOutput`) exist in the new file. All tool outputs are non-functional.

---

### [P3-BUG-006] Missing ChevronUp Icon in Dropdown Trigger

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-006 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-user-nav.tsx:87` |
| **NEW Path** | `features/sidebar/components/sidebar-user-nav.tsx:77-99` |

**Description:**
The new SidebarUserNav is missing the ChevronUp icon that indicates the button is a dropdown trigger.

**Impact:**
Users may not realize the user nav is clickable/expandable. Missing visual affordance for dropdown menu.

**Suggested Fix:**
Add ChevronUp icon from lucide-react to the SidebarMenuButton, positioned with `className="ml-auto"`.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-user-nav.tsx:87`) includes `<ChevronUp className="ml-auto" />` inside the loaded-state `SidebarMenuButton`, imported from `lucide-react` at line 3. New code (`features/sidebar/components/sidebar-user-nav.tsx:77-99`) has the same `SidebarMenuButton` but only contains `<Image>` and `<span>` — no `ChevronUp` icon. The `lucide-react` import for `ChevronUp` is also absent from the new file's imports. Users lose the visual affordance indicating the button opens a dropdown menu.

---

### [P3-BUG-007] Missing SWR Cache Clearing on Logout

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-007 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-user-nav.tsx:139-147` |
| **NEW Path** | `features/sidebar/components/sidebar-user-nav.tsx:136-143` |

**Description:**
The new implementation doesn't clear the SWR cache when signing out, which could show stale data.

**Impact:**
After logout, if user logs in as a different account, they might briefly see the previous user's chat history from the cached data.

**Suggested Fix:**
Add SWR cache clearing before redirect, or ensure the history API properly returns empty for unauthenticated users.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue describes a real difference, with nuance. Old code (`archive/oldapp/components/sidebar-user-nav.tsx:139-147`) uses `useSWRConfig().mutate` with `unstable_serialize(getChatHistoryPaginationKey)` to explicitly clear the SWR infinite cache on logout, because the old `sidebar-history.tsx` uses `useSWRInfinite` for chat history. New code (`features/sidebar/components/sidebar-user-nav.tsx:136-143`) only calls `router.push("/")` and `router.refresh()` after the server logout — no explicit cache clearing. Additionally, the new `sidebar-history.tsx` does NOT use SWR at all (it uses manual `useState` + `fetch`), so the specific SWR clearing mechanism isn't directly applicable. However, the core concern remains: the new implementation relies solely on `router.refresh()` to clear stale data, and the component's in-memory `chats` state array may persist briefly if the component isn't fully unmounted during navigation, risking a flash of the previous user's chat history after logging in as a different account.

---

### [P3-BUG-008] Missing Supabase Client signOut Call

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-008 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-user-nav.tsx:135-136` |
| **NEW Path** | `features/sidebar/components/sidebar-user-nav.tsx:137-142` |

**Description:**
The new implementation only calls the server-side logout API but doesn't call the Supabase client signOut.

**Impact:**
The Supabase client-side session may not be properly cleared, potentially causing auth state inconsistencies.

**Suggested Fix:**
Add `getSupabaseBrowserClient().auth.signOut()` after the logout API call.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a **defect with auth implications**. Old code (`archive/oldapp/components/sidebar-user-nav.tsx:135-136`) chains `fetch("/api/auth/logout")` → `getSupabaseBrowserClient().auth.signOut()` → cache clearing → redirect. This dual-call ensures both the server-side session (cookies) AND the client-side Supabase auth state are invalidated. New code (`features/sidebar/components/sidebar-user-nav.tsx:137-142`) only calls `fetch("/api/auth/logout")` → `router.push("/")` / `router.refresh()`. The Supabase client-side `signOut()` is completely absent, and `getSupabaseBrowserClient` is not imported. Consequences: (1) Supabase's in-memory auth state in the browser remains set to the logged-in user, (2) Supabase's `onAuthStateChange` listener won't fire a `SIGNED_OUT` event, (3) any component using `supabase.auth.getUser()` or `supabase.auth.getSession()` client-side will still see the old user until a full page reload clears the JS context. This is an auth state inconsistency risk — not a privilege escalation since server-side cookies are invalidated, but it can cause confusing UI behavior and potential data leakage if client-side code trusts the Supabase session.

---

### [P3-BUG-009] Missing isNewSession Optimization

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-009 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:185-198` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx:177-189` |

**Description:**
The new implementation always fetches chat history on mount, even for new guest sessions that have no history.

**Impact:**
Unnecessary API calls for new users/guests. Wastes server resources and adds latency to initial page load.

**Suggested Fix:**
Add isNewSession check from auth context and skip fetching when it's a new session.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:185-198`) imports `useAuth` and destructures `{ isNewSession }`, then uses it in the SWR key function: `if (isNewSession && pageIndex === 0) { return null; }` — this prevents the SWR fetch from firing for brand new sessions. New code (`features/sidebar/components/sidebar-history.tsx:177-189`) does not import `useAuth` at all, has no `isNewSession` check, and unconditionally fires `fetchChats()` in a `useEffect` on mount regardless of session state. Every new guest session will result in an unnecessary API call to `/api/history` that will return an empty result.

---

### [P3-BUG-010] Manual "Load More" Button Instead of Infinite Scroll

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-010 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:441-445` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx:347-364` |

**Description:**
The new implementation uses a manual "Load more" button instead of automatic infinite scroll via Virtuoso's endReached callback.

**Impact:**
Worse UX - users must click to load more instead of natural scrolling. The old app seamlessly loaded more chats as users scrolled down.

**Suggested Fix:**
Implement Virtuoso with endReached callback for automatic infinite scroll.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:441-445`) uses `GroupedVirtuoso` (imported from `react-virtuoso`) with an `endReached={handleEndReached}` callback that automatically triggers `setSize((size) => size + 1)` when the user scrolls to the bottom, providing seamless infinite scroll. New code (`features/sidebar/components/sidebar-history.tsx:347-364`) renders a manual `<button onClick={loadMore}>Load more</button>` below the chat list. The new implementation doesn't import `react-virtuoso` at all and uses simple `overflow-auto` on the container with `.map()` rendering. Users must explicitly click the button to load more chats instead of the automatic behavior.

---

### [P3-BUG-011] Missing Chat Deduplication by ID

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-011 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:347-350` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx:180-184` |

**Description:**
The new implementation doesn't deduplicate chats by ID, which could lead to duplicate entries if pagination returns overlapping data.

**Impact:**
Potential duplicate chat entries in sidebar if there are any race conditions or overlapping pagination results.

**Suggested Fix:**
Add deduplication logic when merging new chats into state.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:347-350`) deduplicates chats in the `groupedChats` memo using a `Map` keyed by chat ID: `const uniqueChats = Array.from(new Map(chatsFromHistory.map((chat) => [chat.id, chat])).values())`. This ensures overlapping pagination results or race conditions never produce duplicate entries. New code (`features/sidebar/components/sidebar-history.tsx:180-184`) appends fetched chats directly: `setChats((prev) => [...prev, ...data.chats])` with no deduplication step. The subsequent `groupChatsByDateWithBoundaries` call also operates on the raw `chats` array without deduplication. If the API returns any overlapping chat IDs between pages (e.g., due to concurrent insertions shifting pagination boundaries), duplicate sidebar entries will appear.

---

### [P3-BUG-012] MessageActions Missing SWR Cache Mutation

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-012 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-actions.tsx:92-123` |
| **NEW Path** | `features/chat/components/message-actions.tsx:255-269` |

**Description:**
The new MessageActions component doesn't update the SWR cache after voting, so the UI won't update optimistically.

**Impact:**
After voting, the UI won't update immediately. Users will see the old vote state until the page refreshes.

**Suggested Fix:**
Import useSWRConfig and add mutate calls to update the vote cache optimistically.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/message-actions.tsx:92-123`) imports `useSWRConfig` from `swr` and uses `mutate<UserVote[]>` inside the `toast.promise` success callback to optimistically update the SWR vote cache at key `/api/vote?chatId=${chatId}`. It surgically replaces the current vote entry: filters out the old vote for the message, then appends the new vote object with `{ revalidate: false }`. This causes the UI (vote button states) to update immediately without waiting for a page refresh. New code (`features/chat/components/message-actions.tsx:255-269`) has `handleUpvote`/`handleDownvote` that call `fetch("/api/vote")` wrapped in `toast.promise`, but the success callbacks only return a string — no `mutate` call, no SWR import, no cache update. After voting, the button states (disabled, aria-pressed) won't reflect the new vote until the data is re-fetched or the page reloads.

---

### [P3-BUG-013] MessageEditor Missing Server Action Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-013 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-editor.tsx:86-97` |
| **NEW Path** | `features/chat/components/message-editor.tsx:134-147` |

**Description:**
The new MessageEditor doesn't call the deleteTrailingMessages server action, using console.log instead.

**Impact:**
Message editing is broken. When a user edits a message, the trailing messages (AI responses) won't be deleted from the database.

**Suggested Fix:**
Import and call the deleteTrailingMessages server action from the chat feature.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real bug. Old code (`archive/oldapp/components/message-editor.tsx:13,86-97`) imports `deleteTrailingMessages` from `@/app/(chat)/actions` and calls `await deleteTrailingMessages({ chatId, createdAt })` in the submit handler. New code (`features/chat/components/message-editor.tsx:134-147`) replaces the server action call with `console.log("deleteTrailingMessages", { chatId, createdAt })` and a comment "Note: This would typically call a server action / For now, we'll update the messages directly". As a result, editing a message will NOT delete trailing AI responses from the database — they will persist alongside the regenerated response. The `setMessages` and `regenerate` calls still work client-side, but the database becomes inconsistent. This is a confirmed functional bug.

---

### [P3-BUG-014] Missing Error Toast on Logout Failure

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-014 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-user-nav.tsx:151-156` |
| **NEW Path** | `features/sidebar/components/sidebar-user-nav.tsx:136-143` |

**Description:**
The new implementation doesn't show an error toast if logout fails.

**Impact:**
Users won't know if logout failed. They might think they're logged out when they're not.

**Suggested Fix:**
Add try/catch or .catch() handler with error toast notification.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real bug. Old code (`archive/oldapp/components/sidebar-user-nav.tsx:148-156`) chains the logout flow with `.then(() => getSupabaseBrowserClient().auth.signOut()).then(() => { mutate(...); router.push("/"); router.refresh(); }).catch(() => { toast({ type: "error", description: "Failed to sign out, please try again." }); })`. New code (`features/sidebar/components/sidebar-user-nav.tsx:136-143`) calls `fetch("/api/auth/logout", ...).then(() => { router.push("/"); router.refresh() })` with NO `.catch()` handler. If the logout request fails (network error, server 500, timeout), the Promise rejection is unhandled — the user receives no error feedback and is not redirected. Confirmed missing error path.

---

### [P3-BUG-015] Missing Loading State Toast

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BUG-015 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-user-nav.tsx:117-124` |
| **NEW Path** | `features/sidebar/components/sidebar-user-nav.tsx:128-130` |

**Description:**
The old app showed a toast when clicking logout while auth status was still loading.

**Impact:**
Users clicking logout during initial auth check get no feedback about why nothing happened.

**Suggested Fix:**
Add toast notification when status is "loading" to inform user to wait.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-user-nav.tsx:117-124`) shows a toast when the user clicks logout while auth status is loading: `toast({ type: "error", description: "Checking authentication status, please try again!" }); return;`. New code (`features/sidebar/components/sidebar-user-nav.tsx:128-130`) has: `if (status === "loading") { // Still loading, wait \n return }` — silently returns with only a code comment, no user-visible feedback. The user clicks the logout/login button and nothing happens, with no indication why. This is a UX regression affecting discoverability.

---

## Broken Code

### [P3-BRK-001] Chat Component Missing Core Functionality

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-001 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/chat.tsx:78-332` |
| **NEW Path** | `features/chat/components/chat.tsx:72-277` |

**Description:**
The new Chat component is a placeholder implementation missing critical functionality: visibility toggle integration, artifact integration, settings integration, optimistic chat updates, model persistence to localStorage, query parameter handling, error handling with credit card alerts, and proper streaming data handlers.

**Impact:**
The chat experience is severely degraded. Users cannot: change chat visibility, use artifacts, have model selection persist, see new chats appear optimistically in sidebar, handle URL query parameters, or get proper error feedback for billing issues.

**Suggested Fix:**
Complete the Chat component implementation by integrating all missing hooks and features from the old app.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a critical functional defect — confirmed by line-by-line comparison. The old Chat component (`archive/oldapp/components/chat.tsx:78-332`) integrates ~15 hooks/features that are completely absent from the new implementation (`features/chat/components/chat.tsx:72-277`):

**Missing hooks/integrations:**
- `useChatVisibility` — visibility toggle broken
- `useDataStream` / `setDataStream` — artifact streaming broken
- `useSettings` / `setSelectedModelId` — settings not integrated, model not persisted to localStorage
- `useOptimisticChats` — new chats won't appear in sidebar until DB persistence
- `useArtifact` / `useArtifactSelector` — artifact panel completely disconnected
- `useAuth` / `clearNewSessionFlag` — session flag not cleared
- `useSearchParams` — URL `?query=` parameter not handled

**Missing error handling:**
- No `fetchWithErrorHandlers` in transport — auth errors/session expiry unhandled
- No `onError` callback — no `ChatSDKError` handling, no credit card alerts, no toast feedback
- No `onFinish` — no title poll timers for sidebar title updates

**Missing `onData` handlers:**
- Only handles `data-usage`; missing `isDataChatTitlePart` (title updates), `isDataAppendMessagePart` (appended messages), and artifact stream forwarding

**Missing UI:**
- Placeholder `<select>` instead of `ChatHeader` component
- Placeholder `<textarea>` instead of `MultimodalInput` component
- No `Artifact` component rendered
- No `AlertDialog` for Vercel AI Gateway credit card

This is a scaffold/placeholder, not a functional implementation. The chat experience is severely broken.

---

### [P3-BRK-002] Messages Component Missing Virtualization

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-002 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/messages.tsx:201-215` |
| **NEW Path** | `features/chat/components/messages.tsx:150-187` |

**Description:**
The new Messages component uses a simple map-based render instead of Virtuoso virtualization, which will cause severe performance issues with large message lists.

**Impact:**
With large chat histories (100+ messages), the browser will struggle to render all messages at once, causing: slow initial render, laggy scrolling, high memory usage, and potential browser crashes on low-end devices.

**Suggested Fix:**
Integrate react-virtuoso library and restore the virtualized rendering approach from the old app.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/messages.tsx:3,201-215`) imports `Virtuoso, type VirtuosoHandle` from `react-virtuoso` and renders messages via `<Virtuoso data={renderableMessages} itemContent={itemContent} followOutput="smooth" increaseViewportBy={{ top: 200, bottom: 200 }} atBottomStateChange={handleAtBottomStateChange} />` with proper `Header` and `Footer` callback components. New code (`features/chat/components/messages.tsx:150-187`) renders via `renderableMessages.map((message, index) => ...)` inside a scrollable div. No `react-virtuoso` import exists. Scroll management is manual via `scrollContainerRef.current` and `messagesEndRef.current?.scrollIntoView()`. Additionally, the new code does not call `useDataStream()` (confirming P3-BUG-003) and does not check `autoScroll` setting (confirming P3-BUG-004). For large chat histories (100+ messages), all messages render simultaneously, causing proportional DOM node count, memory usage, and rendering cost.

---

### [P3-BRK-003] Missing Virtualization (GroupedVirtuoso) in SidebarHistory

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-003 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:529-544` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx:324-344` |

**Description:**
The new SidebarHistory uses simple map rendering instead of GroupedVirtuoso virtualization. This will cause severe performance issues with large chat histories (100+ chats).

**Impact:**
With hundreds of chats, the browser will struggle to render all DOM nodes, causing laggy scrolling, high memory usage, and potential crashes on mobile devices.

**Suggested Fix:**
Install react-virtuoso and implement GroupedVirtuoso with the same configuration as the old app.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:529-544`) uses `<GroupedVirtuoso groupContent={renderGroupContent} groupCounts={groupCounts} itemContent={renderItemContent} endReached={handleEndReached} increaseViewportBy={{ top: 200, bottom: 200 }} />` from `react-virtuoso`. New code (`features/sidebar/components/sidebar-history.tsx:324-344`) uses `chatGroups.map((group) => ...)` with nested `group.items.map((chat) => ...)`. No `react-virtuoso` import exists. Instead of automatic `endReached` infinite scroll, the new code has a manual "Load more" button (`<button onClick={loadMore}>Load more</button>` at line 347-364). The old `convertToVirtuosoGroups` has been replaced with `convertToGroups` which has no optimistic chats parameter. All chat items render as DOM nodes simultaneously. This will cause performance degradation with hundreds of chats, especially on mobile.

---

### [P3-BRK-004] Missing Optimistic Chats Integration in SidebarHistory

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-004 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:178` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx` |

**Description:**
The new SidebarHistory has no integration with optimistic chats. New chats won't appear in the sidebar until they're persisted to the database.

**Impact:**
When users start a new chat, it won't appear in the sidebar history immediately. They must refresh or wait for the chat to be saved.

**Suggested Fix:**
Integrate useOptimisticChats hook and implement the optimistic chat handling logic from the old app.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:178`) imports `useOptimisticChats` and destructures `{ optimisticChats, removeOptimisticChat }`. Optimistic chats are passed to `convertToVirtuosoGroups(groupedChats, optimisticChats as Chat[])` and rendered in a pseudo-group `"__optimistic__"` prepended to the "Today" group. Lines 219-249 handle removing optimistic chats once real chats arrive from the API (with deduplication via `processedOptimisticIds` ref). Empty state checks `hasEmptyChatHistory && optimisticChats.length === 0`. New code (`features/sidebar/components/sidebar-history.tsx`) has zero references to `useOptimisticChats`, no optimistic chats parameter in `convertToGroups`, and empty state checks only `chats.length === 0`. When a user starts a new chat, the Chat component (old) calls `addOptimisticChat(id, initialTitle)` but the new SidebarHistory has no way to display it. The chat only appears after the API call returns the persisted chat — a significant UX delay.

---

### [P3-BRK-005] Missing Chat Title Update Event Listener

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-005 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/sidebar-history.tsx:257-267` |
| **NEW Path** | `features/sidebar/components/sidebar-history.tsx` |

**Description:**
The new SidebarHistory doesn't listen for the 'chat-title-updated' custom event that triggers history revalidation when titles are generated.

**Impact:**
When a chat title is generated asynchronously (for short responses), the sidebar won't update to show the new title. Users will see "Untitled" or old titles until they manually refresh.

**Suggested Fix:**
Add useEffect with window.addEventListener('chat-title-updated') that triggers a refetch of chat history.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/sidebar-history.tsx:257-267`) has a `useEffect` that adds a window event listener: `window.addEventListener("chat-title-updated", handleTitleUpdate)` where `handleTitleUpdate` calls `mutate()` (SWR revalidation) to refresh chat history. This event is dispatched by the old Chat component's `onFinish` callback (`archive/oldapp/components/chat.tsx:256-270`) via `window.dispatchEvent(new Event("chat-title-updated"))` on poll timers after streaming completes. New code (`features/sidebar/components/sidebar-history.tsx`) has no `window.addEventListener` calls, no event listener for `chat-title-updated`, and no SWR `mutate` (it uses manual `useState` for chats). Even if the old-style Chat component dispatches this event, the new SidebarHistory won't react to it. Titles generated asynchronously after short responses will remain stale in the sidebar until manual page refresh.

---

### [P3-BRK-006] Missing ArtifactMessages Component

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-006 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact-messages.tsx:1-115` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx:364-369` |

**Description:**
The ArtifactMessages component is completely missing from the new application. This component was responsible for rendering the message list inside the artifact panel.

**Impact:**
Users cannot view or interact with chat messages while an artifact is open. The entire left panel of the artifact view is non-functional.

**Suggested Fix:**
Create features/artifact/components/artifact-messages.tsx with full message rendering functionality.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real missing component. The old `archive/oldapp/components/artifact-messages.tsx` is a full 115-line component that renders chat messages inside the artifact panel using `PreviewMessage` per message, `AnimatePresence` wrapping `ThinkingMessage`, `motion.div` for viewport scroll detection, and a custom `areEqual` memo comparator that suppresses re-renders during artifact streaming. The new `features/artifact/components/artifact-panel.tsx` (lines ~358-368) replaces all of this with a static placeholder: `<p className="text-muted-foreground text-sm">Chat messages would appear here</p>`. No `ArtifactMessages` component exists anywhere in `features/artifact/components/` (directory listing confirms only: artifact-actions.tsx, artifact-close.tsx, artifact-error-boundary.tsx, artifact-panel.tsx, console.tsx, editors/, index.ts). The entire left panel of the artifact view is non-functional.

---

### [P3-BRK-007] Missing Artifact Class Definition

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-007 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/create-artifact.tsx:71-93` |
| **NEW Path** | `features/artifact/types.ts:95-105` |

**Description:**
The old app had a sophisticated `Artifact` class that defined artifact types with: kind, description, content component, actions array, toolbar items, initialize() method, and onStreamPart() handler. The new app only has TypeScript interfaces without the class implementation.

**Impact:**
The artifact plugin system is broken. The old app could register new artifact types dynamically. The new app has no mechanism to register or use artifact definitions.

**Suggested Fix:**
Either implement the Artifact class pattern or create a registration system for artifact types.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real architectural gap. The old `archive/oldapp/components/create-artifact.tsx` defines a full `Artifact` class (lines 71-93) with constructor accepting `ArtifactConfig<T, M>` containing: `kind`, `description`, `content` (ComponentType), `actions` (ArtifactAction[]), `toolbar` (ArtifactToolbarItem[]), `initialize()`, and `onStreamPart()`. All four artifact types (text, code, image, sheet) use `new Artifact({...})` to register themselves with full implementations. The new `features/artifact/types.ts` has an `ArtifactDefinition` interface (lines 95-105) that includes `kind`, `name`, `description`, `actions`, `content`, and optional `initialize`, but is **missing** `toolbar` and `onStreamPart` — two critical properties. Furthermore, the interface is never instantiated with real implementations; `artifact-actions.tsx` defines `defaultArtifactDefinitions` with **empty action arrays** for all four types, and `artifact-panel.tsx` defines `defaultArtifactRenderers` with placeholder `<pre>` renderers. The dynamic plugin registration system is completely absent.

---

### [P3-BRK-008] Missing MultimodalInput in Artifact Panel

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-008 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:397-416` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The old artifact panel included a MultimodalInput component at the bottom for sending messages while viewing artifacts. The new panel has no input capability.

**Impact:**
Users cannot send messages or interact with the chat while an artifact is open. The artifact panel becomes a read-only view.

**Suggested Fix:**
Add MultimodalInput component to the artifact panel, passing necessary props from the parent chat context.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real missing component. The old `archive/oldapp/components/artifact.tsx` (lines 397-416) renders `<MultimodalInput>` at the bottom of the left messages panel inside `<div className="relative flex w-full flex-row items-end gap-2 px-4 pb-4">`, passing 15 props (chatId, input, setInput, messages, setMessages, sendMessage, attachments, setAttachments, status, stop, selectedModelId, selectedVisibilityType, availableModels, className). grep search for "MultimodalInput" in `features/artifact/**` returned zero results. The new artifact panel has no input element whatsoever — the left panel is a static placeholder with just a `<p>` tag. Users cannot send messages, attach files, or interact with chat while an artifact is open. The artifact panel is read-only from the chat perspective.

---

### [P3-BRK-009] Missing Toolbar Component in Artifact Panel

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-009 |
| **Severity** | Critical |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:560-574` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The old artifact panel included a Toolbar component that appeared when the current version was displayed, providing quick actions like summarize, improve, etc.

**Impact:**
Users lose quick access to artifact-specific actions like "Summarize", "Improve", etc.

**Suggested Fix:**
Create a Toolbar component in features/artifact/components/toolbar.tsx and integrate it into the artifact panel.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real missing component. The old `archive/oldapp/components/toolbar.tsx` is a 490-line component providing: `Tool` sub-component with hover/select animations, `ReadingLevelSelector` with drag-based level picker, `Tools` container with AnimatePresence for secondary tools, and the main `PureToolbar` with auto-close timer, click-outside handling, streaming-aware visibility, and artifact-kind-specific tool loading from `artifactDefinitions`. It's used in `artifact.tsx` (lines 560-574) wrapped in `<AnimatePresence>` with `isCurrentVersion` guard. The old artifact types define toolbar items: textArtifact has 2 ("Add final polish", "Request suggestions"), codeArtifact has 2 ("Add comments", "Add logs"), sheetArtifact has 2 ("Format and clean data", "Analyze and visualize data"). grep search for "Toolbar" in `features/artifact/**` returned only a comment in artifact-actions.tsx. No Toolbar component exists in the new codebase's artifact feature. All quick-action toolbar functionality is absent.

---

### [P3-BRK-010] Missing Artifact Type Definitions

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-010 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/artifacts/` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx:48-148` |

**Description:**
The old app had a complete artifact type system defined in archive/oldapp/artifacts/: textArtifact, codeArtifact, imageArtifact, sheetArtifact. The new app has inline placeholder renderers that don't match the functionality.

**Impact:**
All artifact types lose their specialized functionality: Text artifacts lose rich text editing and suggestions; Code artifacts lose syntax highlighting and Python execution; Image artifacts lose editing capabilities; Sheet artifacts lose spreadsheet functionality.

**Suggested Fix:**
Migrate the artifact definitions from archive/oldapp/artifacts/ to the new feature structure.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real missing feature set. The old `archive/oldapp/artifacts/` directory contains four fully-implemented artifact type registrations:
- **text/client.tsx**: Rich text `Editor` (dynamic import), `DiffView`, suggestions via `getSuggestions()`, `onStreamPart` handling `data-suggestion` and `data-textDelta`, 4 actions (view changes, prev/next version, copy), 2 toolbar items.
- **code/client.tsx**: `CodeEditor` (dynamic import), `Console` component, full Python execution via Pyodide (dynamic script loading, matplotlib output capture, package loading), `onStreamPart` handling `data-codeDelta`, 4 actions (run/prev/next/copy), 2 toolbar items.
- **image/client.tsx**: `ImageEditor` component, `onStreamPart` handling `data-imageDelta`, 3 actions (prev/next/copy with canvas-based clipboard), no toolbar.
- **sheet/client.tsx**: `SpreadsheetEditor` (dynamic import), `onStreamPart` handling `data-sheetDelta`, 3 actions (prev/next/copy as CSV via papaparse), 2 toolbar items.

The new `artifact-panel.tsx` (lines 48-148) replaces ALL of these with `defaultArtifactRenderers` — four inline functions that render only `<pre className="whitespace-pre-wrap font-mono text-sm">{content}</pre>` with a loading spinner. No rich editors, no code execution, no image editing, no spreadsheet functionality, no streaming handlers. `artifact-actions.tsx` defines empty `actions: []` arrays for all types.

---

### [P3-BRK-011] Missing VersionFooter Component Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-011 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/version-footer.tsx:1-87` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx:434-450` |

**Description:**
The old app had a dedicated VersionFooter component for version navigation. The new app has an inline implementation that lacks the full functionality including restore version functionality.

**Impact:**
Users cannot restore previous versions of artifacts. The version history feature is incomplete.

**Suggested Fix:**
Import and use the VersionFooter component from components/version-footer.tsx.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real integration gap. The old `archive/oldapp/components/artifact.tsx` imports and renders `<VersionFooter>` from `./version-footer` wrapped in `<AnimatePresence>` with props `currentVersionIndex`, `documents`, `handleVersionChange`. The old `version-footer.tsx` provides: `motion.div` with spring slide-up/down animation, "You are viewing a previous version" message, "Restore this version" button (calls DELETE `/api/document?id=...&timestamp=...` with optimistic SWR cache updates using `isAfter` date filtering), loading spinner during restore, and "Back to latest version" button.

The new `components/version-footer.tsx` EXISTS in the new codebase with equivalent functionality (same restore API call, optimistic SWR updates, motion animations, loading state). However, the new `artifact-panel.tsx` does NOT import it. Instead, lines 434-450 have an inline implementation with only: `<span>Version X of Y</span>` and a `<button>Return to latest version</button>`. The inline version is missing: restore version functionality, motion animations, loading state, and descriptive messaging. The proper component exists but is not wired in.

---

### [P3-BRK-012] Missing AnimatePresence and Motion Animations

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-012 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:323-589` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx:349-452` |

**Description:**
The old artifact panel had sophisticated animations using Framer Motion for panel entrance/exit, smooth transitions between mobile and desktop layouts, bounding box animations, and overlay animations. The new panel has no animations at all.

**Impact:**
The artifact panel feels abrupt and jarring. No smooth transitions when opening/closing. The UX feels unpolished.

**Suggested Fix:**
Add Framer Motion animations using the pattern from the old app. Import from "@/lib/motion".

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real missing feature. The old `archive/oldapp/components/artifact.tsx` (lines 323-589) uses extensive Framer Motion animations:
- `AnimatePresence initial={false}` wrapping the entire visible artifact panel
- `motion.div` for background overlay with width animation from sidebar-adjusted to full width
- `motion.div` for left messages panel with spring animations (`delay: 0.1, stiffness: 300, damping: 30`), opacity/x/scale transitions
- `motion.div` for main artifact panel with bounding box → full screen animation (using `artifact.boundingBox` initial position, transitioning to full `windowWidth`/`windowHeight`), `borderRadius: 50 → 0` morph, spring physics
- Nested `AnimatePresence` for version overlay (opacity transition), toolbar, version footer
- Exit animations: scale 0.5 + opacity 0 with spring + delay

grep search for `AnimatePresence` and `motion\.` in `features/artifact/components/artifact-panel.tsx` returned ZERO results. The new panel uses plain `<div>` elements with no entry/exit animations, no spring transitions, no bounding box morphing, no overlay fades. The `@/lib/motion` module exists and re-exports all Framer Motion utilities but is not imported by the artifact panel.

---

### [P3-BRK-013] Missing useWindowSize Hook in Artifact Panel

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-013 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:297-301` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The old artifact panel used useWindowSize for responsive behavior, detecting mobile vs desktop layouts. The new panel has hardcoded mobile handling.

**Impact:**
The artifact panel may not adapt correctly to different screen sizes. Mobile vs desktop behavior is not properly detected.

**Suggested Fix:**
Import and use useWindowSize from "@/hooks/use-window-size" for responsive behavior.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference, though impact is partially mitigated. The old `archive/oldapp/components/artifact.tsx` (lines 297-301) destructures `const { width: windowWidth, height: windowHeight, isMobile } = useWindowSize()` and uses these values for: (1) `isMobile` to conditionally hide/show the left messages panel and switch between mobile/desktop animation targets, (2) `windowWidth` and `windowHeight` as explicit pixel values in `motion.div` animate props for precise layout animations, (3) dynamic width calculation for the artifact panel (`windowWidth - 400` for desktop). The old `archive/oldapp/hooks/use-window-size.ts` has identical implementation to the new `hooks/use-window-size.ts`.

The new `artifact-panel.tsx` does NOT import `useWindowSize`. Instead it uses CSS: `className="hidden md:block"` for responsive hiding and `md:w-[calc(100dvw-400px)]` for width. This CSS approach handles basic responsive layout adequately. However, without runtime `windowWidth`/`windowHeight` values, the bounding box animations from P3-BRK-012 cannot function (they depend on computed pixel dimensions). As a standalone responsive issue, `md:` breakpoints are a reasonable CSS-only alternative; combined with the missing animation system, the hook is required.

---

### [P3-BRK-014] Missing useSidebar Hook Integration in Artifact Panel

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-014 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:109` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The old artifact panel integrated with the sidebar state to adjust layout when the sidebar was open/closed.

**Impact:**
The artifact panel doesn't adjust its layout when the sidebar opens/closes. This could cause overlap or layout issues.

**Suggested Fix:**
Import useSidebar and integrate sidebar state into layout calculations.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`artifact.tsx:109`) calls `const { open: isSidebarOpen } = useSidebar()` and uses `isSidebarOpen` in animation calculations at lines 340-350 to adjust panel exit/initial widths by 256px when the sidebar is open. The new `artifact-panel.tsx` has zero references to `useSidebar`, `isSidebarOpen`, or any sidebar state. The panel is rendered with fixed positioning and hardcoded widths (`w-[400px]` for the left panel, `md:w-[calc(100dvw-400px)]` for the main panel) with no sidebar awareness. This means the artifact panel won't adjust when the sidebar opens/closes, which could cause visual overlap.

---

### [P3-BRK-015] Missing Artifact Actions Implementation

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-015 |
| **Severity** | High |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/artifacts/text/client.tsx:100-150` |
| **NEW Path** | `features/artifact/components/artifact-actions.tsx:33-62` |

**Description:**
The new ArtifactActions component has empty actions arrays in defaultArtifactDefinitions. The old app had fully implemented actions for each artifact type.

**Impact:**
All artifact action buttons are missing. Users cannot copy content, undo/redo, or perform type-specific actions.

**Suggested Fix:**
Migrate artifact actions from archive/oldapp/artifacts/*/client.tsx files to the new artifact definitions.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code defines rich actions arrays per artifact type: **text** (4 actions: ClockRewind/view changes, Undo/prev version, Redo/next version, CopyIcon/clipboard), **code** (6 actions: Play/execute Python, ClockRewind, Undo, Redo, Copy, Logs/console toggle), **image** (3 actions: Undo, Redo, Copy image to clipboard). New `artifact-actions.tsx:33-62` defines `defaultArtifactDefinitions` with `actions: []` (empty arrays) for all four types (text, code, image, sheet). The `ArtifactActions` component correctly renders nothing when actions are empty (lines 97-100: `if (!artifactDefinition.actions || artifactDefinition.actions.length === 0) { return null; }`). All artifact action buttons are completely missing.

---

### [P3-BRK-016] Message Component Missing MessageReasoning Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-016 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:128-134` |
| **NEW Path** | `features/chat/components/message.tsx:157-174` |

**Description:**
The new Message component has an inline placeholder for reasoning instead of using the dedicated MessageReasoning component.

**Impact:**
Reasoning display lacks the collapsible UI, streaming indicators, and auto-close behavior from the MessageReasoning component.

**Suggested Fix:**
Import and use the MessageReasoning component from ./message-reasoning.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old `message.tsx:128-134` renders `<MessageReasoning isLoading={isLoading} key={key} reasoning={part.text} />` which uses the dedicated `Reasoning`, `ReasoningTrigger`, `ReasoningContent` element components (collapsible UI with streaming detection, `defaultOpen={hasBeenStreaming}`, and `isStreaming` indicator). New `message.tsx:157-174` uses an inline `<div className="rounded-lg border bg-muted/50 p-3">` with a hardcoded "Reasoning" title and `whitespace-pre-wrap` div — no collapsible behavior, no streaming indicator, no auto-open/close logic. Additionally, the new `features/chat/components/message-reasoning.tsx` exists as a standalone component with its own collapsible logic (lines 143-183) but is NOT imported or used by the new message component.

---

### [P3-BRK-017] Message Component Missing MessageEditor Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-017 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:166-185` |
| **NEW Path** | `features/chat/components/message.tsx:206-244` |

**Description:**
The new Message component has a placeholder edit mode instead of using the MessageEditor component.

**Impact:**
Message editing is broken. The placeholder doesn't call the server action to delete trailing messages, doesn't properly update the message state, and doesn't trigger regeneration.

**Suggested Fix:**
Import and use the MessageEditor component from ./message-editor.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a functional defect. Old `message.tsx:166-185` uses `<MessageEditor chatId={chatId} message={message} regenerate={regenerate} setMessages={setMessages} setMode={setMode} />` which calls `deleteTrailingMessages` server action, updates messages state, and triggers `regenerate()`. New `message.tsx:206-244` uses an inline `<textarea>` with plain HTML buttons. The "Save" button only calls `setMode("view")` — it does NOT delete trailing messages, does NOT update message content in state, and does NOT trigger regeneration. Message editing is completely non-functional. Furthermore, the new `features/chat/components/message-editor.tsx` exists with most of the correct logic EXCEPT it uses `console.log("deleteTrailingMessages", ...)` instead of calling the actual server action (line 140). So even if integrated, the editor would still not persist the edit to the database.

---

### [P3-BRK-018] Message Component Missing MessageActions Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-018 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:316-325` |
| **NEW Path** | `features/chat/components/message.tsx:268-293` |

**Description:**
The new Message component has inline action buttons instead of using the MessageActions component with proper voting functionality.

**Impact:**
Voting (upvote/downvote) is completely missing. Copy doesn't use the proper clipboard hook with toast feedback. The edit button doesn't work properly.

**Suggested Fix:**
Import and use the MessageActions component from ./message-actions.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a functional defect with complete feature failure. Old `message.tsx:316-325` uses `<MessageActions chatId={chatId} isLoading={isLoading} message={message} setMode={setMode} vote={vote} />` with proper voting (upvote/downvote via `/api/vote` with SWR cache mutation), clipboard copy via `useCopyToClipboard` with toast feedback, and hover-reveal edit button. New `message.tsx:268-293` uses inline emoji buttons (`✏️ Edit`, `📋 Copy`, `🔄 Regenerate`): (1) Copy button has no `onClick` handler — completely non-functional, (2) Voting (upvote/downvote) is entirely absent, (3) Edit button works but lacks the hover-reveal positioning. The `vote` prop is received but prefixed with underscore (`_vote`) indicating intentional non-use. A standalone `features/chat/components/message-actions.tsx` exists with proper voting logic but missing SWR cache mutation (as noted in P3-BUG-012), yet it is NOT imported by the message component.

---

### [P3-BRK-019] Message Component Missing PreviewAttachment

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-019 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:98-117` |
| **NEW Path** | `features/chat/components/message.tsx:133-151` |

**Description:**
The new Message component has a placeholder attachment display instead of using the PreviewAttachment component.

**Impact:**
File attachments display as plain text with an emoji instead of proper cards with file type icons and preview capabilities.

**Suggested Fix:**
Import and use PreviewAttachment component for proper attachment display.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old `message.tsx:98-117` uses `<PreviewAttachment attachment={{name, contentType, url}} />` which renders a `size-16` card with Next.js `Image` component for image previews, a "File" placeholder for non-images, a loading overlay with `Loader`, an optional remove button, and a filename overlay with gradient background. New `message.tsx:133-151` renders `<div className="rounded-lg border bg-muted p-2 text-sm">📄 {name}</div>` — a plain text div with an emoji. No image preview, no file type detection, no visual card design. The `PreviewAttachment` component exists at `archive/oldapp/components/preview-attachment.tsx` and would need to be migrated.

---

### [P3-BRK-020] Message Component Uses sanitizeHtml Instead of sanitizeText

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-020 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:159` |
| **NEW Path** | `features/chat/components/message.tsx:199` |

**Description:**
The new Message component uses sanitizeHtml while the old uses sanitizeText - these may have different sanitization behaviors.

**Impact:**
Different sanitization could lead to different output. sanitizeText may be more appropriate for plain text content.

**Suggested Fix:**
Verify sanitization functions are equivalent, or use sanitizeText. Consider using Response component for consistent text rendering.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Defect |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Confirmed as a defect — the functions are fundamentally different and the new one will break rendering. Old `sanitizeText` (`archive/oldapp/lib/utils.ts:176-178`) does `text.replace("<has_function_call>", "")` — it only strips a specific internal marker string. New `sanitizeHtml` (`lib/utils/string.ts:83-92`) performs full HTML entity encoding: replaces `&` → `&amp;`, `<` → `&lt;`, `>` → `&gt;`, `"` → `&quot;`, `'` → `&#x27;`, `/` → `&#x2F;`, `` ` `` → `&#x60;`, `=` → `&#x3D;`. When applied to AI-generated text, `sanitizeHtml` will corrupt all markdown (code blocks with `<`, `>`, links with `=`, etc.) and display raw HTML entities to the user. This is a breaking change that will make message content unreadable when it contains any of these common characters.

---

### [P3-BRK-021] Message Component Missing Response Component

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-021 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:158-161` |
| **NEW Path** | `features/chat/components/message.tsx:179-201` |

**Description:**
The new Message component doesn't use the Response component which may provide markdown rendering and other text processing.

**Impact:**
Markdown rendering, code highlighting, and other text processing from the Response component are missing. Messages display as plain text without formatting.

**Suggested Fix:**
Import and use Response component from elements/response or implement equivalent markdown rendering.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T12:00:00Z |

**Findings:** Issue accurately describes a real difference. Old `message.tsx:158-161` wraps text in `<MessageContent ...><Response>{sanitizeText(part.text)}</Response></MessageContent>`. `MessageContent` (from `elements/message.tsx`) provides consistent message container styling. `Response` (imported from `./elements/response`) was likely a Streamdown-based component for markdown rendering (note: the `MessageResponse` in `elements/message.tsx:306-318` wraps `Streamdown` — a streaming-compatible markdown renderer). New `message.tsx:179-201` renders `<div className="whitespace-pre-wrap">{sanitizeHtml(part.text)}</div>` — plain text with whitespace preservation only. No markdown rendering (no headers, code blocks, lists, bold, links, etc.), no `MessageContent` wrapper for consistent styling. Combined with P3-BRK-020's sanitization change, assistant messages will display as plain text with HTML entities instead of rendered markdown. This is a significant UX regression as AI responses heavily rely on markdown formatting.

---

### [P3-BRK-022] MessageReasoning Missing Collapsible Component Integration

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-022 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-reasoning.tsx:48-57` |
| **NEW Path** | `features/chat/components/message-reasoning.tsx:164-183` |

**Description:**
The new MessageReasoning uses a custom implementation instead of the Reasoning component from elements.

**Impact:**
The reasoning UI may look different from other collapsible elements in the app. The Reasoning component from elements may have additional features.

**Suggested Fix:**
Import and use Reasoning, ReasoningTrigger, ReasoningContent from elements/reasoning.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `archive/oldapp/components/message-reasoning.tsx` imports and uses `Reasoning`, `ReasoningTrigger`, `ReasoningContent` from `./elements/reasoning`, which is a Radix UI `Collapsible`-based compound component with: `@radix-ui/react-use-controllable-state` for open state management, `Streamdown` for streaming-compatible markdown rendering of reasoning content, duration tracking (seconds elapsed), focus-aware auto-close (won't close if user is focused within), and proper `CollapsibleContent` animation data attributes. The new `features/chat/components/message-reasoning.tsx` uses a fully custom implementation: inline SVG `BrainIcon` and `ChevronDownIcon`, raw `<button>` + `{isOpen && <div>}` conditional rendering (no Collapsible), plain text rendering (no Streamdown/markdown), a custom `Shimmer` component, and no duration tracking. Critically, the equivalent Collapsible-based components already exist in the new codebase at `components/ai-elements/reasoning.tsx` (exact port of the old elements) and `components/ai/reasoning/thinking.tsx` (wrapper `AIThinking` using them), but `features/chat/components/message-reasoning.tsx` does NOT import either. The custom implementation loses: (1) Radix Collapsible accessibility (keyboard support, ARIA attributes), (2) Streamdown markdown rendering, (3) duration display ("Thought for X seconds"), (4) focus-aware auto-close, (5) CSS animation data attributes for open/close transitions.

---

### [P3-BRK-023] MessageReasoning Missing Default Open Behavior

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-023 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-reasoning.tsx:51` |
| **NEW Path** | `features/chat/components/message-reasoning.tsx:143-152` |

**Description:**
The new MessageReasoning auto-closes after streaming ends, while the old version stayed open if it had been streaming.

**Impact:**
Users who want to read the reasoning after it completes need to manually re-open it.

**Suggested Fix:**
Remove the auto-close behavior or make it configurable. Keep the reasoning open after streaming ends like the old app.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real behavioral difference, though the description requires nuance. **Both** versions auto-close after streaming — the difference is in how they do it.

**Old behavior** (via `elements/reasoning.tsx` `Reasoning` component): Uses a `hasAutoClosed` state guard. Auto-close fires ONCE after streaming ends (1s delay). After auto-close, `setHasAutoClosed(true)` prevents repeated closing. If the user manually re-opens the collapsible after auto-close, it **stays open** permanently — the guard prevents re-triggering. Additionally, the old code checks `collapsibleRef.current?.contains(document.activeElement)` before closing, so it won't close if the user is actively reading/interacting with the content.

**New behavior** (`features/chat/components/message-reasoning.tsx:143-152`): The auto-close useEffect depends on `[hasBeenStreaming, isLoading, isOpen]`. It checks `if (hasBeenStreaming && !isLoading && isOpen)` — there is no `hasAutoClosed` guard. This means: (1) After streaming ends, it closes (correct). (2) If the user manually re-opens (`setIsOpen(true)`), the effect re-triggers because `isOpen` changed back to `true` and all conditions are still met — it closes AGAIN after 1 second. (3) This creates an infinite close loop where the user cannot keep the reasoning panel open after streaming completes. No focus check exists, so it will close even while the user is reading.

The real bug is worse than described: it's not just "auto-closes once" — it's an aggressive, repeating auto-close that makes it impossible to read completed reasoning content.

---

### [P3-BRK-024] Missing Diff Mode Implementation

| Field | Value |
|-------|-------|
| **Issue ID** | P3-BRK-024 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/artifacts/text/client.tsx:94-99` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The new artifact panel has mode state ("edit" | "diff") but the diff mode is not properly implemented. The old app had a DiffView component.

**Impact:**
The diff mode toggle doesn't work. Users cannot compare versions of artifacts.

**Suggested Fix:**
Implement diff view in the artifact renderers or add a DiffView component.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `archive/oldapp/artifacts/text/client.tsx` (lines 94-99) checks `if (mode === "diff")` in the content component and renders `<DiffView newContent={newContent} oldContent={oldContent} />`, a sophisticated Tiptap-based component (`archive/oldapp/components/diffview.tsx`) that uses `@tiptap/core` Editor, custom `DiffMark` for green/red highlighting, and the `diffEditor()` utility from `@/lib/editor/diff` to compute document-level diffs.

The new `features/artifact/components/artifact-panel.tsx` has diff mode infrastructure: `const [mode, setMode] = useState<"edit" | "diff">("edit")` (line 180), `handleVersionChange("toggle")` toggles between edit/diff (line 321), and `mode` is passed to `RendererComponent` as a prop (line 424). However, ALL four `defaultArtifactRenderers` (lines 48-148) completely IGNORE the `mode` prop — none check for `mode === "diff"`. No `DiffView` component exists in `features/artifact/`. The mode toggle changes internal state but has zero visual effect. grep search for `DiffView` in `features/artifact/**` returned only type definitions and no actual component imports or renderings.

---

## Functional Discrepancies

### [P3-FNC-001] Chat Component Missing Multiple Features

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-001 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/chat.tsx:78-100` |
| **NEW Path** | `features/chat/components/chat.tsx:72-81` |

**Description:**
The new Chat component is significantly simplified compared to the old one, missing several features: visibility toggle, artifact integration, settings integration, optimistic chat updates, and model persistence.

**Impact:**
Chat visibility cannot be changed, artifacts won't work, model selection isn't persisted, and new chats won't appear optimistically in sidebar.

**Suggested Fix:**
Complete the Chat component implementation with the missing hooks and features.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real functional gap. Line-by-line comparison confirms the new `features/chat/components/chat.tsx` is a scaffold missing multiple integrations present in the old `archive/oldapp/components/chat.tsx`:

| Feature | Old (archive/oldapp) | New (features/chat) |
|---------|---------------------|--------------------|
| `useChatVisibility` | ✅ Imported, used for visibility type management | ❌ Missing, `initialVisibilityType` passed directly (no runtime toggle) |
| `useDataStream`/`setDataStream` | ✅ Imported, called in `onData` for artifact streaming | ❌ Missing entirely |
| `useSettings`/`setSelectedModelId` | ✅ Settings integrated, model persisted to localStorage | ❌ Missing, no localStorage persistence |
| `useOptimisticChats` | ✅ `addOptimisticChat`/`removeOptimisticChat`/`updateOptimisticChatTitle` | ❌ Missing entirely |
| `useArtifact`/`useArtifactSelector` | ✅ Full artifact state management + reset on chat change | ❌ Missing entirely |
| `useAuth`/`clearNewSessionFlag` | ✅ Session flag cleared on first message | ❌ Missing entirely |
| `useSearchParams` (`?query=`) | ✅ Auto-sends query from URL, clears params | ❌ Missing entirely |
| `fetchWithErrorHandlers` | ✅ Custom fetch in DefaultChatTransport | ❌ Default browser fetch |
| `onError` handler | ✅ Full `ChatSDKError` handling, credit card alerts, toasts | ❌ No `onError` callback at all |
| `onFinish` handler | ✅ Title poll timers for sidebar update | ❌ No `onFinish` callback |
| `ChatHeader` component | ✅ Proper header component | ❌ Placeholder `<div>` with `<select>` |
| `MultimodalInput` component | ✅ Full multimodal input with attachments | ❌ Placeholder `<textarea>` |
| `Artifact` component | ✅ Dynamic import, full 17-prop integration | ❌ Not rendered at all |
| `AlertDialog` (credit card) | ✅ Full dialog with activate link | ❌ Missing entirely |

Note: This issue substantially overlaps with P3-BRK-001 (already verified as Defect with Critical severity). This FNC-001 issue is essentially a duplicate at medium severity, covering the same functional gaps from a feature-checklist perspective rather than a breaking-code perspective.

---

### [P3-FNC-002] Artifact Panel Props Significantly Reduced

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-002 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:59-93` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx:39-42` |

**Description:**
The old Artifact component accepted 17 props for full functionality. The new ArtifactPanel only accepts 2 props (chatId, isReadonly), both of which are unused.

**Impact:**
The artifact panel is disconnected from the chat context. It cannot send messages, handle attachments, or integrate with the chat system.

**Suggested Fix:**
Either pass necessary props from the parent component or use context/hooks to access chat state.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `PureArtifact` in `archive/oldapp/components/artifact.tsx` (lines 59-93) accepts 17 props: `chatId`, `input`, `setInput`, `status`, `stop`, `attachments`, `setAttachments`, `messages`, `setMessages`, `regenerate`, `votes`, `sendMessage`, `isReadonly`, `selectedVisibilityType`, `selectedModelId`, `availableModels`. These props connect the artifact panel to the chat context, enabling: (1) `ArtifactMessages` to render chat messages with votes, (2) `MultimodalInput` at the bottom for sending messages/attachments while viewing artifacts, (3) `Toolbar` with `sendMessage` for quick actions, (4) status-aware rendering (streaming vs idle).

The new `PureArtifactPanel` in `features/artifact/components/artifact-panel.tsx` (lines 152-156) accepts `ArtifactPanelInternalProps` with only `chatId` and `isReadonly`, both renamed with underscore prefix (`_chatId`, `_isReadonly`) — explicitly unused. The panel relies solely on `useArtifact()` hook for artifact state. A full `ArtifactPanelProps` interface with 17 props exists in `features/artifact/types.ts` (lines 128-158) matching the old interface, but it is NOT used by the actual component.

Consequences: The artifact panel cannot render messages, cannot accept user input, cannot execute toolbar actions, and has no awareness of chat streaming status. The full types.ts interface suggests the intent to eventually wire these props but the implementation is incomplete.

---

### [P3-FNC-003] Missing onStreamPart Handler

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-003 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/create-artifact.tsx:64-68, 78-82` |
| **NEW Path** | `features/artifact/types.ts` |

**Description:**
The old Artifact class had an onStreamPart handler for processing streaming data specific to each artifact type. The new types don't include this.

**Impact:**
Artifacts cannot handle streaming data specific to their type. The text artifact won't show suggestions during streaming.

**Suggested Fix:**
Add onStreamPart to the ArtifactDefinition interface and implement it in artifact handlers.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `Artifact` class in `archive/oldapp/components/create-artifact.tsx` (lines 78-82) has `onStreamPart` as a required property: `onStreamPart: (args: { setMetadata, setArtifact, streamPart }) => void`. Each artifact type implements it:
- **text**: Handles `data-suggestion` (appends to metadata.suggestions) and `data-textDelta` (appends to content, auto-shows panel at 400-450 chars)
- **code**: Handles `data-codeDelta` (appends to content)
- **image**: Handles `data-imageDelta` (appends to content)
- **sheet**: Handles `data-sheetDelta` (appends to content)

The new `ArtifactDefinition` interface in `features/artifact/types.ts` (lines 95-105) has `kind`, `name`, `description`, `actions`, `content`, and optional `initialize` — but NO `onStreamPart` property. grep for `onStreamPart` in `features/artifact/**` returned zero results. The old `ArtifactConfig` also had `toolbar: ArtifactToolbarItem[]` which is similarly absent from the new interface.

Without `onStreamPart`, the artifact system cannot process type-specific streaming data: text suggestions won't accumulate during streaming, content deltas won't be applied, and the auto-show behavior (panel becomes visible at 400+ chars of content) is lost.

---

### [P3-FNC-004] Missing Artifact Initialization

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-004 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/artifact.tsx:310-320` |
| **NEW Path** | `features/artifact/components/artifact-panel.tsx` |

**Description:**
The old artifact panel called artifactDefinition.initialize() when a document was loaded. The new panel doesn't have this.

**Impact:**
Artifact-type-specific initialization doesn't happen. Text artifacts won't load their suggestions from the database.

**Suggested Fix:**
Add initialization logic to the artifact panel, calling the artifact definition's initialize method if provided.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `archive/oldapp/components/artifact.tsx` (lines 310-320) has a `useEffect` that calls `safeArtifactDefinition.initialize({ documentId: artifact.documentId, setMetadata })` when `artifact.documentId !== "init"`. For text artifacts, this calls `getSuggestions({ documentId })` from `archive/oldapp/artifacts/text/actions.ts` and sets them into metadata, enabling the Editor to display suggestion markers.

The new `features/artifact/components/artifact-panel.tsx` has NO initialization logic — grep for `initialize` in the file returned zero results. The `ArtifactDefinition` interface in `types.ts` DOES include an optional `initialize` property (line 103), but: (1) none of the `defaultArtifactDefinitions` in `artifact-actions.tsx` implement it (all have only `kind`, `name`, `description`, `actions: []`, `content: () => null`), and (2) the panel component never calls it even if it were defined. The `defaultArtifactRenderers` in `artifact-panel.tsx` are simple inline render functions, not full artifact definition objects.

This means text artifact suggestions from the database will never be loaded when viewing an existing document, and any future artifact type requiring initialization (e.g., loading execution history for code artifacts) will also fail.

---

### [P3-FNC-005] Different Metadata Management Approach

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-005 |
| **Severity** | Medium |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/hooks/use-artifact.ts:103-124` |
| **NEW Path** | `features/artifact/hooks/use-artifact.ts:90` |

**Description:**
The old app used SWR for metadata persistence with documentId-based keys. The new app uses simple useState.

**Impact:**
Metadata is not persisted or cached per document. If the user switches between artifacts, metadata is lost.

**Suggested Fix:**
Consider using SWR for metadata caching, or document this as an intentional simplification.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Issue accurately describes a real difference. The old `archive/oldapp/hooks/use-artifact.ts` (lines 103-124) uses SWR for metadata with documentId-keyed caching: `useSWR<ArtifactMetadata>(() => artifact.documentId !== "init" ? \`artifact-metadata-${artifact.documentId}\` : null, null, { fallbackData: null, revalidateOnMount: true })`. Key benefits: (1) Each documentId gets its own SWR cache entry, (2) switching between artifacts preserves metadata in SWR's in-memory cache, (3) when returning to a previously viewed artifact, metadata (e.g., text suggestions) is instantly available without re-fetching, (4) the `setMetadata` wrapper bridges SWR's `mutate` to React's `SetStateAction` pattern for compatibility with artifact definitions.

The new `features/artifact/hooks/use-artifact.ts` (line 90) uses plain `useState<ArtifactMetadata>(null)` with a `useEffect` that sets metadata to `null` whenever `artifact.documentId` changes. This means: (1) only ONE metadata value exists at a time, (2) switching away from an artifact DESTROYS its metadata, (3) switching back requires re-initialization (re-calling `initialize()` which itself is also missing per P3-FNC-004). For text artifacts, this means switching away from a document and back loses all loaded suggestions, requiring a database re-fetch. For code artifacts, execution history / console output would be lost.

Note: Both approaches use SWR for the core artifact state (`useSWR<UIArtifact>("artifact", null, ...)`) — the difference is specifically in metadata management. The old approach is superior for multi-artifact workflows; the new approach may be acceptable for single-artifact-at-a-time usage but represents a functional regression.

---

### [P3-FNC-006] Chat Component Missing Greeting Import

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-006 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/messages.tsx:11` |
| **NEW Path** | `features/chat/components/messages.tsx:339-355` |

**Description:**
The new Messages component has an inline Greeting component instead of importing the dedicated one.

**Impact:**
The Greeting component exists separately but isn't used. This leads to code duplication.

**Suggested Fix:**
Import and use the Greeting component from ./greeting instead of the inline implementation.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Issue accurately describes a real code duplication problem. Old code (`archive/oldapp/components/messages.tsx:11`) imports `{ Greeting }` from `"./greeting"` and uses it at line ~190 for the empty state. A dedicated `Greeting` component exists at `features/chat/components/greeting.tsx` with motion animations (`motion.div` with opacity/y transitions, delays 0.5/0.6), "Hello there!" heading, and optional `availableModels` count display. New code (`features/chat/components/messages.tsx:339-355`) defines a completely different inline `Greeting` function with a `SparklesIcon`, "How can I help you today?" heading, and "Start a conversation or try one of the suggestions below" subtext — no motion animations, no model count display, and different visual design. The dedicated `features/chat/components/greeting.tsx` is never imported. This is confirmed code duplication with divergent implementations — the inline version is functionally inferior (no animations, no model awareness).

---

### [P3-FNC-007] Message Component Missing MessageContent Component

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-007 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:140-157` |
| **NEW Path** | `features/chat/components/message.tsx:179-201` |

**Description:**
The new Message component doesn't use the MessageContent component from elements for consistent message styling.

**Impact:**
Minor styling inconsistency. MessageContent may provide additional features like consistent padding, border radius, or theme support.

**Suggested Fix:**
Import and use MessageContent component from elements/message for consistency.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Issue accurately describes a real difference. Old code (`archive/oldapp/components/message.tsx:9`) imports `{ MessageContent }` from `"./elements/message"` and uses it at lines 140-157 as the wrapper: `<MessageContent className={cn({...})} data-testid="message-content" style={...}>`. `MessageContent` is a design-system element component providing consistent message container styling. New code (`features/chat/components/message.tsx:179-201`) replaces it with a plain `<div className={cn("message-content", {...})} data-testid="message-content" style={...}>`. The `"message-content"` CSS class is applied but doesn't reference the React component — it's just a string class name. The old `MessageContent` component may contain additional behavior (consistent padding, accessibility attributes, themed styling) beyond a plain div. Additionally, the old code wraps text in `<Response>{sanitizeText(part.text)}</Response>` for markdown rendering, while the new code uses `<div className="whitespace-pre-wrap">{sanitizeHtml(part.text)}</div>` (plain text). This compounds the styling inconsistency.

---

### [P3-FNC-008] Message Component Missing SparklesIcon Import

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-008 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message.tsx:19` |
| **NEW Path** | `features/chat/components/message.tsx:370-387` |

**Description:**
The new Message component has an inline SparklesIcon instead of importing from the icons file.

**Impact:**
Code duplication. The same icon is defined in multiple places.

**Suggested Fix:**
Import SparklesIcon from @/components/icons or a shared icons location.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Issue accurately describes confirmed code duplication. Old code (`archive/oldapp/components/message.tsx:19`) imports `{ SparklesIcon }` from `"./icons"` — a centralized icon module. The shared `components/icons.tsx` (line 406) exports `SparklesIcon` as a reusable component. New code (`features/chat/components/message.tsx:370-387`) defines an inline `function SparklesIcon({ size = 16 })` with the same SVG path, duplicating the icon definition. The `messages.tsx` file also defines its own inline `SparklesIcon` at lines 366-385 (same duplication). Both new files should import from `@/components/icons` instead. The SVG content is identical across all three definitions — confirmed pure duplication with no functional difference.

---

### [P3-FNC-009] MessageActions Missing Icon Imports

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-009 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-actions.tsx:8` |
| **NEW Path** | `features/chat/components/message-actions.tsx:64-151` |

**Description:**
The new MessageActions has inline SVG icons instead of importing from the icons file.

**Impact:**
Code duplication and potential inconsistency. Icons are defined inline instead of using the centralized icons file.

**Suggested Fix:**
Import icons from @/components/icons for consistency.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Issue accurately describes confirmed code duplication. Old code (`archive/oldapp/components/message-actions.tsx:8`) imports `{ CopyIcon, PencilEditIcon, ThumbDownIcon, ThumbUpIcon }` from `"./icons"` — centralized icon components. New code (`features/chat/components/message-actions.tsx:64-151`) defines four inline icon functions: `CopyIcon` (lines 64-82), `PencilEditIcon` (lines 87-106), `ThumbUpIcon` (lines 111-130), `ThumbDownIcon` (lines 135-151). All four icons duplicate SVG definitions that already exist in `@/components/icons`. Additionally, the new component also doesn't import the `Action`/`Actions` element components from `./elements/actions` (used by the old code), replacing them with custom `ActionButton`/`Actions` inline components — further duplication of button styling logic.

---

### [P3-FNC-010] MessageReasoning Missing Icon Import

| Field | Value |
|-------|-------|
| **Issue ID** | P3-FNC-010 |
| **Severity** | Low |
| **Status** | Open |
| **OLD Path** | `archive/oldapp/components/message-reasoning.tsx:5-8` |
| **NEW Path** | `features/chat/components/message-reasoning.tsx:34-78` |

**Description:**
The new MessageReasoning has inline SVG icons instead of importing from a shared location.

**Impact:**
Code duplication. Icons should be centralized for consistency.

**Suggested Fix:**
Use icons from a shared location or use the Reasoning component from elements.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Verified |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Issue accurately describes confirmed code duplication with broader scope than stated. Old code (`archive/oldapp/components/message-reasoning.tsx:5-8`) imports `{ Reasoning, ReasoningContent, ReasoningTrigger }` from `"./elements/reasoning"` — specialized element components that provide collapsible UI, streaming detection, and consistent styling. It uses NO inline icons; the `ReasoningTrigger` component itself contains the icon. New code (`features/chat/components/message-reasoning.tsx:34-78`) defines two inline icon components: `BrainIcon` (lines 34-53) and `ChevronDownIcon` (lines 58-78). Neither icon exists in `@/components/icons` (which has no BrainIcon), so this is partially justified — but the `ChevronDownIcon` duplicates common chevron patterns. More importantly, the new code also doesn't import the `Reasoning`/`ReasoningTrigger`/`ReasoningContent` element components, replacing the entire element system with a custom inline implementation. This is broader than just icon duplication — it's a complete departure from the element component architecture.

---

## Improvement Only

### [P3-IMP-001] SidebarItem Improved Memoization

| Field | Value |
|-------|-------|
| **Issue ID** | P3-IMP-001 |
| **Location** | `features/sidebar/components/sidebar-item.tsx` |

**Description:** SidebarItem improved memoization with chat.visibility and chat.id added to memo comparison.

**Status:** Enhancement - No action required.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Improvement |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Confirmed as a genuine improvement. Old code (`archive/oldapp/components/sidebar-history-item.tsx`) has a `memo` comparator that only checks `prevProps.isActive !== nextProps.isActive` and `prevProps.chat.title !== nextProps.chat.title` — two fields total. New code (`features/sidebar/components/sidebar-item.tsx`) checks four fields: `chat.id`, `isActive`, `chat.title`, and `chat.visibility`. The addition of `chat.id` prevents stale renders when the sidebar list reorders (different chat objects at the same list position). The addition of `chat.visibility` ensures the visibility icon (lock/globe) updates correctly when a user toggles between private/public via the dropdown. Without the `chat.visibility` check, the old code would show stale visibility state after toggling. This is a meaningful memoization improvement that prevents real UI staleness bugs.

---

### [P3-IMP-002] SidebarToggle Uses Lucide-React

| Field | Value |
|-------|-------|
| **Issue ID** | P3-IMP-002 |
| **Location** | `features/sidebar/components/sidebar-toggle.tsx` |

**Description:** SidebarToggle uses PanelLeft from lucide-react instead of custom icon.

**Status:** Enhancement - No action required.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Improvement |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Confirmed as a valid improvement, with nuance that two sidebar-toggle files exist. The file at `features/sidebar/components/sidebar-toggle.tsx` uses `PanelLeft` from `lucide-react` — a standardized icon library. The old code (`archive/oldapp/components/sidebar-toggle.tsx`) uses `SidebarLeftIcon` from `"./icons"` — a custom SVG component. However, the file at `components/sidebar-toggle.tsx` (the re-export/wrapper) still uses `SidebarLeftIcon` from `@/components/icons`, identical to the old approach. So the improvement exists specifically in the feature-based version. Using `lucide-react` is an improvement because: (1) it reduces custom icon maintenance, (2) `PanelLeft` is a well-recognized sidebar toggle icon, (3) it's consistent with other lucide-react usage throughout the codebase (e.g., `ArrowDownIcon`, `AlertCircle`, `RotateCcw` in messages.tsx). No functional regression.

---

### [P3-IMP-003] AppSidebar Uses Server Actions

| Field | Value |
|-------|-------|
| **Issue ID** | P3-IMP-003 |
| **Location** | `features/sidebar/components/sidebar.tsx` |

**Description:** AppSidebar uses deleteAllChats server action instead of direct fetch calls.

**Status:** Enhancement - No action required.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Improvement |
| **Verified At** | 2026-02-16T19:00:00Z |

**Findings:** Confirmed as a genuine architectural improvement. Old code (`archive/oldapp/components/app-sidebar.tsx:47-61`) uses a direct `fetch("/api/history", { method: "DELETE" })` call wrapped in `toast.promise` for delete-all, coupled with SWR cache mutation via `mutate(unstable_serialize(getChatHistoryPaginationKey))`. New code (`features/sidebar/components/sidebar.tsx`) imports `deleteAllChats` from `"../actions"` — a proper Next.js server action defined at `features/sidebar/actions/delete-chat.action.ts:52` that goes through the service layer (`chatService.deleteAllChats(ctx)`). Benefits: (1) type-safe server action vs raw fetch, (2) server-side validation/auth via the service layer, (3) follows the repository/service pattern mandated by the v6 architecture, (4) eliminates the need for a separate API route endpoint. The new code also adds loading state tracking (`isDeleting` state) and disables buttons during deletion — the old code relied on `toast.promise` alone without disabling the button. Minor trade-off: the old code cleared SWR cache explicitly; the new code relies on `router.push("/") + router.refresh()` which may not immediately clear in-memory state.

---

### [P3-IMP-004] Artifact Close Button Optional onClose

| Field | Value |
|-------|-------|
| **Issue ID** | P3-IMP-004 |
| **Location** | `features/artifact/components/artifact-close.tsx` |

**Description:** Artifact close button enhanced with optional onClose prop.

**Status:** Enhancement - No action required.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Improvement |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Confirmed as a genuine improvement. The old `archive/oldapp/components/artifact-close-button.tsx` has `PureArtifactCloseButton()` with no props — the close handler is hardcoded inline to always call `setArtifact()` with the streaming/idle logic. The new `features/artifact/components/artifact-close.tsx` accepts `{ onClose }: ArtifactCloseProps` where `ArtifactCloseProps = { onClose?: () => void }` (from `features/artifact/types.ts:166`). When `onClose` is provided, it calls `onClose()` instead of the default `setArtifact` logic, enabling parent components to customize close behavior (e.g., confirmation dialogs, cleanup). The default fallback preserves the same streaming/idle logic as the old component. The icon change (CrossIcon → lucide-react X) is cosmetic (covered by P3-UI-005). This is a pure enhancement with no regressions.

---

### [P3-IMP-005] Artifact Error Boundary TypeScript

| Field | Value |
|-------|-------|
| **Issue ID** | P3-IMP-005 |
| **Location** | `features/artifact/components/artifact-error-boundary.tsx` |

**Description:** Artifact error boundary uses proper override keyword.

**Status:** Enhancement - No action required.

#### Verification

| Field | Value |
|-------|-------|
| **Verification Status** | Improvement |
| **Verified At** | 2026-02-16T18:00:00Z |

**Findings:** Confirmed as a genuine improvement. Line-by-line comparison of old (`archive/oldapp/components/artifact-error-boundary.tsx`) and new (`features/artifact/components/artifact-error-boundary.tsx`) shows identical logic, state shape, `getDerivedStateFromError`, `componentDidCatch`, and render output (same fallback UI, same error display). The differences are: (1) New code adds `override` keyword to `componentDidCatch` and `render` methods, enforcing TypeScript's `--noImplicitOverride` correctness — this prevents accidental method name typos silently not overriding the parent method. (2) Props type extracted to a shared `ArtifactErrorBoundaryProps` interface in `features/artifact/types.ts` instead of inline type definition. (3) State type renamed from `ArtifactErrorBoundaryState` to `ErrorBoundaryState` (internal, no external impact). (4) Added JSDoc module documentation. All changes are TypeScript hygiene improvements with zero behavioral difference.

---
