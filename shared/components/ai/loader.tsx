"use client";

/**
 * Loader Wrapper
 *
 * Thin wrapper around AI Element Loader component.
 * Re-exports all components and types for consistency with other wrappers.
 */

// Re-export everything from the base component
export { Loader, type LoaderProps } from "@/components/ai-elements/loader";
