/**
 * Mock for server-only package
 *
 * The server-only package is used by Next.js to mark modules that should
 * only run on the server. In tests, we mock it as an empty module.
 *
 * @module src/test/mocks/server-only
 */

// Empty mock - server-only is just a marker package
export {}
